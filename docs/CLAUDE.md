<!-- updated: 2026-05-18T14:30:00Z -->
# docs/

## Purpose

Long-form SDK documentation. ADRs here are the source of truth for cross-cutting decisions; package-level READMEs are the short-form mirror.

## Contents

| File | Topic | Status |
|---|---|---|
| `adr/0001-sdk-go-multimodule-layout.md` | 4-layer architecture, 5 Go modules glued by `go.work` | Accepted |
| `adr/0002-sdk-errors-package.md` | Layered typed `errs` package, Public/Private split, registry, breaking changes | Accepted (§Registry superseded by ADR 0005) |
| `adr/0003-sdk-codec-package.md` | Universal codec surface (10 formats + baseenc) behind `Marshal/Unmarshal` dispatch | Accepted (M1+M2+M3+M4 shipped) |
| `adr/0004-sdk-bazel-build-system.md` | Bazel 9 as single build/test system; visibility replaces depguard | Accepted |
| `adr/0005-sdk-error-codes-dotted-quad.md` | `Code uint32` laid out `MM.LL.PP.SS`, wrap trail, CIDR-style `PrefixMatcher` | Accepted (supersedes ADR 0002 §Registry) |
| `adr/0006-sdk-error-code-registry-extension.md` | Registry assignments for logger v2 sinks/middleware + `kernel/ring` | Accepted (amends ADR 0005 §Registry) |
| `adr/0007-sdk-release-and-versioning.md` | Impact-driven patch tags `pkg/<major>/vX.Y.Z`, docs versioning via tag snapshots | Accepted |
| `adr/0008-readme-from-code-generation.md` | `README.md` generated from Go doc comments via gomarkdoc | Accepted |
| `adr/0009-pkg-public-module-resolvability.md` | Published `pkg/<major>` must be `go get`-resolvable + accessible (no local `replace`) | Accepted (requirement); impl Deferred |
| `adr/0010-kernel-recycler-primitive.md` | Generic kernel `Pool[T]` + `CappedPool[T]`; `buffer` + `core/codec/scratch` become consumers | Accepted |
| `adr/0011-kernel-snapshot-primitive.md` | Generic kernel `Value[T]` copy-on-write container; codec registry consolidates onto it | Accepted |
| `adr/0012-logger-writer-registry.md` | Named, config-driven `Sink` factory registry (`writer`); console/file in-tree + AWS s3/cloudwatch under third-party/ (root module) | Accepted (amends ADR 0005/0006 §Registry) |
| `adr/0013-sdk-crypto-domain.md` | Crypto domain — `AEAD` `Seal`/`Open` with hidden nonce; stdlib AES-256-GCM default, x/crypto schemes under third-party/ | Accepted (amends core purpose + ADR 0005 §Registry) |
| `adr/0014-sdk-transform-crypto-ports-config-topology.md` | The verb wave — `core/transform` (5th sibling), crypto MAC/Agreement/StreamSealer ports + KeyEnvelope/KeyTree, `logger.FromConfig` + `ConfigDecoder` | Accepted (amends core purpose + ADR 0012/0013 + ADR 0005 §Registry) |
| `adr/0015-sdk-logger-writer-taxonomy-and-rotation.md` | Writer taxonomy + `depTier` as a first-class property; default writers console+file; rotation off-by-default → compress + 24h archive + 7-day retention; dep-light DB/transport gate (WI-9 dbsink shipped; WI-10/WI-11 gated) | Accepted (amends `core/writer` purpose + ADR 0005 §Registry) |
| `adr/0016-sdk-process-supervision-domain.md` | OS process-supervision domain (`proc`): 6th core sibling; `process`/`signal`/`reaper`/`rlimit`/`cgroup`/`sdnotify` facades; central error block `0.2.6.*`; build-tag platform selection | Accepted |
| `adr/0017-pkg-bare-module-path.md` | Public module is the bare `…/pkg` (Go forbids the `/v1` suffix); code stays under `v1/`, imports unchanged; tag shape `pkg/vX.Y.Z`, first release `v0.1.0` alpha | Accepted (amends ADR 0009 path, ADR 0007 tag shape, ADR 0001 module path) |
| `adr/0018-sdk-cross-platform-portability.md` | Cross-platform portability strategy — two bars (build + runtime); uniform `UnsupportedPlatform` contract; `_linux`/`_unix`/`_bsd`/`_windows`/`_other` split; `bazel-ci.yml` cross-build job (build) + `e2e-vm.yml` (runtime, real kernels) gates; OpenBSD `RLIMIT_AS` precedent; native-backend roadmap (FreeBSD `rctl`, Windows Job Objects, BSD `procctl`) | Accepted |
| `adr/0019-pkg-errs-public-construction.md` | Public error construction API (`pkg/v1/errs.New`/`Wrap`/`Field` via non-panicking `kernel/errs.NewRuntime`) + third-party Major reservation `0x40–0x7F` (`MinAppMajor`/`MaxMajor`) | Accepted (amends ADR 0002 / 0005 §Registry) |
| `adr/0020-errs-audit-dual-reason-derivation.md` | errs AST audit accepts `screamingSnake(varName)` OR `screamingSnake(CodeConst − "Code")`; closes the `//:audit_sources` gap excluding ~10 namespaced emitters (ring + logger middleware/sinks) | Accepted (amends ADR 0005 §Semantics / formalises ADR 0006) |
| `adr/0021-sdk-codec-bson.md` | BSON codec (M5) over `go.mongodb.org/mongo-driver/bson` in `internal/service/codec/bson` (library-backed codec precedent, not third-party quarantine); non-streaming + Appender; error block `0.3.36.*` | Accepted (extends ADR 0003 §M5) |
| `adr/0022-sdk-codec-hcl.md` | HCL codec (M5) **quarantined** in `third-party/codec/hcl` (root module) because `hcl/v2` would **introduce** the banned `x/sys` into `internal/service` (ADR 0034 corrects the original "downgrade" wording); opt-in, NOT in `pkg/v1/codec`; first `third-party/codec/` subtree; error block `0.3.37.*` | Accepted (extends ADR 0003 §M5; follows ADR 0012 quarantine, contrasts ADR 0021) |
| `adr/0023-sdk-schema-codecs.md` | Schema codecs (M6): opt-in, under `third-party/codec/` — schema-bound (can't honour the universal round-trip contract); Protobuf concrete (`0.3.38.*`); Avro/Cap'n Proto deferred | Accepted (extends ADR 0003 §M6) |
| `adr/0024-sdk-id-domain.md` | Identifier-generation domain (`id`): 7th core sibling, `Generator`/`Scheme` registry (UUIDv4/v7, ULID, snowflake); stdlib-only, cross-OS; opens the Phase-B new-domain wave; error block `0.2.7.*`/`0.3.39.*`. Since extended in place with NanoID, KSUID and TypeID (same block; TypeID constructor-only per ADR 0031) | Accepted (amends `internal/core` purpose) |
| `adr/0025-sdk-cache-kernel.md` | Generic LRU+TTL `Cache[K,V]` as a **kernel** primitive (domain-neutral, stdlib-only, reuses `clock` for testable TTL); `Fetch` not `Get`; no error codes; `pkg/v1/cache` type aliases | Accepted (kernel primitive, Phase B) |
| `adr/0026-sdk-resilience-domain.md` | Reliability domain (`resilience`): 8th core sibling (no registry), composable `Runner` policies — retry/circuit-breaker/rate-limit/bulkhead/timeout, extended with fallback/hedging; error block `0.2.8.*` | Accepted (Phase B) |
| `adr/0027-sdk-metrics-domain.md` | Observability domain (`metrics`): 9th core sibling, Counter/Gauge/Histogram + `Meter` + `Exporter` registry; in-memory meter + the stdlib `text` and `prometheus` exporters; error blocks `0.2.9.*` + `0.3.45.*`; labels and the Prometheus text exposition format shipped in place; **OTLP/JSON shipped in ADR 0048**, OTLP/protobuf and Prometheus protobuf still deferred. **Re-shaped by ADR 0044**: the domain now speaks the OpenTelemetry metrics data model, implemented from the specification with zero OTel imports. | Accepted (Phase B) |
| `adr/0028-sdk-config-domain.md` | Configuration domain (`config`): 10th core sibling, `Source`/`Validator`/`Watcher` ports; env+file layering, JSON round-trip decode, cross-OS poll watcher; closes Phase B; error block `0.2.10.*` | Accepted (Phase B) |
| `adr/0029-sdk-net-domain.md` | Network domain (`net`): unified inbound + outbound transport | Accepted |
| `adr/0030-stdout-is-a-protocol-channel.md` | No SDK default writes to `os.Stdout`: the registered `text` metrics exporter targets stderr, `ConsoleStderr` becomes the `ConsoleStream` zero value (and an absent or empty `target` maps to it); stdout stays reachable by naming it | Accepted (amends ADR 0027 §Decision 2 / ADR 0015 §D3) |
| `adr/0031-policy-zero-values-are-never-inert.md` | A policy constructor never returns an inert policy: **clamp** where a working default needs no explanation (`BreakerConfig.OpenDuration` → 30s), **refuse** where any SDK-chosen value would be arbitrary (`RateLimiterConfig.Rate`, `NewTimeout`) via the `PolicyMisconfigured` sentinel `0.2.8.6` | Accepted (amends ADR 0026 §Decision 2) |
| `adr/0032-logger-slog-bridge.md` | Slog is an adapter at the public edge, never a dependency of the domain | Accepted |
| `adr/0033-consumer-rule-enforcement.md` | A rule the SDK cannot check is a suggestion; enforcement runs at build time, over source | Accepted |
| `adr/0034-hcl-quarantine-rationale-corrected.md` | The HCL quarantine stands; its stated mechanism does not. Under MVS a dependency requiring a lower version cannot downgrade a higher requirement; measured, adding `hcl/v2` to `internal/service` **introduces** `x/sys` (via `x/tools`) into a module that bans it | Accepted (amends ADR 0022 §Context.1 — mechanism only) |
| `adr/0035-pp-range-ownership-enforcement.md` | `PP`-range ownership enforced by exclusivity + a hand-maintained `codeRangeOwners` table, keyed on Code declarations rather than `Define` calls; the table is never generated from the constants it audits | Accepted (supplies enforcement for ADR 0005 §Registry) |
| `adr/0036-sdk-codec-form-urlencoded.md` | form-urlencoded: repetition is the only array syntax; framework dialects and last-wins refused; non-bijectivity stated rather than faked | Accepted (extends ADR 0003) |
| `adr/0037-sdk-codec-multipart.md` | multipart/form-data: the boundary is not in the body, so the codec grows extension interfaces (`BoundaryCodec`, `BoundaryProvider`) instead of widening the universal contract | Accepted (extends ADR 0003) |
| `adr/0038-id-schemes-and-the-unregistered-typeid.md` | NanoID, KSUID, TypeID — and a `Scheme` deliberately absent from the registry, which makes the registry non-exhaustive by design | Accepted (extends ADR 0024) |
| `adr/0039-extending-a-published-port-without-breaking-it.md` | Extend a published port with a sibling interface, never by widening it: a `pkg/v1` type alias publishes the *shape*, and Go interfaces are structural | Accepted (reverses the `clock` Do NOT) |
| `adr/0040-changing-a-published-shape-while-v0.md` | A published concrete shape may still change — v0 is the only reason, it must be said out loud, and the licence expires at v1 | Accepted (completes ADR 0039) |
| `adr/0041-sdk-scheduler-domain.md` | Time-driven execution domain (`scheduler`): 12th core sibling, no registry; FUNC ports; five-field POSIX cron, everything else refused by name; DST / missed deadlines / overlap decided and documented; `clock.Timed` throughout, enforced by an AST test; blocks `0.2.12.*` / `0.3.43.*` | Accepted |
| `adr/0042-sdk-token-domain.md` | Security-token domain (`token`): 13th core sibling, `Issuer`/`Verifier` ports, JWT over JWS compact + PASETO v4.public. **No registry, on purpose** — its key would be the attacker-written `alg` header. The algorithm is bound by a constructor that accepts only the one key type it can use, so the confusion bug does not compile; `none` has no representation in the `Algorithm` enum; bounds are checked before the work they fund (CVE-2025-30204); blocks `0.2.13.*`/`0.3.44.*`. v4.local deferred — it needs primitives the SDK keeps in `third-party/` | Accepted |
| `adr/0043-drain-is-a-signal-not-a-cancellation.md` | Draining is announced to the handler, never imposed: a signal on the request context, not a cancellation. Fixes a pre-existing `DRAIN_TIMEOUT` on every long-lived request | Accepted (amends ADR 0029 §shutdown) |
| `adr/0044-metrics-adopts-the-otel-data-model.md` | `metrics` adopts the OpenTelemetry metrics **data model** — typed attributes, aggregation temporality, Resource, InstrumentationScope, non-monotonic sums, observable instruments — implemented from the specification with **zero** `go.opentelemetry.io` imports. Published shapes change under the v0 licence (ADR 0040); the ports grow by siblings (ADR 0039). The Prometheus exporter becomes a named-loss connector; exemplars, exponential histograms and array attributes are deferred with reasons | Accepted (amends ADR 0027) |
| `adr/0045-sdk-session-domain.md` | Server-side session domain (`session`): 14th core sibling, no registry, `Store` frozen at five methods with `Sweeper` and `io.Closer` as siblings. Fixation prevented by **absence** — `Regenerate` is the only subject-binding call and always rotates, guarded by reflection over the port. The identifier is a 256-bit secret that redacts itself, compares with `crypto/subtle`, and is never a map key, a filename or at rest. Absolute AND sliding expiry, earlier deadline always wins, clamped in both the store and the value type; a rotation buys no time unless the principal changed. File store seals, binds each record to its filename, narrows **then stats** permissions, publishes by `rename(2)`, locks with `flock` — and returns `UnsupportedPlatform` at construction where those do not exist. Blocks `0.2.14.*`/`0.3.46.*` | Accepted (amends core purpose) |
| `adr/0046-sdk-validation-domain.md` | Value-validation domain (`validation`): 15th core sibling, no registry; `Constraint[T]` is a FUNC port; a violation is LOCATED by one path grammar (`user.addresses[2].zip`, json-tag names); collect-all by default and a real short-circuit as the opt-in; a violation is not an error and neither is the report (`Report.Err()` converts, returning a genuine nil); a message never echoes the value; two front ends — reflection-free combinators and a struct-tag plan cached per type, measured in `BENCH.md`; `email` / `url` / `uuid` / tag-borne regexp refused BY NAME; blocks `0.2.15.*` / `0.3.47.*` | Accepted |
| `adr/0047-sdk-net-websocket.md` | WebSocket (RFC 6455), server side, in the stdlib — no `gorilla/websocket`. Every MUST-fail is enumerated and enforced (unmasked client frame, reserved bits/opcodes, fragmented or oversized control frame, non-minimal length, orphan continuation, interrupted fragmentation, unsendable close codes, invalid UTF-8); UTF-8 is judged on the REASSEMBLED message because a rune may straddle a fragment; ceilings are checked against the ANNOUNCED length before any allocation. permessage-deflate refused twice — no `Sec-WebSocket-Extensions` header AND any RSV bit fails the frame. Origin checked by default. It also fixes a defect that predated it: the engine CLOSED every hijacked socket, making any upgrade impossible; blocks `0.2.11.27`–`0.2.11.33` | Accepted (extends ADR 0029) |
| `adr/0048-sdk-metrics-otlp-json.md` | OTLP/JSON metrics exporter — the native wire, written from the specification with `encoding/json` and `net/http` and **zero** `go.opentelemetry.io` imports. Two surfaces: `EncodeOTLPJSON` (snapshot to bytes, no I/O) and `NewOTLPHTTPExporter` (the POST). Unlike the Prometheus connector it loses nothing — temporality travels. Refuses an unresolved temporality and a bucket ladder OTLP cannot express; classifies failures instead of retrying, so `resilience` owns the backoff. Blocks `0.3.45.5`–`0.3.45.10` | Accepted (closes ADR 0044 §Deferred) |
| `adr/0049-cache-becomes-a-domain.md` | `cache` becomes the 16th core sibling while the ADR 0025 kernel primitive stays exactly where it is: `Store[V]` frozen at three methods with `EntryFetcher`/`Tagger`/`Loader` as ADR 0039 siblings, **no registry** (a `map[Name]Store[V]` cannot exist for an open `V`), tags with a two-way index measured at 1.16× across a 100× store, stampede protection that is stated FOUR times to stop at the process boundary, and an L1/L2 chain whose every ordering names the side it protects. Backed by a new kernel primitive, `singleflight`: a caller that abandons does not condemn the others, and a panic is re-raised in every waiter with the ORIGINATING stack. Its claimed second consumer (`config`) was checked and **does not exist** — recorded rather than dropped. Blocks `0.2.18.*` / `0.3.48.*` | Accepted (amends ADR 0025) |
| `adr/0050-sdk-lifecycle-domain.md` | Ordered start/stop domain (`lifecycle`): the SDK had every piece of a service's startup and no assembly of them, and **two lifecycle defects found in this repository are the argument** — ADR 0043 (one endless stream spent the entire drain budget, making `DRAIN_TIMEOUT` the normal outcome of every deployment) and ADR 0047 §D9 (the engine closed the socket it had just handed to the handler, so no hijack worked at all). Both are the same shape: cleanup that ran, looked reasonable in isolation, and destroyed something still needed. **No graph** — the Add order IS the dependency order, because a linear sequence already is a topological order and the caller who wrote the constructors is the only party who knows it; the cost (the SDK cannot detect a wrong order) is stated, not hidden. **No autowiring** — a wiring convention is a framework. `Start`/`Stop` are FUNC ports, so ADR 0039 is satisfied structurally. A partial start is unwound before `Start` returns, through the SAME `stopEach` an ordinary `Stop` uses, over a list appended to BEFORE the next `Start` is attempted, with contexts detached by `WithoutCancel` so a start aborted by cancellation still gets a real cleanup — and the component that FAILED is never stopped, because a `Start` that fails owns what it acquired. The budget is **per component**: expiry cancels that `Stop`'s context (an announcement, ADR 0043's instrument), stops waiting, records `StopTimeout`, and continues — it kills no goroutine and closes nothing the component owns (ADR 0047 §D9), so nothing that could finish is cut short and `Stop` returns in at most n × StopTimeout, a price stated rather than discovered. A non-positive budget CLAMPS to 30s (ADR 0031), asserted observably. Signals and `sd_notify` are opt-in `RunConfig` fields that delegate to `service/proc/{signal,sdnotify}` rather than reimplementing them. Errors aggregate with `errors.Join` so the SDK's verdict and the component's own error sit side by side. **No registry**. Nothing in the package waits on the wall clock — a named AST test fails the build on any `time` wait, in production code AND in the suite. Blocks `0.2.19.*` / `0.3.49.*`. **Two justifications offered for this domain are explicitly NOT used**: the `HttpKernel` analogy (an `HttpKernel` supervises neither the process nor its goroutines) and `google/wire` as a model (archived 2025-08-22) | Accepted |
| `adr/0051-sdk-trace-domain.md` | Distributed-tracing domain (`trace`): 18th core sibling, observability's third pillar — the OpenTelemetry trace data model AND W3C Trace Context, both implemented from their documents with zero `go.opentelemetry.io` imports. Sampling decided ONCE at the root and propagated by the `sampled` bit; an unsampled span is a no-op that still carries a context; `Extract` never fails a request (W3C §4.3); `Ratio(0)` REFUSED because 0 also spells "unconfigured". The attribute/Resource/Scope model is REUSED from `core/metrics` — they are OTel's shared `common.proto` — so `trace.Attr` IS `metrics.Attr` and exemplars finally have a trace id to hold. OTLP/JSON on `/v1/traces` per ADR 0048, plus its own fifth rule: identifiers are **hex, not base64**. The OTLP/HTTP emitter is never registered. The two HTTP middlewares are the net domain's own middleware type, and the status wrapper implements `Unwrap` and nothing else so it cannot lie about Flush/Hijack. Blocks `0.2.20.*` / `0.3.50.*` | Accepted (closes part of 0044 §Deferred) |

| `adr/0052-sdk-lock-domain.md` | Mutual-exclusion domain (`lock`): 19th core sibling, **no registry**; `Locker` frozen at two methods and `Lease` at three, neither taking a TTL — a lease lifetime is configured where the locker is wired, so the zero that would be read as "already expired" cannot be typed in a hot path. A non-positive TTL is REFUSED at construction, because its two natural readings are opposites and one of them grants every `Acquire` while excluding nobody (ADR 0031's refuse half; `FileConfig.Poll` is the clamp half, in the same package, for contrast). The three decisions expiry forces are made out loud: **ownership** is token-checked, so a lapsed holder's `Release` releases NOTHING rather than ending the new holder's section; **renewal** is `Extend` — refused on a lapsed lease even when nobody has taken the lock, because expiry is the deadline, not "until someone else takes it" — plus a `Keepalive` that CANCELS a derived context, since the caller who needs the news is already inside the section; **fencing** ships, a strictly-increasing per-name `uint64`, persisted in the lock file for the file backend and refused rather than reset when the ledger is corrupt. And the guarantee that is NOT made is stated in four places: the SDK can only ISSUE a fence, so where the protected resource cannot compare it, mutual exclusion is not guaranteed against a GC pause or a `SIGSTOP`. Two backends that differ in the one property a caller must know — the memory lease expires and implements `Deadliner`, the file lease never expires and deliberately does not — because a dead process is noticed by the kernel and a dead goroutine is noticed by nobody. `flock(2)` was MEASURED before use: two descriptions in one process do exclude, but the same description re-locked is a no-op, and eight goroutines sharing one descriptor were all inside one section, every run — so the file locker takes an in-process gate first. Windows refused rather than approximated (`LockFileEx` is byte-range and mandatory). Blocks `0.2.21.*` / `0.3.51.*` | Accepted |
| `adr/0053-sdk-events-domain.md` | In-process event bus domain (`events`), and **the line between it and the `queue` domain that comes next** — drawn before either could drift, because a library that "supports both" reliably guarantees neither. `events` is in-process, synchronous, same-goroutine, same transaction: no durability, no retry, no dead-letter path, and if the process dies mid-dispatch the event never happened. A caller who wants "asynchronous but reliable" is sent to `queue`, not served the asynchrony without the reliability — so there is deliberately no async mode, not even as an option (§Why not). The routing key is the event's concrete Go TYPE, never a name: the compiler mints it, two packages cannot both spell `"user.created"`, and an interface type is REFUSED at registration because `reflect.TypeOf` never yields one and such a listener would be listed and never called (ADR 0031). Typing is erased-plus-generic because Go methods take no type parameters: `Listener func(ctx, any) error` with a package-level `On[E]` that derives the key and asserts — a `Bus[E]` would carry exactly one event type, which is a channel. **The assertion is measured, not asserted: 2.4 ns per listener call against an identical erased control, 3.5 % of a dispatch**, and the 16 B the caller pays to box the event is published beside it rather than hidden behind the other rows' `0 allocs/op`. Listeners run in ascending priority with ties in registration order, sorted at registration so the read path never allocates. Stopping propagation is an AUTHORITY declared at the wiring site (`MayHalt`) and exercised by returning the `Halt` sentinel — an unauthorised halt does NOT stop the dispatch and is reported, because honouring it makes the permission decorative and swallowing it leaves a listener convinced it vetoed an event every sibling then saw. A halt is not a failure: `Publish` returns nil. A listener error never short-circuits and aggregates with `errors.Join`, verdict beside cause. A listener PANIC is recovered on the publisher's goroutine, carries the originating stack in a field, never hijacks the code, is never read as a halt, and does not stop the siblings. **`kernel/topic` was read and deliberately NOT used** — its per-subscriber buffer makes `Publish` return before the listeners have run and puts `Halt` on the wrong side of a channel; the domain takes `kernel/snapshot`, the primitive `topic` itself is built on. No registry, no `Config`, no new kernel primitive. Blocks `0.2.22.*` / `0.3.52.*` | Accepted |
| `adr/0054-sdk-queue-domain.md` | Asynchronous durable message queue domain (`queue`), the right-hand column of ADR 0053's frontier | Accepted |
| `adr/0055-sdk-sql-domain.md` | Relational-database domain (`sql`): transaction ownership, savepoints, and a migration lock that dies with its holder | Accepted |
| `adr/0056-sdk-vfs-domain.md` | Filesystem domain (`vfs`), and exactly how far "atomic" reaches | Accepted |
| `adr/0057-sdk-authz-domain.md` | Authorization domain (`authz`): abstention is a verdict, and there is no policy language | Accepted |
| `adr/0058-sdk-view-domain.md` | Server-side rendering domain (`view`): a contract ON TOP of `html/template`, one trust type, and the XSS the SDK cannot see | Accepted |
| `adr/0059-no-mapper-domain.md` | There is no `mapper` domain: a projection is a type, not a tag | Accepted |
| `adr/0060-sdk-health-domain.md` | Health probes where the wrong wiring does not compile: liveness takes a context-free `SelfCheck`, readiness a context-carrying `Check` | Accepted |
| `adr/0061-sdk-config-schema.md` | Configuration schema: required keys, typed defaults, and a closed vocabulary | Accepted |
| `adr/0062-logger-trace-correlation.md` | A log line names the span it came from, and the bridge lives at the top layer | Accepted |
| `adr/0063-sdk-i18n-domain.md` | Message-translation domain (`i18n`): a named CLDR subset, everything outside it refused BY NAME, and the guarantees that are NOT made | Accepted |
| `adr/0064-sdk-mail-domain.md` | Electronic-mail domain (`mail`): composition is a mechanism, SMTP is a stdlib protocol, and a provider is neither | Accepted |
| `adr/0065-sdk-cli-domain.md` | Command-line domain (`cli`): what `flag` does not have, and the four things this adds instead of a framework | Accepted |
| `adr/0066-third-party-compressors.md` | Third-party compressors: zstd and s2, one library, one package, and a ceiling that cannot be omitted | Accepted |
| `adr/0067-metrics-instrument-description.md` | An instrument gets a description, and it belongs to the NAME | Accepted |
| `adr/0068-layer-firewall-is-a-checked-graph.md` | The layer firewall is a checked graph, because Gazelle's visibility admits the whole repository (amends ADR 0004) | Accepted |
| `adr/0069-websocket-origin-behind-a-proxy.md` | Behind a proxy that announces the scheme, the default origin rule refuses instead of guessing (amends ADR 0047) | Accepted |
| `adr/0070-logger-reserves-the-correlation-keys.md` | The two keys the logger writes itself are reserved, and a caller's are renamed rather than dropped (amends ADR 0062) | Accepted |
| `adr/0071-a-registry-refuses-what-it-cannot-store.md` | A registry refuses a plug-in it cannot store, at the call that publishes it | Accepted |
| `adr/0072-health-bounds-every-wait-it-owns.md` | Health bounds every wait it owns, including the two that belonged to somebody else (amends ADR 0060) | Accepted |
| `adr/0073-session-waits-are-abandonable.md` | The session store's two waits can be abandoned, because a blocking flock cannot (amends ADR 0045) | Accepted |
| `adr/0074-what-a-public-alias-may-point-at.md` | A public alias points at the layer that OWNS the type, and a single engine's configuration is owned by that engine | Accepted |
| `adr/0075-reading-the-cgroup-cap-that-already-bounds-us.md` | The SDK reads the cgroup cap that already bounds this process, not only the ones it writes for others | Accepted |
| `adr/0076-what-a-branch-changed-is-a-value-that-can-say-it-does-not-know.md` | What a branch changed is a value that can say it does not know, and running git against a repository you do not control is hardened | Accepted |
| `adr/0077-a-self-update-is-an-order-of-operations-and-a-product-name-is-not-part-of-it.md` | A self-update is an order of operations, and a product name is not part of it | Accepted |
| `adr/0078-entitlement-is-quarantined-because-ssh-brings-x-sys.md` | Entitlement ships under third-party/ because proving key possession brings x/sys with it | Accepted |
| `adr/0079-the-entitlement-split-x-sys-was-never-in-the-mechanism.md` | The entitlement split: x/sys was never in the mechanism, only in the identity | Accepted |
| `adr/0080-the-gate-decides-and-performs-nothing.md` | The gate decides, and performs nothing | Accepted |
| `adr/0081-the-windows-file-lock-is-a-different-primitive.md` | The Windows file lock: a different primitive, measured rather than recited, and the one rule it cannot run | Accepted |
| `adr/0082-the-lock-path-is-a-file-never-a-link-to-one.md` | The lock path is a file, never a link to one: `O_NOFOLLOW` and `FILE_FLAG_OPEN_REPARSE_POINT`, and the `LOCK_PATH_REDIRECTED` refusal that replaces a silent redirection | Accepted |
| `adr/0083-a-path-is-a-chain-and-a-held-lock-can-lose-its-file.md` | A path is a chain: the `pathchain` kernel primitive, the parent-component rule it feeds, and `LOCK_FILE_REPLACED` — detection where prevention is impossible | Accepted |
| `adr/0084-the-windows-lock-directory-has-an-answer-and-it-is-not-a-mode.md` | The Windows lock directory's verdict becomes a DACL question rather than a mode question — two `advapi32` exports, not the 250 lines three ADRs deferred it on | Accepted |
| `adr/0085-both-halves-of-a-release-read-the-same-range.md` | The two halves of a release stop reading different things — the `Release-bump` trailer is read over the range `compute-bumps.sh` diffs, largest wins, scoped per commit, walked `--first-parent` so a contributor's commit inside a merged branch cannot size a release | Accepted |
| `adr/0086-creating-an-entry-is-not-replacing-one-and-windows-says-so-in-two-bits.md` | The two lock rules stop sharing a mask, `BUILTIN\Users` becomes the third "anybody", all eight discretionary ACE shapes are decoded, and the SACL and the planted-lock-file deferrals are made permanent with their evidence | Accepted |
| `adr/0087-the-root-a-caller-named-is-a-spelling-it-did-not-choose.md` | Six of ADR 0076's seven deferred entries closed: the root a caller named answers alongside git's canonical one, a shallow probe that will not answer degrades, `origin/HEAD` is verified in the invocation that reads it, the diff-prefix config keys are pinned, `ShowFile` separates absent from failed, and the `GitDir` memo is re-validated | Accepted (§1 amended by 0095) |
| `adr/0088-a-suite-nothing-runs-is-not-a-test-suite.md` | A suite nothing runs is not a test suite. ADR 0085 shipped 32 BATS tests for the two scripts that decide which tag every merge gets and recorded, under Deferred, that no CI lane and no `make` target executed them; re-verified before this change, `grep -rn bats Makefile .github/` returned nothing across all ten workflows. That is worse than having no tests, because none of the three defects the suite covers fails loudly — each produces a PLAUSIBLE version number. `separator=` is not an output separator: git parses the `%(trailers:…)` option list on commas, so `separator=,` leaves the separator EMPTY and concatenates repeated trailers — `Release-bump: mi` + `Release-bump: nor` reads back as the single value `minor` and cuts a minor release, measured on git 2.47.3 and invisible on review; the same construct also turns a doubled `Release-bump: minor` into `minorminor`, which ranks as unrecognised and silently degrades to a patch. A walk without `--first-parent` descends into the commits a merge brought IN, handing ADR 0007 §2's authorisation to anyone able to open a PR — five commits here carry the trailer, touch `pkg/`, and are not on main's first-parent line. A true merge reports NO files under a plain `--name-only`, so a maintainer's trailer on one was scoped against an empty list and counted for nothing — 14 of 33 trailer-bearing commits. Each was confirmed to make its own test go RED when reintroduced and GREEN when reverted: the suite had never been red, because it was written alongside the fix. A fourth defect was found in the harness itself and not predicted by it — the disposable fixtures inherit the host's `core.hooksPath`, so a developer's own refusing `pre-commit` failed the suite in `setup` on correct code, and neither a repo-local `core.hooksPath` nor `GIT_CONFIG_GLOBAL=/dev/null` outranks a hooks path injected at `-c` precedence; only `--no-verify` does. `release-scripts-test.sh` runs the suites and bats-core is a prerequisite, not a vendored artefact: the runner resolves it from PATH and exits 127 naming three install routes rather than cloning a third-party repository on every run, because `make lint` already refuses network egress. `ci-gates-check.sh` holds the manifest — a listed gate must have a target, be `.PHONY`, and be invoked by `bazel-ci.yml` — and lists its own target, so the commit that drops it from CI fails it. Confirmed red in all three failure modes before being wired green. The lane is a sibling job, `shell-gates`: neither gate needs Bazel or Go, and these are the first `make` invocations in `bazel-ci.yml`'s history, which is exactly why the Makefile↔CI link needed asserting — it did not exist | Accepted |
| `adr/0089-a-file-that-cannot-cut-a-release-cannot-size-one.md` | A file that cannot cut a release cannot size one. ADR 0007 §2 required a behavioural change in `internal/**` observable through `pkg` to be a MINOR, while its very next row gated the only way to ask for one on the commit touching `pkg/` — so the table demanded something it gave no way to express. Measured against the unmodified script: a commit touching only `internal/service/svc.go` with a well-formed `Release-bump: minor` cut `pkg/v0.1.1`, while `compute-bumps.sh` cut the release for it anyway through rdeps and tagged the whole graph. The scope it did enforce protected nobody: the repository merges with `squash_merge_commit_message: COMMIT_MESSAGES`, so GitHub composes the squash message from the contributor's own commit bodies rather than the maintainer's, and a commit touching only `pkg/v1/foo/CLAUDE.md` with `Release-bump: minor` cut `v0.2.0` — a documentation file sizing a release that the same file cannot cut, because `compute-bumps.sh:70` excludes `*/CLAUDE.md` and `*/BUILD.bazel` and `cut-tags.sh` never learned that exclusion. Both halves now share ONE notion of a file that counts: under `pkg/` or `internal/`, minus those two names. The net is a hardening, not a relaxation — widening makes the required minor expressible, excluding closes the cheapest bypass, and after both, sizing a release requires touching code that actually cuts one. The two are correct only together, and the history proves it: `3f681412` touched `pkg/v1/resilience/CLAUDE.md` AND three `.go` files under `internal/service/resilience/`, so the exclusion alone would have dropped its only `pkg/` path and SILENCED the misplaced-trailer refusal that the widening keeps — measured: that commit's `Release-bump: minor` sits at line 99 of a 126-line message, git parses NOTHING from it, so what the pair rescues is the alarm and not the value, and the exclusion alone would have re-created issue #217 inside the fix for #218. Per-commit `bazel query rdeps` is deliberately not repeated in `cut-tags.sh`: it runs only once `compute-bumps.sh` has already emitted `pkg`, so the release is happening and only its size is open. Four BATS tests, red against the pre-fix script and green after, with a `pkg/` code control that passes both ways so a rule answering no to everything cannot pass. Deliberately NOT a fix for issue #220: the shared notion still counts `_test.go`, and measured against `8addc948` (the commit that cut `pkg/v0.4.1`, two files, both tests, both internal) the old `^pkg/` rule says no while `counts_for_release` says YES — this widens what may SIZE that release rather than stopping it, and `compute-bumps.sh`, which alone decides whether to cut, is byte-identical to main here | Accepted |
| `adr/0090-a-port-named-in-public-must-be-implementable-in-public.md` | A port named in public must be implementable in public: `pkg/v1/clock` publishes the kernel time port as aliases, because type identity — not an identical method set — is what `clock.Timed` required, and nine configurations named it without anyone being able to satisfy it | Accepted |
| `adr/0091-a-single-trust-anchor-is-a-key-with-no-way-out.md` | A single trust anchor is a key with no way out: the entitlement anchor becomes an ordered, bounded, build-time LIST and a roster is authentic against any entry, because a rotation had no path in band — new-key rosters were refused as forgeries before the update floor was read, and the release carrying the new key was refused by the key it replaced | Accepted |
| `adr/0092-a-possession-proof-that-cannot-name-the-key-it-is-about.md` | A possession proof that cannot name the key it is about: the authorised fingerprint never crossed the `Identity` port, so no implementation could bind its proof to it. Shipped as the `BoundProver` SIBLING rather than by widening `ProvePossession`, because a published port is frozen at any version — and because widening would break every implementer without guaranteeing the binding it appears to buy | Accepted |
| `adr/0093-a-sweep-takes-the-zombie-never-the-status.md` | A sweep takes the zombie, never the status: `internal/service/proc/childwait` claims each child the SDK spawns and hands a status the reaper's `wait4(-1)` collected to the `Process` that owns it, so a clean exit is no longer reported as `WAIT_FAILED` when a pid-1 reaper wins the race | Accepted |
| `adr/0094-a-test-compiles-where-its-package-does.md` | A test compiles where its package does, and macOS runs every one: the build bar `go vet`s (test files included) across the 10 `cross-build` targets; test helpers sit where their callers compile; Unix sockets bind under `socketDir(t)`; the macOS lane runs the whole suite, Windows as an inventory | Accepted (Windows inventory superseded by 0095) |
| `adr/0095-windows-runs-every-test-and-gates.md` | Windows runs every test and gates: the whole-suite step loses `continue-on-error`; its 19 failing packages were each classified — nine production bugs fixed (multipart filename, three in `net/server`, the `vcs/git` root spelling, the queue's mode-bit directory rule now a DACL rule through `lock.GrantsAnyone` that refuses a list it cannot read, a Windows self-update refused before download, journald's dialer, a closable `NewMulti` Logger), the rest Unix premises replaced by what Windows promises | Accepted |
| `adr/0096-a-secret-is-a-value-no-rendering-writes-down.md` | A secret is a value no rendering writes down, a name with versions, and a key that rotates without breaking what it sealed: the `secret` domain — a redacting `Value`, a frozen versioned `Store` (memory, environment, sealed file), a `Keyring` whose boxes carry their version, a `Rotator` | Accepted |

This table is one of THREE indexes of the same ADRs — the others are
`docs/adr/CLAUDE.md`'s Contents table and the root `CLAUDE.md` Reference list —
and all three are checked against `docs/adr/` by
`scripts/pre-commit/check-domain-docs.sh`. This one was unchecked until 2026-09-12
and had lost 23 entries, every ADR from 0061 on among them.

## ADR conventions

- Sequential numbering (`0001`, `0002`, …). Never reuse a number.
- Filename slug = short kebab-case summary of the decision.
- Header fields: `Status`, `Date`, `Deciders`, optional `Supersedes`, `Superseded by`, `Amends`, `Related`.
- Standard sections: Context, Decision, Consequences / Semantics, Breaking changes, Why not …, Deferred, References.
- Immutable after merge. Supersede via a new ADR that references the old one (e.g. ADR 0005 supersedes ADR 0002 §Registry; ADR 0006 amends ADR 0005 §Registry).

## Registry coupling

The dotted-quad allocation table in `adr/0005-…` + the extension in `adr/0006-…` is the **documentary source of truth**; the AST audit test in `internal/kernel/errs/registry_external_test.go` embeds the same table as the **executable source of truth**. Keep the two in sync manually on every change — the audit catches drift on uniqueness and `reason = screamingSnake(varName)` OR `screamingSnake(CodeConst − "Code")` (ADR 0006/0020) over every package in `//:audit_sources`.

`docs/error-codes.yaml` is the **generated human-readable mirror** of every `errs.Code` constant in the tree (one entry per code: dotted-quad, const name, hex, package). Regenerate with `make error-codes` (`scripts/gen-error-codes.sh`); the `scripts/pre-commit/check-error-codes-drift.sh` guard fails the commit when it is stale. It is a convenience index, not authoritative — the AST audit remains the executable gate.

## Do NOT

- Put feature documentation here. Packages document themselves via `README.md` next to their code.
- Delete an accepted ADR. Mark it superseded by adding a new ADR and updating both `Status` lines.
- Re-number existing ADRs, even after a supersede chain.
- Edit an ADR's Decision section after merge — write a new ADR.

## Subtree

- `adr/` — Architecture Decision Records (see the table above; it is the count, so no separate number goes stale here)
