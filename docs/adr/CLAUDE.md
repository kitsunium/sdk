<!-- updated: 2026-05-18T14:30:00Z -->
# docs/adr/

## Purpose

Architecture Decision Records. Each ADR captures one cross-cutting decision (layout, error model, build system, code allocation) with rationale, alternatives considered, and impact. ADRs are authoritative — when an ADR and a `CLAUDE.md` disagree, the ADR wins and the CLAUDE.md gets corrected. Treat ADRs as append-only: never edit a merged ADR's decision; if a follow-up changes direction, write a new ADR that supersedes the previous one and update its `Status` field.

## Contents

| File | Decision | Status |
|---|---|---|
| `0001-sdk-go-multimodule-layout.md` | Per-sublayer Go modules (`internal/kernel`, `internal/core`, `internal/service`, `pkg/v1`) with `replace` directives + `go.work` umbrella | Accepted |
| `0002-sdk-errors-package.md` | Typed `errs.Define` / `errs.Wrap` infrastructure with `Public` / `Private` split | Accepted (Registry section superseded by 0005) |
| `0003-sdk-codec-package.md` | Universal `Codec` / `StreamingCodec` / `Appender` triad + 10-format registry | Accepted |
| `0004-sdk-bazel-build-system.md` | Bazel 9 with `rules_go` + `gazelle` + `package_group`/`visibility` for layer firewall | Accepted |
| `0005-sdk-error-codes-dotted-quad.md` | `Code uint32` packed as `MM.LL.PP.SS` with wrap trail + canonical `Error()` regex | Accepted |
| `0006-sdk-error-code-registry-extension.md` | Logger v2 + `ring` code-range allocations on top of 0005 | Accepted |
| `0007-sdk-release-and-versioning.md` | Impact-driven patch tags `pkg/<major>/vX.Y.Z` + docs versioning from tag snapshots | Accepted |
| `0008-readme-from-code-generation.md` | `README.md` generated from Go doc comments (gomarkdoc) | Accepted |
| `0009-pkg-public-module-resolvability.md` | Published `pkg/<major>` must be `go get`-resolvable + accessible; release tags the whole module chain | Accepted; mechanism implemented |
| `0010-kernel-recycler-primitive.md` | Generic `recycler.Pool[T]` + `CappedPool[T]` object pools; `buffer`/`scratch` become consumers | Accepted |
| `0011-kernel-snapshot-primitive.md` | Generic `snapshot.Value[T]` copy-on-write container; codec registry consolidates onto it | Accepted |
| `0012-logger-writer-registry.md` | Named, config-driven `Sink` factory registry (`writer`); console/file in-tree + AWS s3/cloudwatch under third-party/ (root module) | Accepted (amends 0005/0006 §Registry) |
| `0013-sdk-crypto-domain.md` | Crypto domain — `AEAD` `Seal`/`Open` with hidden nonce; stdlib AES-256-GCM default, x/crypto schemes under third-party/ | Accepted (amends core purpose + 0005 §Registry) |
| `0014-sdk-transform-crypto-ports-config-topology.md` | The verb wave — `core/transform` (5th sibling), crypto MAC/Agreement/StreamSealer ports + KeyEnvelope/KeyTree, `logger.FromConfig` + `ConfigDecoder`, single-MR sequencing | Accepted (amends core purpose + 0012/0013 + 0005 §Registry) |
| `0015-sdk-logger-writer-taxonomy-and-rotation.md` | Writer taxonomy + `depTier` first-class property; default console+file (`DefaultMulti`); rotation off-by-default → compress + 24h archive + 7-day retention; dep-light DB/transport gate (WI-9 `dbsink` shell shipped; WI-10/WI-11 vendored writers gated) | Accepted (amends `core/writer` purpose + 0005 §Registry) |
| `0016-sdk-process-supervision-domain.md` | OS process-supervision domain (`proc`): 6th core sibling; `process`/`signal`/`reaper`/`rlimit`/`cgroup`/`sdnotify` facades; central error block `0.2.6.*`; build-tag platform selection | Accepted |
| `0017-pkg-bare-module-path.md` | Public module is the bare `…/pkg` (Go forbids the `/v1` suffix); code stays under `v1/`, imports unchanged; tag `pkg/vX.Y.Z`, first release `v0.1.0` alpha | Accepted (amends 0009 path / 0007 tag shape / 0001 module path) |
| `0018-sdk-cross-platform-portability.md` | Cross-platform portability strategy — build bar + runtime bar; uniform `UnsupportedPlatform` contract; `_linux`/`_unix`/`_bsd`/`_windows`/`_other` split convention; `bazel-ci.yml` cross-build + `e2e-vm.yml` gates; OpenBSD `RLIMIT_AS` precedent; native-backend roadmap (FreeBSD `rctl`, Windows Job Objects, BSD `procctl`) | Accepted |
| `0019-pkg-errs-public-construction.md` | Public error construction API (`pkg/v1/errs.New`/`Wrap`/`Field` via non-panicking `kernel/errs.NewRuntime`) + third-party Major reservation `0x40–0x7F` (`MinAppMajor`/`MaxMajor`) | Accepted (amends 0002 / 0005 §Registry) |
| `0020-errs-audit-dual-reason-derivation.md` | errs AST audit accepts `screamingSnake(varName)` OR `screamingSnake(CodeConst − "Code")`; closes the `//:audit_sources` gap excluding ~10 namespaced emitters | Accepted (amends 0005 §Semantics / formalises 0006) |
| `0021-sdk-codec-bson.md` | BSON codec (M5) in `internal/service/codec/bson` over `mongo-driver/bson` (library-backed precedent); non-streaming + Appender; block `0.3.36.*` | Accepted (extends 0003 §M5) |
| `0022-sdk-codec-hcl.md` | HCL codec (M5) quarantined in `third-party/codec/hcl` (`hcl/v2` would introduce the banned `x/sys` into service — see 0034, which corrects the original "downgrade" wording); opt-in, not in `pkg/v1/codec`; block `0.3.37.*` | Accepted (extends 0003 §M5; follows 0012, contrasts 0021; mechanism corrected by 0034) |
| `0023-sdk-schema-codecs.md` | Schema codecs (M6) opt-in under `third-party/codec/` (schema-bound, break universal round-trip); Protobuf concrete `0.3.38.*`; Avro/Cap'n Proto deferred | Accepted (extends 0003 §M6) |
| `0024-sdk-id-domain.md` | Identifier-generation domain (`id`): 7th core sibling, `Generator`/`Scheme` registry (UUIDv4/v7, ULID, snowflake), canonical-string output, stdlib-only/cross-OS; opens Phase-B; blocks `0.2.7.*`/`0.3.39.*`. Since extended in place with NanoID, KSUID and TypeID (same block; TypeID constructor-only per ADR 0031) | Accepted (amends core purpose; opens Phase B) |
| `0025-sdk-cache-kernel.md` | Generic LRU+TTL `Cache[K,V]` as a **kernel** primitive (domain-neutral, stdlib-only, reuses `clock`); `Fetch` not `Get`; no error codes; `pkg/v1/cache` aliases | Accepted (kernel primitive, Phase B) |
| `0026-sdk-resilience-domain.md` | Reliability domain (`resilience`): 8th core sibling, **no registry** (concrete `Runner` policies: retry/breaker/ratelimit/bulkhead/timeout, + fallback/hedging by later extension); composable; block `0.2.8.*` | Accepted (Phase B; proc-style no-registry sibling) |
| `0027-sdk-metrics-domain.md` | Observability domain (`metrics`): 9th core sibling, instruments + `Meter` + exporter registry (writer-registry model); in-mem meter + the `text` and `prometheus` exporters; blocks `0.2.9.*` + `0.3.45.*`; labels + the Prometheus text exposition format shipped in place; **OTLP/JSON shipped in ADR 0048**, OTLP/protobuf still deferred. **Re-shaped by ADR 0044**: the domain now speaks the OpenTelemetry metrics data model, implemented from the specification with zero OTel imports. | Accepted (Phase B) |
| `0028-sdk-config-domain.md` | Configuration domain (`config`): 10th core sibling, `Source`/`Validator`/`Watcher` ports; env+file loader + cross-OS **poll** watcher; closes the Phase-B wave; block `0.2.10.*` | Accepted (Phase B; closes the wave) |
| `0029-sdk-net-domain.md` | Network domain (`net`) as the 11th core sibling: one contract package, service engines for TLS identity / guarded client / listener engine, `pkg/v1` facades; goroutine-per-connection on the netpoller, `net/http` adapted rather than reimplemented; block `0.2.11.*` | Accepted |
| `0030-stdout-is-a-protocol-channel.md` | No SDK default writes to `os.Stdout`: the registered `text` metrics exporter targets stderr, `ConsoleStderr` becomes the `ConsoleStream` zero value (and the absent/empty `target` maps to it); stdout stays reachable by naming it | Accepted (amends 0027 §Decision 2 / 0015 §D3) |
| `0031-policy-zero-values-are-never-inert.md` | A policy constructor never returns an inert policy: **clamp** where a working default needs no explanation (`BreakerConfig.OpenDuration` → 30s), **refuse** where any SDK-chosen value would be arbitrary (`RateLimiterConfig.Rate`, `NewTimeout`) via the new `PolicyMisconfigured` sentinel `0.2.8.6` | Accepted (amends 0026 §Decision 2) |
| `0032-logger-slog-bridge.md` | `log/slog` is permitted in exactly one package (`pkg/v1/logger/slogbridge`), as an adapter to a foreign ecosystem at the public edge; the domain keeps its "never log/slog" rule. Removes the two-parallel-loggers pattern and the three silent defects it produces | Accepted (qualifies the rule in `core/logger/level`) |
| `0033-consumer-rule-enforcement.md` | SDK rules are enforced on consumers at BUILD time by `tools/sdkguard`, a stdlib-only CLI. Runtime detection was measured impossible (`log/slog` is not a module; a local `slog.Logger` never touches `slog.Default`), and a vettool needs `x/tools` — which `tools/*` cannot take without breaking its Bazel build. Rules target constructs not imports, and split invariant/convention so adoption is incremental. Also warns (never fails, by default) when the consumer's go.mod pins an SDK older than the latest release — ADR 0007 cuts patches for internal fixes invisible in a public-API changelog | Accepted |
| `0034-hcl-quarantine-rationale-corrected.md` | The HCL quarantine stands; its stated mechanism does not. MVS selects the **maximum** required version, so a dependency asking for a lower one cannot downgrade a higher requirement. Measured: adding `hcl/v2` to a copy of `internal/service` **introduces** `x/sys` at `v0.20.0` (via `x/tools`) into a module that bans it. A ban violation, not a version regression | Accepted (amends 0022 §Context.1 / §Why not — mechanism only) |
| `0035-pp-range-ownership-enforcement.md` | `PP`-range ownership gets the enforcement 0005 §Registry assumed: exclusivity (one package per `MM.LL.PP`) + conformance to a hand-maintained `codeRangeOwners` table, keyed on Code **declarations** rather than `Define` calls. The table is never generated from the constants — a derived table would record a squatter as the owner. Four fixtures prove the checks fail on violations | Accepted (supplies enforcement for 0005 §Registry) |
| `0036-sdk-codec-form-urlencoded.md` | form-urlencoded: repetition is the only array syntax (`a=1&a=2` → `["1","2"]`, native `url.Values`); the `a[]=`/`a[0]=`/comma dialects and last-wins are refused; non-bijectivity stated — bytes are canonicalised and the **second** round-trip is the fixpoint; a repeated key into `map[string]string` returns typed `MULTI_VALUE` | Accepted (extends 0003) |
| `0037-sdk-codec-multipart.md` | multipart/form-data: the delimiter lives in the `Content-Type`, which the `Codec` contract cannot carry. Three sub-problems, three answers — encode re-emits and recovers via `ContentType(data)`, decode sniffs then confirms against the closing delimiter, streaming gets the `BoundaryCodec`/`BoundaryProvider` extension interfaces reached by type assertion. What it cannot cover (a `--`-prefixed preamble, byte-streaming inside a part) is stated | Accepted (extends 0003; introduces codec extension interfaces) |
| `0038-id-schemes-and-the-unregistered-typeid.md` | NanoID, KSUID and TypeID. TypeID is deliberately **not** registered — a prefix names the caller's entity, so a singleton would invent their vocabulary or mint a prefixless id (the ADR 0031 degradation). The `Scheme` registry is therefore no longer exhaustive. NanoID rejects rather than modulos; a KSUID outside the 32-bit window is refused, not wrapped | Accepted (extends 0024 §Registry) |
| `0039-extending-a-published-port-without-breaking-it.md` | A published port is extended by a **sibling interface**, never by widening. `Clock` is reachable through a `pkg/v1/cache.Config` type alias and Go interfaces are structural, so adding a method breaks every downstream two-method double at compile time with no deprecation window. `Clock` stays frozen, `Waiter` is new, `Timed` is the union, `System` widens as a *value*. Guarded by an executable test, not a comment — the comment that was there is what this reverses | Accepted (reverses the `clock` Do NOT; generalises to every aliased port) |
| `0040-changing-a-published-shape-while-v0.md` | The concrete-type half of 0039. `SnapshotValue` changed shape and `pkg/v1/metrics.Snapshot` aliases it, so the change reached consumers; there is no sibling trick for a data shape. Permitted **only because the module is v0** (`pkg/v0.1.26`), stated explicitly in the commit, and gone at v1. Also requires a pre-v1 audit of every concrete type an alias publishes | Accepted (completes 0039) |
| `0041-sdk-scheduler-domain.md` | Time-driven execution domain (`scheduler`): 12th core sibling, **no registry**; `Job`/`Schedule` are FUNC ports so a published port cannot grow a method at all; five-field POSIX cron with the day-field OR rule and every other dialect refused BY NAME; DST decided (spring-forward skips, fall-back fires once); missed deadlines skipped **and counted**, never replayed; overlap skipped by default behind the `AllowOverlap` assertion; the engine waits through `clock.Timed` and an AST test fails the build on any wall-clock wait; blocks `0.2.12.*` / `0.3.43.*` | Accepted |
| `0042-sdk-token-domain.md` | Security-token domain (`token`) as the 13th core sibling: `Issuer`/`Verifier` ports, JWT over JWS Compact + PASETO v4.public, **no registry** because its key would be the attacker-written `alg`. Algorithm confusion is made unwritable by the constructor set (`crypto.Key` is not an `*ecdsa.PublicKey`) and then still compared at verification; `alg:none` is unrepresentable in the `Algorithm` enum; every bound is checked before the work it funds; `exp` is required by default on both sides. Blocks `0.2.13.*`/`0.3.44.*`. States what it does NOT guarantee: no confidentiality, no replay detection, no revocation, no key retrieval. `v4.local` refused by name — XChaCha20+BLAKE2b live in `third-party/` and stay there | Accepted (amends core purpose) |
| `0043-drain-is-a-signal-not-a-cancellation.md` | Adding SSE exposed a defect already there: an open stream held `Shutdown` for the caller's whole budget and returned `DRAIN_TIMEOUT`, because `http.Server.Shutdown` waits for in-flight requests and one that never ends never becomes idle — the normal outcome of every deploy, uncovered by any test. The adapter now publishes a drain **signal** on the request context, deliberately **not** a cancellation: cancelling would tell handlers to abandon the very response the drain exists to let them finish. Mutation-checked | Accepted (amends 0029 §shutdown) |
| `0044-metrics-adopts-the-otel-data-model.md` | The `metrics` domain adopts the OpenTelemetry metrics DATA MODEL and imports none of its code — OTel is a specification, and the SDK implements it from the document like the Prometheus exposition format or RFC 7517. Typed attributes (the KIND is part of the series identity), aggregation temporality made REAL (a delta `Collect` consumes the window it reports and advances the start), `Resource` + `InstrumentationScope` carried once per payload, `UpDownCounter` as a sibling port with monotonicity as a FIELD of the sum, and observable instruments read at collection. Exemplars, exponential histograms and array attributes deferred by name. The published shapes change under the ADR 0040 v0 licence, stated out loud; the ports grow by ADR 0039 siblings. The Prometheus exporter survives, requalified as a deliberately LOSSY connector whose losses are enumerated — temporality REFUSED, attribute type flattened, Resource/Scope dropped, OTel-conventional dotted keys refused | Accepted (amends 0027; applies 0039 + 0040) |
| `0045-sdk-session-domain.md` | Server-side sessions as the 14th core sibling — the stateful counterpart to 0042, and the difference is revocation: a token ends when it expires, a session ends when `Destroy` says so. **No registry** (the choice is a deployment decision, not data); `Store` frozen at five methods with `Sweeper` and `io.Closer` as type-asserted siblings (0039). Fixation is prevented by ABSENCE — `Regenerate` is the only subject-binding call and always mints a new identifier, so the mistake is not writable; two reflection guards fail on anyone who re-adds a `SetSubject`, and the forged session is constructible and deliberately not persistable so `FIXATION_REFUSED` stays reachable. The identifier is 256 bits that redact themselves, compare with `crypto/subtle`, and never become a map key, a filename or a byte on disk (stores index by SHA-256). Expiry is absolute AND sliding with the earlier deadline always winning, clamped in the store *and* in the value type so the rule holds for third-party stores; `IdleTimeout >= AbsoluteTimeout` refused as an inert policy (0031); rotation keeps the ceiling unless the principal changed. File store: AEAD at rest, AAD binding each record to its own filename, permissions narrowed **then stat'ed** (a chmod that reports success is not proof), `rename(2)` publication verified byte-for-byte against a simulated failure, one store-wide `flock`; Windows and friends get `UnsupportedPlatform` at construction with the closure recorded rather than a store that pretends (0018). Frontier stated in three places: the SDK ships the store and the sealing; the firewall, the voters, the login conventions and `http.SetCookie` belong to the framework. Blocks `0.2.14.*`/`0.3.46.*` | Accepted (amends core purpose) |
| `0046-sdk-validation-domain.md` | Value-validation domain (`validation`) as the 14th core sibling: `Constraint[T]` is a FUNC port, so ADR 0039 is satisfied structurally. Three decisions carry it — a violation says **where** (one path grammar, member named by its `json` tag because `config.Load` round-trips every format through JSON); **every** violation is collected by default, and stopping is a combinator compiled into the plan rather than a truncated report; and a violation is **not** an error, nor is the report, because a value type implementing `error` makes `if err != nil` true for a passing validation. A message names the rule and the bound and never the value — tested. Two front ends produce the identical violation: reflection-free combinators, and a struct-tag plan cached per `(type, mode)` (48× over recompiling, 26 % over the reflection-free path). The constraint set is closed and small; `email`, `url`, `uuid`, a tag-borne regexp and map descent are refused BY NAME with the fix in the message. Feeds `config.Validator` without replacing it. Blocks `0.2.15.*` / `0.3.47.*` | Accepted (amends core purpose) |
| `0047-sdk-net-websocket.md` | WebSocket (RFC 6455), server side, implemented in the stdlib: the format in `internal/core/net`, the connection in `internal/service/net/websocket`, the façade in `pkg/v1/server/websocket`. The RFC is mostly MUST-fail, because a length-prefixed frame stream that keeps going after a disagreement is two endpoints reading different messages from the same bytes — so each refusal is enumerated with its section and its close code, and provoked in tests by bytes assembled BY HAND. Masking is a security requirement, not ceremony. UTF-8 is validated on the reassembled message (a rune may straddle a fragment). Bounds are checked against the peer's ANNOUNCED length before any allocation, and there is no "unbounded" setting. permessage-deflate is refused on the wire twice over. Origin is checked by default — the browser's same-origin policy does not apply to WebSocket. **It also fixes a defect that predated it**: the engine closed every hijacked socket the instant net/http reported the hijack, so no upgrade-based protocol could work at all; a hijacked connection is now the handler's, and is not waited for by the drain — exactly as net/http's own Shutdown carves out. Mutation-checked. Blocks `0.2.11.27`–`0.2.11.33`. No client — the outbound contract returns a fully-read body, never a socket | Accepted (extends 0029) |
| `0048-sdk-metrics-otlp-json.md` | OTLP/JSON, the wire the OTel data model was adopted for — implemented from the document with `encoding/json` and `net/http`, importing neither `go.opentelemetry.io` nor a protobuf runtime. TWO surfaces, and the boundary is the byte slice: `EncodeOTLPJSON` is a pure snapshot→bytes function, `NewOTLPHTTPExporter` adds only transport — so an encoding defect and a network defect are never the same investigation. The four protobuf-JSON rules that are invisible until a receiver silently drops the payload are each cited and tested: lowerCamelCase names, 64-bit integers as decimal strings, enums as INTEGERS (OTLP overriding generic proto3 JSON, which uses names), and unknown fields ignored. Three fields are emitted at their zero because presence is meaning — the `asInt`/`asDouble` oneof, the `optional` histogram `sum`, and `isMonotonic`, which would otherwise vanish exactly when it carries the surprising answer. Field order is the schema's FIELD-NUMBER order, so the hand-written expected bytes are checkable against the .proto rather than against the code. Delta temporality TRAVELS (the whole difference from Prometheus); `UNSPECIFIED` is REFUSED because the schema says it MUST NOT be used, as is a bucket ladder that is not one count more than strictly-increasing finite bounds. Non-finite doubles are named `"NaN"`/`"Infinity"`, an unset `time.Time` encodes as 0 rather than `UnixNano`'s undefined year-2339 garbage, and HTML escaping is off so a URL attribute stays readable. The POST is a connector: bounded response read, no redirects (CWE-918), endpoint refused at construction, and NO retry — it classifies (`OTLPRetryable`) so `resilience` owns the backoff. Registered on stderr as `otlpjson`; the HTTP emitter is never registered, because arming a network client from an import is worse than arming a writer. Blocks `0.3.45.5`–`0.3.45.10`, all inside the block the package already owns | Accepted (closes 0044 §Deferred) |
| `0049-cache-becomes-a-domain.md` | `cache` becomes the 16th core sibling and the ADR 0025 kernel primitive does not move — the domain is added ABOVE it, carrying the three things a primitive deliberately lacks: tag invalidation, stampede protection, tier chaining. `Store[V]` is FROZEN at three methods with `EntryFetcher`/`Tagger`/`Loader` as type-asserted siblings, guarded by a named three-method-double test (0039); no shape published by 0025 changed, so the 0040 v0 licence is not used — and that is said out loud rather than left silent. **No registry**, and the mechanics settle it: `Store` is generic in `V` and Go has no `map[Name]Store[V]` for an open `V`, so a registry would have to erase the very type the port exists to keep. Tag invalidation is indexed both ways and MEASURED — 100 entries out of 1 000 costs 31.2 µs, out of 100 000 costs 36.2 µs, so a 100× store costs 1.16× and the residual is locality, not work; the reverse index must be unlinked on FOUR paths that share no mechanism, because the primitive deliberately does not fire `OnEvict` for an explicit `Delete`. One mutex covers the primitive and the index, `Fetch` takes it exclusively (a hit mutates — the reason 0025 named it Fetch), and `onEvict` takes NO lock because it runs on the caller's goroutine inside a held one. The chain refuses at construction any tier that cannot return its entry: promoting without tags produces a copy `InvalidateTag` can never reach, and an invalidation that silently misses one copy is worse than one never offered. Its `Delete` goes far→near because near-first would let the next read promote the value back and undo the delete permanently. Stampede protection stops at the PROCESS boundary and says so in four places. Backed by a new kernel primitive, `singleflight` (`Group[K,V]`): `fn` runs on its own goroutine under `WithCancel(WithoutCancel(leaderCtx))`, so an abandoning caller gets its own `ctx.Err()` while the call survives for the rest, and is cancelled only when the LAST caller leaves; a panic is captured WITH the originating stack and re-raised in every waiter, because the alternatives are a fault converted to an ignorable value or a crash with N goroutines parked on a channel that will never close; measured at ≈2 µs per leading call, which the package documentation turns into a rule about when not to use it. Its claimed second consumer, `config`, was checked and **does not exist** — recorded, with two named candidates deferred, rather than quietly dropped. Blocks `0.2.18.*` / `0.3.48.*` | Accepted (amends 0025) |
| `0050-sdk-lifecycle-domain.md` | Ordered start/stop domain (`lifecycle`), the 17th core sibling: the SDK had every piece of a service's startup and no assembly of them, and the argument is **two lifecycle defects found in this repository** rather than an analogy — ADR 0043, where one endless stream spent the whole drain budget so `DRAIN_TIMEOUT` was the normal outcome of every deployment, and ADR 0047 §D9, where the engine closed the socket it had just handed to the handler so no hijack worked at all. Both are cleanup that ran, looked reasonable in isolation, and destroyed something still needed. **No graph and no autowiring, both recorded as decisions**: a linear sequence already IS a topological order and the caller who wrote the constructors is the only party who knows it, while a convention that resolves dependencies is a framework — the cost (the SDK cannot detect a wrong order) is stated rather than hidden. `Start`/`Stop` are FUNC ports, satisfying ADR 0039 structurally. The partial-start unwind rests on three mechanics: a component is recorded up the instant its `Start` returns nil and BEFORE the next is attempted, the reversal happens in exactly ONE place, and the unwind calls the SAME `stopEach` an ordinary `Stop` calls — with contexts derived by `WithoutCancel`, because a start most often fails BECAUSE its context was cancelled and a cleanup driven by that context does nothing. The component that failed is deliberately not stopped: a `Start` that fails owns what it acquired, which is the only rule under which every other `Stop` may assume its own succeeded. The shutdown budget is PER COMPONENT and not shared — ADR 0043's lesson one layer up — and expiry does exactly three things: cancels that `Stop`'s context (an announcement), stops WAITING, and records `StopTimeout`. It kills no goroutine (Go cannot) and closes nothing the component owns (ADR 0047 §D9), so a budget that expires never cuts short what was about to finish; `Stop` returns in at most n × StopTimeout, said out loud. `Stop` reads its own context for values and never for cancellation, since the context that announces a shutdown is the one just cancelled. A non-positive budget CLAMPS to 30s (ADR 0031: a field on a struct that does other work, and Kubernetes' own floor), guarded observably. Signals and `sd_notify` are opt-in `RunConfig` fields delegating to `service/proc/{signal,sdnotify}`; an empty signal set leaves a nil channel in the select, the right absence. Errors aggregate with `errors.Join` because `errs.Wrap` would hit origin-wins and relabel the SDK's verdict with the component's code. No registry. Nothing waits on the wall clock, enforced by a named AST audit over production code AND the suite, itself proved to fire against fixtures. Blocks `0.2.19.*` / `0.3.49.*`. Two justifications offered for the domain are explicitly refused: the `HttpKernel` analogy, and `google/wire` as a model (archived 2025-08-22) | Accepted |
| `0051-sdk-trace-domain.md` | Distributed tracing as the 17th core sibling, and observability's third pillar. OTel is a published SPECIFICATION, so the trace data model and W3C Trace Context are implemented from their documents with zero `go.opentelemetry.io` imports — the official SDK would introduce the banned `x/sys` in exchange for a model that is a shape, and interoperability is a property of the WIRE, not of the import graph. **The decision is taken once, at the root**, and travels in the `sampled` bit: a per-span decision makes a trace with holes, and a span whose parent was dropped is an orphan the backend renders as its own root — so one request appears as several and nothing looks wrong at the process that caused it. An unsampled span is therefore a no-op that still CARRIES a context, because without the header every downstream hop starts its own root and one dropped trace becomes N kept ones. `Extract` returns NO error: §4.3 prescribes exactly one response to a malformed header a stranger wrote, and handing back an error invites the one response the specification forbids. Each refusal cites its section (all-zero trace-id §3.2.2.3, all-zero parent-id §3.2.2.4, version `ff` §3.2.2.1, an undashed tail on a higher version §3.2.4 — without which a 56th hex byte reads as a TRUNCATED flag field and the sampled bit is silently wrong); undefined flag bits are masked on OUTPUT and preserved on input, the only combination §3.2.2.5.2 and §3.2.4 both permit. `tracestate` is an ordered immutable list rather than a map because §3.5 makes the order meaning, and the grammar's own numbers are the limits — with ONE stated leniency at the list's whitespace edges, where RFC 7230 already normalises. **`Ratio(0)` is refused**: 0 means both "sample nothing" and "nobody set this", and honouring it disables tracing silently, since the symptom is the absence of telemetry (0031). The attribute, `Resource` and `Scope` types are ALIASES of `core/metrics`' rather than twins — they are `common.proto`/`resource.proto`, shared by every signal — so `trace.Attr` IS `metrics.Attr`, a process cannot carry two disagreeing `service.name`s, and the exemplars 0044 deferred have somewhere to put a trace id (with the hand-off spelled out in §Deferred); `DefaultScopeName` is the one thing deliberately NOT shared, because a scope names the library that produced THIS signal. OTLP/JSON on `/v1/traces` follows 0048 exactly — pure encoder, separate emitter, field-number order (evidence: `flags` is emitted last, after `status`, because 16 follows 15) — plus a fifth rule of its own: `traceId`/`spanId` are **hex, not base64**, the one place OTLP overrides the protobuf-JSON mapping and the one a generated encoder gets wrong silently. `flags` is `fixed32` so it is a JSON number and is always emitted; `status` is OMITTED when unset, the opposite call from metrics' three always-emitted fields and for a stated presence reason; `parentSpanId` is absent on a root, because sixteen zeroes would be the INVALID identifier. The emitter is never registered — one step beyond 0030, since no endpoint could be a correct default — bounds its response, refuses redirects (CWE-918), refuses a pathless endpoint at construction, and classifies instead of retrying. The two middlewares are `core/net`'s OWN generic middleware type instantiated at `http.Handler` and `http.RoundTripper`; the span is named by METHOD alone (a path in a span name is the cardinality mistake that becomes a bill), 4xx is an error on a CLIENT span and not on a SERVER one, the request is CLONED because RoundTrip must not modify it, and the status wrapper implements `Unwrap` and NOTHING else — declaring Flush/Hijack would make it claim capabilities the real writer may not have, which is the 0047 defect re-introduced one layer up. Blocks `0.2.20.*` / `0.3.50.*` | Accepted (closes part of 0044 §Deferred) |
| `0052-sdk-lock-domain.md` | Mutual-exclusion domain (`lock`) as the 17th core sibling, **no registry** — the two backends differ in the one property a caller must know, so resolving one from a config string would let a typo swap a lock that never expires for one that does, with every call still succeeding. `Locker` frozen at two methods, `Lease` at three, and NEITHER takes a TTL: a lease lifetime belongs to the work, is configured where the locker is wired, and therefore cannot be typed as a zero in a hot path. A non-positive TTL is REFUSED at construction, because its two natural readings — "already expired" and "never expires" — are opposites, and the first grants every `Acquire` while excluding nobody, which is indistinguishable from a lock nobody contends (0031's refuse half; `FileConfig.Poll` is the clamp half, deliberately in the same package). The ADR's subject is what expiry forces, decided out loud rather than left emergent: **ownership** is token-checked, so a lapsed holder's `Release` releases NOTHING and reports `LOCK_NOT_HELD` — unlocking by name would have it end the new holder's section, and the new holder would never learn of it; **renewal** is `Extend`, refused on a lapsed lease even when nobody has taken the lock, because expiry is the deadline and not "until someone else takes it", plus a `Keepalive` that CANCELS a derived context rather than returning an error, since the caller who needs the news is already inside the section and its context is the only channel that reaches it (the exact inverse of 0043's drain signal, and for the same reason stated backwards); **fencing** SHIPS — a strictly-increasing per-name `uint64`, in-memory for one backend and `fsync`ed into the lock file for the other, so it survives a reboot — and a corrupt ledger is REFUSED rather than reset, since a restarted fence reissues numbers the resource has already accepted. What the domain does NOT guarantee is stated in four places: the SDK can only ISSUE a fence, and where the protected resource cannot compare it, mutual exclusion is not guaranteed against a GC pause or a `SIGSTOP`. The two backends expire differently on purpose — a dead process is noticed by the KERNEL, a dead goroutine by nobody — so the memory lease expires and implements `Deadliner` while the file lease never expires and deliberately does not, the absence being how a caller discovers which world it is in (0039). `flock(2)` was MEASURED before the backend was written: two open file descriptions in one process do exclude, but the same description re-locked is a lock CONVERSION that returns at once, and eight goroutines sharing one descriptor were all inside one counted section, every run — the efficient-looking design gives ZERO goroutine exclusion while working perfectly between processes, invisible to any test that spawns processes. So the file locker takes an in-process gate first, never calls a blocking `flock` (no cancellation reaches a parked syscall), and refuses a world-writable non-sticky directory, where unlinking the lock file hands the next process a different INODE to lock. Windows refused rather than approximated — `LockFileEx` is byte-range and mandatory, and "almost like flock" is the whole problem for a primitive whose failures are invisible when they happen. No test in the domain sleeps, enforced by the 0041 AST audit. Blocks `0.2.21.*` / `0.3.51.*` | Accepted |
| `0053-sdk-events-domain.md` | In-process event bus domain (`events`), whose first job is to draw **the line between itself and the `queue` domain that follows** — before either is written, because the two are described with the same words and guarantee opposite things, and the library that supports both reliably guarantees neither. `events` is one process, synchronous, the publisher's goroutine, the publisher's transaction; `queue` is many processes, asynchronous, durable, retried, dead-lettered. There is deliberately **no async mode, not even as a `Config` field**: it would return before the work happened, take the listener's error off the caller's error path, and lose the work on a process exit — none of it visible at the `Publish` call site. The answer for a caller who needs both is two statements. The routing key is the concrete Go TYPE and not a name, because a name is unchecked in every direction that matters and its failure mode is silence; an interface type is refused at registration, since `reflect.TypeOf` yields a DYNAMIC type and never an interface, so such a subscription would be accepted, listed and never called — ADR 0031's inert registration exactly. Go methods cannot take type parameters, and that settles the typing: the port is erased (`Listener func(ctx, any) error`) and a package-level `On[E]` derives the key with `reflect.TypeFor[E]()` and wraps the typed function in one that asserts, both from the same expression so they cannot disagree; the alternative, `Bus[E]`, carries exactly one event type and is a typed channel with a registry bolted on. **The assertion was measured against an identical listener registered erased and never converting — 2.4 ns per call over ten runs at 2 s, 3.5 % of a one-listener dispatch, ranges barely touching** — and the one cost the domain cannot remove is published next to it: 16 B and one allocation to box the event, which every other bench row hides behind a hoisted loop-invariant. Ordering is ascending priority with ties in REGISTRATION order, both promises, asserted on the observable trace and across ten consecutive publishes because one correct dispatch proves nothing about the next; the list is sorted at registration, so the read path allocates nothing. Stopping propagation is an authority written at the wiring site (`MayHalt`, zero value false) and exercised by returning `Halt`, the `io.EOF` shape — an unauthorised halt continues the dispatch and is reported as `HaltNotPermitted`, because honouring it would make the permission decorative and swallowing it would leave a listener convinced it vetoed an event that every sibling then saw. A halt is a decision, not a failure, so `Publish` returns nil and reports it through `Halted`/`HaltedBy`/`Skipped`. A listener error never short-circuits — listeners are independent consequences of one fact — and aggregates with `errors.Join`, the SDK's verdict beside the listener's own error so `errs.HasCode` and `errors.Is` both answer. `ctx` is passed through and never inspected: an event is a fact that already happened, and abandoning half its consequences is the partial state the synchronous contract prevents. A panic is recovered on the publisher's own goroutine — no boundary, so no `PanicValue` type is needed, unlike `kernel/group` — carries the originating stack in a field, travels as a FIELD so it cannot hijack the code, is classified BEFORE the halt so a crash is never read as a decision, and does not stop the siblings. **`kernel/topic` was read in full and deliberately not used**, with the reason recorded rather than left as an absence: its per-subscriber buffer makes `Publish` return when a value entered a buffer rather than when a listener returned, and puts `Halt` on the wrong side of a channel where "before the next listener" does not exist; what the domain actually needed was the copy-on-write membership underneath it, which is `kernel/snapshot`. No registry, no `Config` (every knob considered was a contract, not a setting), no new kernel primitive. Blocks `0.2.22.*` / `0.3.52.*` | Accepted |
| `0055-sdk-sql-domain.md` | Relational-database domain (`sql`): a set of PORTS above `database/sql` and deliberately never an ORM — the ports speak the stdlib's own `*sql.Rows`/`*sql.Row`/`sql.Result` because re-declaring them is the first step of the mapping layer this domain refuses to grow. **`database/sql` is permitted, a driver never is**: pgx / go-sql-driver / the sqlite bindings are vendor connectors and live under `third-party/` or nowhere (ADR 0012), so the suite writes its OWN `driver.Driver` — which buys a statement log, deterministic failure on a named statement, and no container. Transaction ownership is STRUCTURAL: a `TxFunc` receives an `Executor` with no Commit, no Rollback and no Begin, so "a callee committed under its caller" does not compile. Nesting is a SAVEPOINT with every trap closed — an SDK-generated name (an identifier cannot be bound), a counter that never resets inside one transaction (a reused name shadows rather than replaces, so RELEASE frees the wrong one), non-zero options REFUSED because a savepoint changes neither isolation nor read-only-ness, and a context CHAIN keyed on the manager pointer so two databases never nest into each other. A failed sub-transaction does not condemn the outer one — `ROLLBACK TO SAVEPOINT` is exactly what clears SQLSTATE 25P02 — unless that statement ITSELF fails, which POISONS: the engine's state is unknown and committing work the SDK believes it undid is the one outcome worse than failing. **A cleanup statement never travels on the context whose cancellation caused it** (ADR 0050's rule one layer down): the nested undo ran on the expired context, `grabConn` refused it before the driver, and a per-scope deadline poisoned a healthy transaction — a real defect, found by writing the test first, and `ErrTxDone` is now separated from poison because "already over" is a KNOWN state. The dialect set is CLOSED with two distinct refusals (never-heard-of vs recognised-and-declined-with-a-reason): T-SQL has no RELEASE, Oracle has none, Db2 mandates a cursor-retention clause — nesting there is a different algorithm, not a different string. The migration lock is the ENGINE's session-scoped advisory lock on a dedicated connection, and NOT `pkg/v1/lock`, because what decides it is what happens when the holder dies: a row in a table or a lease in a service survives that death and every later deploy hangs or is invited to steal, while the server drops a session lock when the process stops existing — no lease, no heartbeat, no stealing, and the same failure domain as the schema. SQLite is refused at construction BY NAME rather than run unlocked. **A partially applied migration is answered per engine**: PostgreSQL and SQLite roll the schema change and its version row back together; MySQL's DDL commits IMPLICITLY, so the change is already committed while its row is rolled back — no client can fix that, so it is stated in four places, the run stops at the first failure, and the consumer's rule is one DDL statement per migration. `MaxOpen` is REFUSED when non-positive (database/sql reads 0 as unlimited) while `MaxLifetime` CLAMPS (0 means "reuse forever", which survives a failover) — both sides of ADR 0031 in one struct. No registry, no migration file format, no directory convention. Nothing waits on the wall clock, enforced by a named AST audit proved against fixtures. Measured in `internal/service/sql/BENCH.md`: ownership 715 ns / 4 allocs over raw `database/sql`, the Executor guard 25 ns and ZERO allocations per statement, one nesting level ~1.9 µs — the actionable fact being two extra round trips per nested scope. Blocks `0.2.24.*` / `0.3.54.*` | Accepted |
| `0056-sdk-vfs-domain.md` | Filesystem domain (`vfs`), 18th core sibling: reading is `io/fs` **unchanged** (`FS` is a type ALIAS, so `fs.WalkDir`/`fs.Glob` apply with no adapter and there is deliberately no `vfs.Walk`), writing is a port FROZEN at four verbs, and publication is `AtomicWriter` — an ADR 0039 sibling, because a filesystem that cannot publish atomically is still a filesystem. The ADR's subject is **how far "atomic" reaches**. It guarantees: a concurrent reader sees the complete old or complete new content (measured — 0 torn reads out of 600, against 540–597 for an in-place write under an identical harness); on any failure through the rename the previous bytes are byte-for-byte intact and no temporary survives (proved by injecting a failure at each of five steps over a REAL half-written handle and comparing SHA-256 before and after); and after step 5 both the file and its directory entry are flushed. It does NOT guarantee anything across filesystems (`EXDEV` is made unreachable by creating the temporary in the target's directory, never handled — and there is no copy-and-delete fallback, because that is not atomic and would make the method's guarantee depend on a mount table), anything where `rename` is not the kernel's (FUSE, SMB, FAT/exFAT may implement it as unlink-then-link; on NFS a client attribute cache can still serve stale metadata), durability beyond what `fsync` actually does (a device that lies about write-through loses the file and no code here can detect it), mutual exclusion between publishers (no locking, deliberately — the last renamer wins and no reader sees a mixture), or freedom from a symlink planted between the `Lstat` and the open (the race-free property is confinement to the root, and that one is the kernel's via `os.Root`). A post-rename directory-flush failure is a DIFFERENT verdict (`DirectorySyncFailed`) and is deliberately not rolled back, because undoing it means a second non-atomic write to repair a durability problem. Windows is refused at construction (ADR 0018): no directory flush exists, an `fs.FileMode` is not an ACL, and `MoveFileEx` fails when a reader holds the destination. Measured: publication costs two device round trips and NOT the bytes — a 256× larger payload costs 4.7 % more — so batching helps and buffering does not. Blocks `0.2.25.*` / `0.3.55.*` | Accepted |
| `0060-sdk-health-domain.md` | Liveness and readiness are different questions, so they take different TYPES: `Check` gets a context, `SelfCheck` does not — a liveness check therefore cannot call a dependency without a closure that visibly discards the deadline. Startup gates liveness so a slow boot is never read as a dead process, and `Drain` makes readiness fail permanently without running a check, ahead of ADR 0043's signal | Accepted |

## Conventions

- File name: `NNNN-<short-slug>.md` where `NNNN` is the next zero-padded number.
- **Two branches writing ADRs at once: claim distinct numbers, then merge in
  numeric order.** The number is claimed when the file is committed on a branch,
  not when the branch merges, so a second branch reads the first one's claim and
  takes the number after it. Merging low-to-high keeps `main` contiguous at every
  point in time, which is what the check below verifies. Merging high-first
  leaves `main` with a real gap until the other branch lands — legal in the end,
  but the check fails in between, so do not do it. **Renumbering to close that
  window is the one wrong answer**: the branch that already holds the lower
  number would then collide with it, and a number is cited from ADR headers,
  package docs and source comments long before it merges.
- Header block: **Status**, **Date**, **Deciders**, plus **Supersedes** / **Superseded by** / **Amends** / **Related** where they apply.
- Standard sections, in this order: **Context**, **Decision**, **Consequences / Semantics**, **Breaking changes**, **Alternatives considered** and/or **Why not <option>**, **Deferred**, **References**. This is the same list as `docs/CLAUDE.md` §ADR conventions — keep the two in step; when they disagree, the two are both wrong until they agree.
- A standard section with nothing to say says so rather than being dropped — `Breaking changes` → "None. `<domain>` is a new domain in this change set" is the house form (ADR 0026/0027/0028). A missing heading reads as an oversight; an explicit "None" reads as a decision.
- Mermaid / ASCII diagrams welcome where they reduce reading time.
- Cross-link with relative paths: `../adr/0005-…md` so links survive a move.
- Use `1.2.0.3` style for codes in prose, never the hex literal.

## Do NOT

- Edit a merged ADR's `Decision` section. Supersede it via a new ADR that links to the old one and flips the old `Status` to `Superseded by NNNN`.
- Store implementation detail here — keep ADRs about the *decision* and the *constraint*, not the code. Implementation specifics live in the package `CLAUDE.md` / `README.md`.
- Reference internal commit SHAs or PR numbers in the body — the URL in a markdown link is the only authoritative reference.

## Verification

```
ls docs/adr/ | grep -E '^[0-9]{4}-' | sort
# every ADR must follow the NNNN-slug.md pattern; no gaps in the numeric sequence.

# On a branch, a gap is expected when a lower number is claimed on another
# branch — check before blaming the sequence, and merge low-to-high.
# The fetch is not optional: git ls-remote reads the remote without updating
# origin/*, so without it git ls-tree silently sees nothing for a branch this
# clone has never fetched, and the check reports "no one holds it".
git fetch --prune origin
git for-each-ref --format='%(refname)' refs/remotes/origin \
  | grep -v '^refs/remotes/origin/HEAD$' \
  | while read -r ref; do
      git ls-tree --name-only "$ref" docs/adr/ \
        | grep -oE '[0-9]{4}' | sed "s#^#${ref#refs/remotes/origin/} #"
    done | sort -u -k2
```
