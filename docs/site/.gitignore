node_modules/
dist/
.astro/
# pagefind generates dist/_pagefind/ as a post-build step (npm run build).
# Already covered by dist/ — listed here for discoverability.
# Generated by scripts/sync-versions.mjs at build time — populated from
# real release tags (see ADR 0007). Never commit; always rebuildable.
src/data/versions.json
src/data/build-info.json
public/_search/
src/content/docs/*/
