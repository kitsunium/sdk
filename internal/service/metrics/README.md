# metrics (service)

In-memory `Meter` + lock-free Counter/Gauge/Histogram instruments implementing
`internal/core/metrics`, plus a stdlib text Exporter. Cross-OS portable. Public
facade: `pkg/v1/metrics`. ADR 0027. See `CLAUDE.md`.
