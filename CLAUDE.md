<!-- updated: 2026-05-21T21:27:56Z -->
# kitsunium/sdk

## Purpose

Go SDK providing a normed, performant toolbox for downstream applications. Thirty domains ship today — a structured **logger** (one alloc per emit, multi-sink; the `sync.Pool` recycles the builder but the handler clones the attrs — see `pkg/v1/logger/BENCH.md`, pinned by `TestV116BuildSendAllocatesOnePerEmit`; **trace-correlated by default** — ADR 0062: a record emitted inside a span carries `trace_id` and `span_id` as top-level fields in lowercase hex, a record emitted outside one carries neither key rather than an all-zero identifier W3C declares invalid, both names are RESERVED at that level so a caller attribute spelling one renders as `attr.trace_id` rather than letting a decoder read it as the line's correlation — ADR 0070 — and it costs no second allocation because the ids are hex-encoded straight into the handler's borrowed buffer and never become a Go string), a universal **codec** (24 wire formats behind a single `Marshal/Unmarshal` dispatch), typed **errs** (dotted-quad codes + public/private split), a **crypto** suite (AEAD, hash, sign, MAC, KDF, key-agreement, password hashing, and JWK/JWKS key representation — RFC 7517 for EC/OKP/oct, with private export opt-in and never the default), **transform** (compression — stdlib gzip/flate/zlib; `flate` is raw DEFLATE, `zlib` is the RFC 1950 envelope HTTP misnames `deflate`), OS **proc** supervision, **id** generation (UUIDv4/v7, ULID, snowflake, NanoID, KSUID, TypeID — ADR 0024), **resilience** policies (retry/circuit-breaker/rate-limit/bulkhead/timeout/fallback/hedging — ADR 0026; hedging duplicates the operation, so it demands an in-code idempotence assertion and a load cap — ADR 0031), **metrics** (the OpenTelemetry metrics DATA MODEL, implemented from the specification with **zero** `go.opentelemetry.io` imports — ADR 0027/0044: typed attributes whose KIND is part of the series identity, explicit delta/cumulative temporality where a delta `Collect` consumes the window it reports, `Resource` + `InstrumentationScope` carried once per payload, counter/up-down-counter/gauge/histogram plus observable instruments read at collection, all over a bounded, visibly-overflowing cardinality; the stdlib `prometheus` exporter is a deliberately LOSSY connector that refuses a snapshot that is not cumulative and refuses a name the exposition format cannot spell rather than transliterating it into a collision, while **OTLP/JSON** — ADR 0048, written from the specification with `encoding/json`/`net/http` and no OTel or protobuf dependency — is the native wire that loses nothing and carries the temporality), **net** (inbound and outbound over one substrate: TLS/mTLS identity, per-phase deadlines, policy, and Server-Sent Events and **WebSocket** — ADR 0029/0043/0047; RFC 6455 is implemented in the stdlib and its MUST-fails are enforced rather than tolerated, because a length-prefixed frame stream that keeps going after a disagreement is two endpoints reading different messages from the same bytes), and **config** (env+file layering, typed decode, cross-OS poll-watch — ADR 0028; plus a **schema** — ADR 0061: a missing required key fails at STARTUP and every missing key is named in one error, because the pass runs over the merged map BEFORE the decode, the only position where "absent" and "explicitly zeroed" are still distinguishable; a key no field addresses is REFUSED by default, since the symptom of an ignored `APP_PORTT` is the absence of an effect; a default is a LAYER under every source rather than a post-decode fallback, so `timeout = 0` stays 0, and a key is never both required and defaulted, or the clause could never fire; the schema describes the SHAPE and delegates the VALUE to `validation` — `Rule` IS a `validation.Constraint` and there is no `min`, `max` or `oneof` in this domain; and no message ever names a value; plus PROVENANCE — ADR 0097: `LoadWithOrigins` / `LoadSchemaWithOrigins` return, per leaf key, the layer that supplied its final value and the variable or file behind it, never the value, through a `Describer` sibling of the frozen `Source`, and a `secret.Value` field is marked `Secret` and filled from the environment's RAW text, because the JSON coercion that makes `8080` an int makes `1e3` a thousand). The Phase-B wave also adds the kernel `cache` primitive (ADR 0025). New domains land in the same 4-layer shape (ADR 0001), and **scheduler** (five-field POSIX cron + fixed intervals — ADR 0041; DST, missed deadlines and overlap are decided and documented rather than emergent: a non-existent wall-clock time does not fire, a repeated one fires once, a missed deadline is skipped **and counted**, and an overlapping fire is skipped by default). The Phase-B wave also adds the kernel `cache` primitive (ADR 0025). New domains land in the same 4-layer shape (ADR 0001), and **token** (JWT over JWS Compact + PASETO v4.public — ADR 0042; the algorithm is bound by the constructor, never read from the token, so algorithm confusion is a call that does not compile, and `alg:none` has no representation in the type). The Phase-B wave also adds the kernel `cache` primitive (ADR 0025). New domains land in the same 4-layer shape (ADR 0001), and **validation** (a `Constraint` port, a located `Violation` and the engine that composes them — ADR 0046; a violation says WHERE in one path grammar (`user.addresses[2].zip`), every violation is collected by default and stopping is a real short-circuit rather than a truncated report, a violation is not an error and neither is the report — `Report.Err()` converts on demand and returns a genuine nil — and a message names the rule and the bound but never the value, which is a security property with its own test; both a reflection-free programmatic path and a struct-tag front end whose plan is cached per type, measured in `internal/service/validation/BENCH.md`; it FEEDS `config.Validator` rather than replacing it), and **session** (server-side sessions — ADR 0045; an opaque 256-bit identifier that redacts itself, a memory store and a file store behind one frozen `Store` port, expiry that is absolute AND sliding with the earlier deadline always winning, and `Regenerate` as the only call that binds a subject — so session fixation is prevented by the absence of any other way to spell a login rather than by remembering a step), and **cache** (ADR 0049, amending ADR 0025: the kernel LRU+TTL primitive stays exactly where it was and a DOMAIN is added above it, carrying the three things a primitive deliberately lacks — invalidation by tag, protection against a stampede of concurrent misses, and L1/L2 chaining. `Store[V]` is frozen at three methods with `EntryFetcher`/`Tagger`/`Loader` as type-asserted siblings; there is no registry, and here the mechanics settle it rather than taste, since Go has no `map[Name]Store[V]` for an open `V`. `Fetch` keeps the primitive's honest verb because a read mutates, and in the memory store it takes a write lock for exactly that reason. Tag invalidation is indexed both ways and measured — 100 entries out of 100 000 costs 1.16× what the same 100 cost out of 1 000, so it does not scan. The stampede protection collapses concurrent misses **within one process and not across replicas**, which is stated in four places because a reader who assumes otherwise sizes an origin wrong by a factor of N. It rests on a new kernel primitive, `singleflight`, where a caller that abandons does not condemn the others still waiting and a panic is re-raised in every waiter carrying the stack of the goroutine that actually failed), and **lifecycle** (ADR 0050 — ordered bring-up, reverse teardown, and the two things everyone gets wrong: a partial start is unwound before `Start` returns, through the SAME code path an ordinary `Stop` uses and with contexts detached from the cancellation that caused the failure, while the component that failed is never stopped because a `Start` that fails owns what it acquired; and the shutdown budget is PER COMPONENT, so the first thing that will not finish cannot spend everyone else's — an expired budget cancels that `Stop`'s context, records it, and steps aside, killing no goroutine and closing nothing the component owns. The Add order is the dependency order and there is deliberately no graph and no autowiring; signals and `sd_notify` are opt-in and delegate to `proc`), and **lock** (ADR 0052; named exclusive leases over one process or one machine. A lock that lies is worse than no lock, so a zero TTL is REFUSED at construction rather than read as "expires immediately", `Release` releases only a lock this holder still holds — a lapsed lease releases NOTHING rather than ending the new holder's section — `Extend` renews and a `Keepalive` cancels the protected work's context when it cannot, and every acquisition carries a strictly-increasing fencing token. What the domain does not guarantee is stated as loudly: the SDK can only ISSUE a fence, so where the protected resource cannot compare it, mutual exclusion is not guaranteed against a GC pause or a `SIGSTOP`. The in-process lease expires and says so through the `Deadliner` sibling; the file lease never does, because the kernel already notices a dead process, and its absence of `Deadliner` is how a caller finds out. `flock(2)` gives ZERO exclusion between goroutines on a shared descriptor — measured at 8 of 8 inside one section — so the file locker composes it with an in-process gate. Windows is served by `LockFileEx` since ADR 0081, bound from `kernel32` with no `x/sys`, and its answer to that same question is the OPPOSITE — re-locking one handle is refused where `flock` converts — so the gate is redundant for exclusion there and kept for three other reasons; its locks are also MANDATORY, which protects the fencing ledger better than an advisory one and costs a non-holder the ability to read a HELD lock file; and the lock directory's permission rule runs on Unix only, because `os.Stat` synthesises `0777` for every writable directory on Windows and the rule would refuse all of them. That directory rule was also the whole of the answer to substitution, and it reached only half of it — ADR 0082: the lock filename is `hex(sha256(name))`, steered by no caller string and in the same stroke PREDICTABLE, and the rule governs UNLINKING an entry that exists while the attack CREATES one at a name nobody has taken, which `0777|sticky` — what `/tmp` is, a row the table explicitly accepts — permits. Probed against the shipped locker, an acquisition over a planted symlink returned `held=true` with no error and put the `flock` and the fencing ledger outside the checked directory, and a link to a decimal file handed the planter the fencing token the victim then reported. It is closed on BOTH kernels by one refusal with two mechanisms — `O_NOFOLLOW`, where the kernel fails the open, and `FILE_FLAG_OPEN_REPARSE_POINT`, where the open SUCCEEDS on the link and the handle is rejected on its attributes — under the new `LOCK_PATH_REDIRECTED` rather than `LOCK_BACKEND_FAILED`, because nothing failed and that code invites a retry, which is the one wrong response; and the errno is never branched on, since `O_NOFOLLOW` is `ELOOP` on Linux, `EMLINK` on FreeBSD and `EFTYPE` on NetBSD and a three-value table across six kernels is wrong on the seventh. ADR 0082 §Deferred named two things it did not close and ADR 0083 closes both, one by prevention and one by DETECTION, and says which is which: a link planted at a PARENT component still moved the whole lock directory, because `O_NOFOLLOW` is a final-component flag, so a new kernel primitive `pathchain` resolves a path one component at a time over `os.Root` directory handles and reports every indirection with the mode of the directory that HOLDS it — `syscall.Openat` was read rather than assumed and exists in go1.27 for linux, aix and wasip1 alone, so a hand-rolled walk would have served one of the six kernels `e2e-cross` runs. The policy stays in the domain because the primitive cannot have one: `/tmp` is a symbolic link on macOS and `/var/run` is one on most Linux distributions, so an indirection at a parent is refused only when the directory holding it is WORLD-WRITABLE, and the sticky bit exempts nothing there since planting a component creates an entry rather than unlinking one. The second half is `LOCK_FILE_REPLACED` (`0.3.51.5`): in a `0777|sticky` directory the entry's owner may unlink the lock file WHILE the victim holds it, reproduced as two holders over two inodes with the fence reset to 1 on both and the victim's `Extend` returning nil — and no flag prevents it, because the descriptor outlives the name on every kernel. So the lock is not kept and the holder is TOLD, at `Acquire` and at `Extend`, which a `Keepalive` turns into a cancelled context; a test asserts the second holder still acquires, on purpose, so nobody reads prevention into it), and **trace** (distributed tracing — ADR 0051; observability's third pillar, on the OpenTelemetry trace DATA MODEL and the W3C Trace Context propagation format, both implemented from their documents with **zero** `go.opentelemetry.io` imports and the same OTLP/JSON wire `metrics` already speaks, on `/v1/traces`. The sampling decision is taken ONCE at the root and travels in the `sampled` bit, because a per-span decision produces a trace with holes and a span whose parent was dropped is an orphan the backend renders as its own root; a malformed `traceparent` starts a new trace and never fails a request, which is what W3C §4.3 requires of a header a stranger wrote; and a sampling rate of exactly 0 is REFUSED rather than honoured, because it also spells "nobody configured this" and the symptom of guessing wrong is the absence of telemetry. Its attributes, `Resource` and `Scope` are the SAME types `metrics` uses — they are OTel's shared `common.proto`, so `trace.Attr` and `metrics.Attr` are one type and the exemplars ADR 0044 deferred have somewhere to put a trace id), and **events** (ADR 0053 — an IN-PROCESS event bus, and the ADR's first job is the line between it and the `queue` domain that follows: `events` is one process, synchronous, the publisher's goroutine and the publisher's transaction, with no durability, no retry and no dead-letter path, so a caller who wants "asynchronous but reliable" is sent to `queue` rather than served half of it — there is deliberately no async mode. Subscriptions are keyed on the event's concrete Go TYPE rather than a name, so the compiler mints the key and an interface type is refused at registration instead of being listed and never called; the typing is erased plus a package-level `On[E]`, because Go methods take no type parameters and a `Bus[E]` would carry exactly one event type — the assertion that buys the typing back is measured at 2.4 ns per listener call, and the 16 B the caller pays to box the event is published beside it. Listeners run in ascending priority with ties in registration order; stopping propagation is an authority declared at the wiring site and a halt is not a failure; a listener error never short-circuits and aggregates with `errors.Join`; a listener panic is recovered on the publisher's goroutine with its originating stack, is never read as a halt, and never silences the siblings), and **health** (ADR 0060 — liveness, readiness and startup are three different questions, and the domain makes them take three different TYPES rather than three different names. A readiness `Check` is `func(ctx context.Context) error` and may reach a dependency; a liveness `SelfCheck` is `func() error` and has no context to reach one with — so "liveness pings the database", the mistake that turns one slow dependency into a rolling restart of every replica, needs a closure that visibly discards the deadline. It does not become impossible, it becomes impossible to write by accident. Startup is the third: it latches, because a probe that can go back to "still starting" after it has reported ready is describing readiness, not startup), and **authz** (ADR 0057 — RBAC, ABAC and their combinators, resting on two decisions. **There is no policy DSL**: a condition is a Go func, a grant table a Go slice, a resource a string compared by byte equality. A DSL is a second, weaker language inside a program that already has one, it puts a parser on the authorization path, and it is untyped — while what it is bought for, changing a rule without recompiling, is already available by loading rule DATA through `config`. And **abstention is a third verdict**, not a shade of one of the other two: `Abstain` is the zero value, because folding "this policy has no opinion" into a grant is a hole, and folding it into a refusal is an outage that gets repaired by making the combiner permissive — which puts the hole back one layer up. `DenyOverrides` is the only combining algorithm, the XACML alternatives are refused by name, and an `Allow` does not short-circuit so the fold stays commutative and associative. A request no policy is applicable to is refused, in `Check`, the single closure that turns "nobody said Allow" into a denial — and it is a refusal rather than an error, because an unmatched request is not a fault. Every refusal renders the byte-identical sentence and names no policy, role or attribute, because a refusal that explains itself is a description of the policy set handed to the party it exists to keep out; the diagnosis lives in Private and fields. Evaluation allocates zero at ~210 ns), and **cli** (ADR 0065 — command-line applications on the stdlib `flag`, with zero third-party dependencies, because an argument parser is a mechanism and not a connector. It adds exactly four things `flag` lacks — sub-commands to arbitrary depth, one help generated from the declarations, a typed exit status, and a seam to `config` — and a `Binder` receives the stdlib's own `*flag.FlagSet` unwrapped, exactly as `vfs` takes `io/fs`. `flag.ExitOnError` is refused BY NAME, because it calls `os.Exit` from inside a library: the parse becomes untestable, every deferred function is skipped, and a decision that belongs to `main` is taken elsewhere — enforced by an AST audit over production code AND the suite. `flag` is also kept silent, since on failure it writes its own message and calls usage before returning the error. A command is a leaf or a group, never both and never neither, and the whole tree is validated at construction — which is what catches a command binding `-h`, the one flag whose absence is how `flag` signals a help request. There is no prefix matching and no "did you mean": the help just written already lists every name that would have worked), and **i18n** (ADR 0063 — message translation, and the failure it exists to refuse: Polish has four plural categories and English two, so a library that resolves an unknown language to English's rules renders a grammatically wrong sentence on every page and NOTHING observes it — the page renders, the tests pass, nothing is logged. So the CLDR subset is NAMED: thirteen hand-transcribed entries covering all six categories and every rule shape, and every other language is refused BY NAME at construction with the supported set beside it. A counted message missing a category its own language can produce is refused at LOAD, naming the file, the key and the form — the one catalogue defect a translator cannot see by reading their own file. The plural rules follow the MESSAGE and not the request, so an English message serving a Polish miss is pluralised with English rules rather than asked for a `few` it does not carry. Negotiation is RFC 4647 §3.4 Lookup for selection and §3.3.1 basic filtering for a `q=0` refusal, §3.3.2 refused by name, and it has no error return at all because the header was written by a stranger. A missing key renders the KEY plus an error, because a blank is a defect nobody reports. Interpolation is named placeholders, substituted once and never rescanned, never escaped — that is `view`'s job and it is contextual — and no error ever names an argument value. The catalogue is decoded through `codec` and read through `io/fs`, so the domain ships no parser and no filesystem), and **mail** (ADR 0064 — MIME composition is a mechanism and lives in the SDK, SMTP is a third-party protocol whose client is already in the standard library so it costs no dependency, and a provider API would be a `third-party/` connector. A CR or LF in a header is REFUSED and never repaired, because the three stdlib helpers that would repair it deliver a message the caller did not write while reporting success — and `net/mail.Address.String()` passes a CRLF in the *address* straight through. Bcc reaches `RCPT TO` and no header. The MIME container is a pure function of the populated fields, plain first inside `multipart/alternative` because RFC 2046 §5.1.4 orders alternatives by increasing preference, and the structure is checked by reparsing it rather than by comparing strings. `TLSMode`'s zero is refused, there is no opportunistic STARTTLS mode at all, and credentials never leave the process unencrypted — checked twice, because `smtp.PlainAuth` sends them in the clear to a server named localhost; an `SMTPConfig` writes `<redacted>` for its password under every fmt verb and in its JSON, and `ParseURL` reads `smtp://user:pass@host:587?tls=starttls|implicit|none` or `smtps://…` into a configuration `NewSMTP` accepts, never quoting the URL in a refusal), and **queue** (ADR 0054 — the asynchronous, durable counterpart `events` was drawn against before it existed, and the right-hand column of ADR 0053's frontier table on every axis: many processes, asynchronous, a consumer's goroutine, durable, retried, dead-lettered. Delivery is AT-LEAST-ONCE because exactly-once does not exist over a transport, and the consequence is in the type rather than in a paragraph — `Deliveries` is a field of every delivery and `ConsumerConfig.HandlerIsIdempotent` is refused at its zero value. A message is removed at acknowledgement and never at read, so a consumer that is SIGKILLed loses only its lease; ordering is not guaranteed and one retry is enough to lose it. The durable broker's entire state is a directory and every transition is one `rename(2)`, which is also the exclusion — the loser gets `ENOENT`, identically for goroutines and processes, unlike the `flock(2)` ADR 0052 measured — so there is no lock, no sweeper and nothing a dead process can hold. It composes `vfs` for the two writes that must be atomic and durable and stops where `WritableFS` stops, at `Rename`. Durability costs 1 865× on a round trip and the cost is the two `fsync`s and not the payload), and **sql** (ADR 0055 — a set of PORTS above `database/sql` and deliberately never an ORM: the ports speak the stdlib's own `*sql.Rows`/`*sql.Row`/`sql.Result`, because once `Rows` is ours scanning is ours and mapping is one refactor away. `database/sql` is permitted and a DRIVER never is — pgx, go-sql-driver and the sqlite bindings are vendor connectors that live under `third-party/` or nowhere — so the suite writes its own `driver.Driver`, which buys a statement log and deterministic failure on a named statement that no live database can give. Transaction ownership is structural: a `TxFunc` receives an `Executor` with no Commit, no Rollback and no Begin, so a callee committing under its caller is a sentence with no spelling. Nesting is a savepoint whose name the SDK generates from a counter that never resets — a reused name shadows rather than replaces, so `RELEASE` would free the wrong one — and a cleanup statement never travels on the context whose cancellation caused it, because the nested undo on an expired context poisoned a transaction that was perfectly healthy. The migration lock is the ENGINE's session-scoped advisory lock and deliberately not the `lock` domain, since what decides a migration lock is what happens when its holder DIES: a row in a table survives that death and every later deploy hangs behind it, while the server drops a session lock when the process stops existing. And a partially applied migration is answered per engine rather than glossed — PostgreSQL and SQLite roll the schema change and its version row back together, MySQL's DDL commits implicitly and cannot, which is stated in four places instead of once), and **view** (ADR 0058 — server-side rendering built ON the stdlib `html/template` and deliberately not reimplementing it: contextual escaping is not an escaping function but an HTML parser tracking the cursor through branches that must agree on where they leave it, and a hand-written one would be the single most likely source of the XSS the domain exists to prevent. `text/template` has no representation at all, because the two packages are API-compatible — the substitution compiles, passes every test that does not assert on escaped output, and ships stored XSS — so an AST audit fails the build on the import, in production code and in the suite. `Render` returns `[]byte` and never takes an `io.Writer`, since execution fails halfway and a `ResponseWriter` would already carry the status line and half the page. `TrustHTML` is the one bypass the SDK spells, the other six trust types are refused in render data, and what is NOT prevented is said out loud: `TrustedHTML` is a type alias — it must be, because html/template recognises trust by an exact type switch — so a `Trusted` built from user input is an XSS the SDK cannot see. Measured: reparsing per request costs 34× on a realistic tree and only 1.7× on a toy one, which is exactly how the mistake survives review), and **vfs** (ADR 0056 — a filesystem port whose read half is `io/fs` *unchanged* (`FS` is a type alias, so `fs.WalkDir` and `fs.Glob` apply with no adapter and there is deliberately no `vfs.Walk`), a write half frozen at four verbs, and publication as an ADR 0039 sibling rather than a fifth method. The ADR's subject is how far "atomic" reaches: a concurrent reader sees the complete old or the complete new content — measured at 0 torn reads out of 600 against 540–597 for the same harness writing in place — and on any failure through the rename the previous bytes are byte-for-byte intact with no temporary surviving, proved by injecting a failure at each of five steps over a genuinely half-written handle and comparing SHA-256 before and after. What it does NOT reach is stated as loudly: nothing across filesystems (`EXDEV` is made unreachable by keeping the temporary in the target's directory, and there is no copy-and-delete fallback because that is not atomic), nothing where `rename` is not the kernel's (FUSE, SMB, FAT), no durability beyond what `fsync` truly does, and no mutual exclusion between publishers since the domain takes no locks. A directory flush that fails AFTER the rename is a different verdict and is deliberately not rolled back, because undoing it means a second non-atomic write to repair a durability problem. Publication costs two device round trips and not the bytes — a 256× larger payload costs 4.7 % more — so batching helps and buffering does not), **secret** (ADR 0096 — a secret is a value no rendering writes down: `secret.Value` writes `<redacted>` from `String`, `GoString`, `Format` (every fmt verb), `MarshalJSON` and `MarshalText`, holds its bytes behind a pointer so fmt's reflection over an UNEXPORTED field prints an address rather than a byte list, does not compile with `==`, and decodes from a JSON string only — a number has been re-spelled before a decoder sees it, and the placeholder coming back in means a dumped configuration was loaded as a real one. A frozen five-method `Store` keeps numbered versions that are never reused, over a closed DNS-label name grammar that maps one-to-one onto an environment variable; three stores — memory, the environment with the `NAME_FILE` convention (both set is REFUSED, not resolved), and a 0700 directory of 0600 records published through `vfs`, serialised through `lock`, and sealed with AES-256-GCM when given a key — a key `KeyFile` creates on first use as exactly 32 raw bytes, published by a hard link the kernel refuses on an existing name, so processes racing on first use all return the one key that won. A `Keyring` turns one secret's versions into keys: boxes and signatures carry their version, the newest seals, every kept one opens, a pruned one is refused; a `Rotator` keeps at least two versions and starts no goroutine), and — the second wave of a framework's mechanisms moving into the SDK — a program's knowledge of itself: `git.Head` for what a working tree is at, and the running process's own `Build` and `Self` (ADR 0100 — a module keeps its release, its commit and its local directory apart, the commit time is read from the raw object because `git log` honours `log.showSignature`, and a CPU time that is only the runtime's GC-refreshed estimate says so), **redact** (ADR 0101: a value, a JSON document, a text or log attributes rendered for DISPLAY with every secret the `Redactor` recognises replaced — a NAME carrying a configured word, a field DECLARED secret by a configurable tag, a URL's credentials — within an EXACT byte bound, because the writer is the package's own and counts every byte before writing it; credentials are scrubbed BEFORE a text is cut, a cut is a prefix, and nothing it is given is mutated), a strict JSON decoder in `codec/strictjson` (ADR 0102: one document read ONE way — `encoding/json/v2` refuses the duplicate name, the case-only match, the unknown member, invalid UTF-8 and trailing data that `encoding/json` silently accepts — within a bound on READING, with refusals that never quote the input and a request-body form that answers 413, 415 or 400), a keyed rate limiter, a public backoff curve and a retry waiting on an injected clock (ADR 0103 — publishing the curve fixed a float-to-duration conversion that made amd64 retries stop waiting at attempt 35), and a `Waker` sibling that lets an idle queue consumer sleep until a publication or a due instant rather than poll (ADR 0104).

**Repository**: `github.com/kitsunium/sdk` · **Module name**: same · **Go**: 1.27.1 (pinned in `MODULE.bazel`)

## Architecture at a glance

```
internal/
├── kernel/        stdlib-only AND generic primitives
│                  batcher, buffer, cache, clock, errs, group, heap,
│                  pathchain, plugin, recycler, ring, singleflight,
│                  snapshot, topic, worker
├── core/          domain interfaces + domain values
│                  authz, cache, cli, codec (+ scratch), config, crypto,
│                  entitlement, events,
│                  gate,
│                  health, i18n, id, lifecycle, lock, logger, logger/level,
│                  mail, metrics, net, proc, queue, resilience, scheduler,
│                  secret, selfupdate, session, sql, token, trace, transform,
│                  validation, vcs,
│                  vfs,
│                  view, writer
└── service/       concrete implementations
                   authz  (RBAC + ABAC + deny-overrides + Check)
                   cache   (tagged memory store + L1/L2 chain)
                   cli    (resolution loop + generated help + config seam)
                   events  (synchronous priority-ordered bus + On[E])
                   lifecycle (ordered engine + per-component stop budget
                              + opt-in signal/sd_notify Run)
                   lock    (in-process leases + file locker over flock(2)
                            or LockFileEx + keepalive)
                   logger (+ encoder, sink/{console,file,memory,syslog},
                             middleware/{async,encwrite,failover,multi,
                                         recover,route,sample,tee})
                   writer (console, dbsink, file, journald, levelgate,
                           nettransport, rotfile)
                   crypto (aesgcm, ecdsasig, ed25519sig, hkdfsha256,
                           hmacsha2, jwk, keyenvelope, keytree, pbkdf2pw,
                           stdhash, streamaead, x25519)
                   codec  (asn1, baseenc, bson, cbor, csv, flatbuffers,
                           form, json, msgpack, multipart, ndjson, pem, tlv, toml,
                           xml, yaml; + strictjson, a decoder and not a Format)
                   queue  (file broker + memory broker + Consume loop
                           waiting on the Waker sibling)
                   proc   (cgroup, childwait, exec, memlimit, reaper,
                           rlimit, sdlisten, sdnotify, self, signal)
                   i18n   (CLDR plural table + catalogue + negotiator + printer)
                   id     (uuidv4, uuidv7, ulid, snowflake, nanoid,
                           ksuid, typeid)
                   net    (tlsid, client, server, sse, websocket)
                   mail   (MIME composition + SMTP + memory double)
                   metrics (in-memory meter + text/prometheus/otlpjson
                             exporters + the OTLP/HTTP emitter)
                   scheduler (cron parser + fixed interval + engine)
                   secret (memory, environment and sealed file stores,
                           keyring over versions, rotator)
                   session (memory store, file store, AEAD cookie sealer)
                   sql    (transaction manager + savepoints + pool policy
                           + migration runner)
                   token  (JWS compact + PASETO v4.public)
                   trace  (tracer + samplers + recorder + otlpjson
                             exporter + the OTLP/HTTP emitter + the
                             server/client HTTP middlewares)
                   transform
                   validation (constraints + combinators + struct-tag plan)
                   view   (html/template engine + trust scan + parse-once)
                   vfs    (os.Root-confined FS + memory FS + atomic publish)
                   vcs    (git changed-set: merge-base + index + worktree
                           + untracked, hardened invocations; + Head)
                   redact (display redaction: names, declared fields, URL
                           credentials, within an exact byte bound)
                   selfupdate (signed release -> verified archive -> atomic
                           replacement, with consent and escalation opt-ins)
                   entitlement (vendor-signed roster -> grant, with an
                           offline cache, an anti-rollback ratchet and a CI seat)
                   gate   (may this invocation run? a policy value and a pure
                           decision; it verifies nothing and exits nothing)
pkg/
└── v1/            stable public API (type aliases + ergonomic helpers)
    ├── cache/     (the ADR 0025 primitive AND the ADR 0049 domain, side by side)
    ├── clock/     (the time port: Clock/Waiter/Timed + System + ManualClock — ADR 0090)
    ├── logger/    (+ ldflags-injected Version, + writer/, + slogbridge/)
    ├── errs/      (construction + introspection: New, Wrap, CodeOf, …)
    ├── events/    (in-process synchronous bus — ADR 0053; NOT a queue)
    ├── codec/     (blank-imports all 16 service codecs + transform;
    │                 + strictjson/ — one document read one way — ADR 0102)
    ├── crypto/    (+ agree, hash, kdf, mac, password, sign)
    ├── id/        (UUIDv4/v7, ULID, snowflake, NanoID, KSUID, TypeID — ADR 0024, ADR 0038)
    ├── lifecycle/ (ordered start, reverse stop, per-component budget — ADR 0050)
    ├── lock/      (Locker/Lease/Deadliner + memory & file lockers — ADR 0052, ADR 0081, ADR 0082, ADR 0083)
    ├── proc/      (+ cgroup, memlimit, process, reaper, rlimit, sdlisten,
    │                 sdnotify, signal; process also reads the process itself
    │                 — Self, Build — ADR 0100)
    ├── metrics/   (the OTel data model, zero OTel imports — ADR 0044)
    └── scheduler/ (Parse/ParseInLocation/Every + the engine — ADR 0041)
    └── server/    (+ sse/ — ADR 0029/0043, + websocket/ — ADR 0047)
    └── secret/    (a Value no rendering writes down, versioned stores, keyring, rotator — ADR 0096)
    └── session/   (memory + file Store, AEAD Sealer, Regenerate — ADR 0045)
    └── token/     (JWT over JWS compact + PASETO v4.public — ADR 0042)
    └── trace/     (W3C Trace Context + the OTel span model — ADR 0051)
    └── validation/ (Constraint / Violation / Report + the struct-tag front end — ADR 0046)
    └── authz/      (RBAC + ABAC, no DSL, abstention is the zero — ADR 0057)
    └── i18n/       (CLDR plurals over a named 13-language subset — ADR 0063)
    └── cli/        (flag + sub-commands + generated help + typed exit — ADR 0065)
    └── mail/       (compose + Transport, injection refused — ADR 0064)
    └── queue/      (durable at-least-once broker, no lock — ADR 0054)
    └── sql/        (ports over database/sql, no driver, no ORM — ADR 0055)
    └── vfs/        (io/fs reading unchanged + atomic publication — ADR 0056)
    └── git/        (what a branch changed; degrades, never empties — ADR 0076)
    └── selfupdate/ (signature THEN digest THEN disk; no key, no install — ADR 0077)
    └── entitlement/ (signed roster -> grant; bring your own Identity — ADR 0079)
    └── gate/        (may this invocation run? decides, performs nothing — ADR 0080)
    └── redact/      (secrets replaced for display, exact bound — ADR 0101)
    └── view/       (html/template, one trust type, parse once — ADR 0058)
third-party/       opt-in vendor integrations (root module only)
                   aws/writer/{cloudwatch,s3}, codec/{hcl,protobuf},
                   entitlement (the ssh Identity + enrolment ONLY; the
                     mechanism is pkg/v1/entitlement — ADR 0079),
                   db/writer/{clickhouse,mysql,redis},
                   transform (zstd + s2 — ADR 0066),
                   x-crypto/{argon2id,xchacha}
```

`baseenc` is NOT a `pkg/v1/codec` subpackage — it is a service codec
(`internal/service/codec/baseenc`) registering nine base-N Formats
(base16/32/45/58/62/64/64url, hex, ascii85) through the same registry as
every other codec.

- Five SDK modules held together by `go.work` — plus two **auxiliary** modules deliberately kept OUT of it: `e2e/` and `tools/genindex/`. Bazel's `go_deps` extension reads `go.work` and cannot process extra modules, so adding either breaks the build; both carry `replace` directives and are built with `GOWORK=off`. Counting `go.mod` files therefore yields seven — that is not drift, it is the invariant. See `e2e/CLAUDE.md` §Do NOT and `tools/CLAUDE.md` §Do NOT. The five workspace modules are: root (umbrella — also hosts the opt-in, vendor-dependent integrations under `third-party/*`, e.g. the AWS writers; see ADR 0012), `internal/kernel`, `internal/core`, `internal/service`, and the public `pkg` (module `github.com/kitsunium/sdk/pkg`, `go.mod` at `pkg/go.mod`; its consumer packages live under `pkg/v1/` and import as `…/pkg/v1/*`, but the *module* is the bare `…/pkg` because Go forbids a `/v1` module-path suffix — ADR 0017). Each module-local `go.mod` carries `replace` directives so `GOWORK=off go build ./...` per-module still works. Heavy vendor deps (AWS SDK) live in the **root** `go.mod` only — nothing requires the root module, so `pkg` consumers stay dep-light.
- Dependency direction is strictly top-down: kernel → core → service → pkg/v1, with `third-party/` above all. It is enforced on the BUILD GRAPH by `scripts/check-layer-deps.sh` (in `make lint` and CI — ADR 0068), not by visibility: ADR 0004 put it on `package_group` + `visibility`, but Gazelle gives every package under `internal/` the visibility `//:__subpackages__`, which admits the whole repository, and a kernel package importing core was shown to build. A rogue import fails `make lint` and CI, naming the target it reached.
- Consumers import only `pkg/v1/*`; `internal/*` is blocked by Go's `internal/` firewall, which is also what keeps them out under Bazel.
- Build / test / lint go through **Bazel 9** — see ADR 0004. `go test ./...` still works locally for quick iteration but CI only runs `bazel`. Because the two build systems disagree about what is in scope, anything excluded from one MUST be covered by the other — see rule 12.

## How to work

| Goal | Flow |
|---|---|
| New feature or bug fix | `/plan "description"` → `/do` → `/git --commit` → `/git --merge` |
| Code review | `/review` |
| Linting | `make lint` (mod-tidy + gazelle drift + gofumpt -l + ktn-linter + alloc-lane coverage + audit coverage + domain-doc drift + `make guard`) |
| Local test suite | `make build && make test` (build prep + race tests) |
| Allocation gates | `make test-alloc` — race-off pass; the ONLY lane that runs `//go:build !race` tests (targets in `tools/alloc-lane-targets.txt`, see rule 12) |
| Single-package test | `bazel test //<path>:<target>` (e.g. `bazel test //internal/kernel/errs:errs_test`) |
| Regenerate BUILD.bazel | `bazel run //:gazelle` after changing imports or `go.mod` |
| Coverage | `bazel coverage --combined_report=lcov //...` — LCOV at `$(bazel info output_path)/_coverage/_coverage_report.dat` |
| Release dry-run | `make release-dry-run` (computes patch bumps locally without pushing tags — see ADR 0007) |
| Regenerate READMEs | `make docs-readme` (regenerates `pkg/v1/{codec,crypto,errs,hash,sign,kdf,password,logger,logger/writer}/README.md` from package doc comments — see ADR 0008) |

Branch naming matches the conventional commit prefix: `feat/*`, `fix/*`, `refactor/*`, `chore/*`, `docs/*`.

## SDK-wide rules (non-negotiable)

1. **Kernel gate.** A kernel package MUST be stdlib-only AND generic (no domain vocabulary). `level` was moved OUT of kernel because it fails the second half — see ADR 0002 / the layer-placement audit in `.claude/contexts/sdk-layer-placement-audit.md`.
2. **Typed errors only.** Every error returned from SDK code goes through `errs.Define` or `errs.Wrap` (`internal/kernel/errs`). `fmt.Errorf` / `errors.New` are banned in production code. The AST audit (`//internal/kernel/errs:errs_test`, run as part of `make test`) fails the build on violations.
3. **Dotted-quad error codes.** `Code` is a `uint32` laid out `MM.LL.PP.SS` (Major / Layer / Package / Serial) — see ADR 0005. Each package owns a `PP` slot; ADR 0005 §Registry + the ADR 0006 extension (logger v2 + ring) are the authoritative allocation table. Two AST audits enforce it: `registry_external_test.go` checks that no two `Define` calls resolve to the same **value**, and `registry_ownership_external_test.go` checks **range ownership** — one package per `MM.LL.PP`, against a hand-maintained `codeRangeOwners` table that is deliberately independent of the constants it audits (ADR 0035). A new range is allocated in that table in the same change that introduces its codes. Both audits only judge files that reach them as runfiles of `//:audit_sources`, so `scripts/pre-commit/check-audit-coverage.sh` fails the build when a package declaring codes is missing from that list — the gap ADR 0020 records having already hidden ~10 emitters, and which had silently reopened for all five `internal/service/net/*` packages. Match codes with `errs.HasCode(err, CodeX)` (walks `Unwrap() error` *and* `Unwrap() []error`) or `errors.Is(err, errs.NewPrefixMatcher(...))` for subnet-style routing.
4. **Public/Private split.** Every SDK error carries a wire-safe `Public` (string literal ≤120 runes, no newline) and a log-only `Private`. `err.Error()` renders `"[<code> <REASON>] <public>"` on the no-trail fast path; when the wrap trail is non-empty, ADR 0005 §Semantics extends the bracket header with `" <- "`-separated trail codes and an optional `" (truncated)"` marker — never Private, never Fields. Log-parser regex: `\[[\d.]+(?: <- [\d.]+)*(?: \(truncated\))? \w+\]`.
5. **No empty stub files / dirs.** If a file or directory only carries a placeholder, inline its content into an existing file or delete it.
6. **Origin wins on wrap.** When `errs.Wrap` receives an `*errs.Error` cause, it inherits the cause's Code/Reason/Public/Private. Wrappers can only add `Fields` (and extend the intrinsic wrap trail). To relabel, define a fresh sentinel.
7. **`Version` via build-time injection.** `pkg/v1/logger.Version` is stamped at link time — under Bazel via `x_defs` + `--stamp` + `tools/workspace_status.sh` (`STABLE_VERSION`); under raw `go build` via `-ldflags "-X github.com/kitsunium/sdk/pkg/v1/logger.Version=…"`. `FrameworkVersion()` returns `"dev"` when unset; every emitted log record carries `framework_version` automatically.
8. **Every package is documented.** Pre-commit guard `scripts/pre-commit/check-pkg-docs.sh` blocks the commit when any `internal/*` or `pkg/v*/**` directory containing Go production code is missing `CLAUDE.md` AND `README.md`. Public packages (`pkg/v*/**`) additionally require `README.md` (consumer-facing — pkg.go.dev renders it; the model is `pkg/v1/errs/README.md`). The `scripts/release/*.{sh,mjs}` and `docs/site/scripts/*.mjs` trees are tooling, not library code, and are exempt from this gate.
9. **Every benchmark package ships its numbers.** Pre-commit guard `scripts/pre-commit/check-bench-md.sh` blocks the commit when a directory contains `*_bench_test.go` but no sibling `BENCH.md`. The report is regenerated with `make bench`; it stamps machine, RAM, CPU, OS, Go toolchain, git SHA, and timestamp so cross-machine deltas can be evaluated honestly.
10. **`pkg/v*/**/README.md` are generated, not hand-authored.** The `gomarkdoc` binary (shipped by the devcontainer Go feature, pinned to `@v1.1.0`) reads each package's Go doc comments and emits `README.md` per package (ADR 0008). Edit the package comment in the existing `.go` file (`codec.go` / `accessors.go` / `logger.go`); run `make docs-readme` to regenerate; `make lint` blocks any commit where the file on disk doesn't match what gomarkdoc would produce now. Maintainer rationale (Why-this-shape, layering, do-not lists) stays in `CLAUDE.md` — consumer-facing prose belongs in the package doc comment.
11. **Docs travel with the code — always update them in the same change.** Documentation is part of the change, never a follow-up. Whenever you add/rename/remove an exported symbol, package, format, code range, capability, or convention, update every doc that describes it **in the same commit**: the package's `CLAUDE.md` (Purpose/Surface/Contents/Sentinels), the parent/layer `CLAUDE.md` tables (e.g. `internal/service/codec/CLAUDE.md` Streaming/Appender columns, `internal/core/CLAUDE.md` registry counts), the root `CLAUDE.md` domain list, and — for public packages — the Go doc comment that `gomarkdoc` renders into `README.md` (rule 10). A doc that names a symbol, count, file, or code that no longer matches the code is a defect: fix the doc or the code, never leave them divergent. When in doubt, grep the docs for the old name/number before committing. Two of this rule's failure modes are now mechanical rather than remembered: `scripts/pre-commit/check-domain-docs.sh` fails the build when this file's architecture tree stops naming exactly the `internal/core` directories, when a domain is described twice in the Purpose paragraph, or when either ADR index drifts from the files in `docs/adr/` — both of which parallel union merges had actually produced here, along with two whole Purpose paragraphs coexisting while `events` was documented only in the stale one and `health` in neither.
12. **Every test excluded from normal discovery needs a named, executable, currently-green gate — or an explicit declaration that it must not run.** Exclusion mechanisms compound silently: `//go:build !race` hides a file from the race suite (race is on by default, see `.bazelrc`), `gazelle:excluded` + `manual` + `-test.run=^$` hides a target from `bazel test //...`, and a `.ktn-linter.yaml` entry hides it from the linter. Each exclusion is individually justified and documented; *together* they have already produced tests that nothing ever ran — including `pkg/v1/logger`'s `TestV116BuildSendAllocatesOnePerEmit`, the regression guard for the allocation claim in this very file. Gates in force today: the race-off alloc lane (`tools/alloc-lane-targets.txt`, mechanically enforced by `scripts/pre-commit/check-alloc-lane-coverage.sh`, wired into `make lint` and CI) covers every `//go:build !race` test, and CI's `test-386` job compiles and runs those same files a second time on linux/386, where `-race` does not exist; `TestGenerateBenchMD` is opt-in by exact `-test.run` name under BOTH build systems; `integration` / `localstack` tagged tests declare their run procedure in their package `CLAUDE.md`. When you add an exclusion, name its compensating lane in the same commit — and remember that a lane which exists but has been failing for weeks verifies nothing.

After cloning, wire the in-repo hooks with `bash scripts/install-hooks.sh` (one-time per clone — sets `git config core.hooksPath .githooks`).

## Layout

```
/workspace/
├── internal/              see internal/CLAUDE.md
├── pkg/v1/                see pkg/CLAUDE.md + pkg/v1/CLAUDE.md
├── third-party/           opt-in vendor integrations in the root module (AWS writers — ADR 0012)
├── docs/                  ADRs — see docs/CLAUDE.md
├── .devcontainer/         devcontainer infrastructure (template-seeded; leave alone)
├── .github/               CI workflows — bazel-ci.yml is the SDK lane
├── go.work, go.mod        workspace + umbrella module (read by Bazel via from_file)
├── MODULE.bazel           Bzlmod entry point (rules_go 0.60.0 + gazelle 0.50.0 + go_sdk 1.27.1 + go_deps)
├── BUILD.bazel            root gazelle target + audit_sources filegroup
├── .bazelrc               race-on by default; named configs: race / pure / ci / alloc
├── .bazelversion          pins Bazel to 9.0.2
├── Makefile               build / test / test-alloc / lint / bench / cover / docs / serve / release-dry-run / docs-readme … (run `make` for the full list)
├── e2e/                   real-kernel conformance harness — auxiliary module, OUTSIDE go.work (GOWORK=off)
├── tools/sdkguard/        consumer-facing rule enforcement (stdlib-only CLI — ADR 0033)
├── tools/workspace_status.sh  prints STABLE_VERSION (consumed by --stamp + x_defs)
├── tools/alloc-lane-targets.txt  target list for the race-off alloc lane (rule 12)
├── tools/genindex/        docs-site symbol index — auxiliary module, OUTSIDE go.work (GOWORK=off)
├── .golangci.yml          code-quality second-opinion linters (layer firewall is now Bazel visibility)
├── AGENTS.md, agent.toml  devcontainer agent specs (not SDK)
└── README.md              the SDK quickstart (packages, install, verification)
                           — per-package docs live next to their code
```

## Verification

| Command | Expected |
|---|---|
| `ktn-linter lint ./...` | No issues found |
| `bazel build //...` | all targets pass |
| `bazel test --config=race //...` | every `*_test` target green (race on by default — see `.bazelrc`) |
| `bazel coverage --combined_report=lcov //...` | LCOV at `$(bazel info output_path)/_coverage/_coverage_report.dat` |
| `bazel query 'kind("go_library", deps(//internal/kernel/...)) except //internal/kernel/...'` | empty — kernel has zero outgoing go_library edges |
| `bash scripts/check-layer-deps.sh` | exit 0 — no layer reaches one above it: kernel → core → service → pkg/v1, `third-party/` above all (ADR 0068; visibility alone does not bind `internal/` packages) |
| `make build` | `bazel mod tidy` + `bazel run //:gazelle` + `gofumpt -l -w` + `bazel build //...` |
| `make test` | every `*_test` target green incl. `//internal/kernel/errs:errs_test` (AST audit) |
| `make test-alloc` | race-off allocation gates green — every target in `tools/alloc-lane-targets.txt`, the only lane running `//go:build !race` tests |
| `bash scripts/pre-commit/check-alloc-lane-coverage.sh` | exit 0 — no `!race` test sits outside `tools/alloc-lane-targets.txt` (rule 12) |
| `bash scripts/pre-commit/check-domain-docs.sh` | exit 0 — the architecture tree still names exactly the `internal/core` directories, no domain is described twice, and both ADR indexes name exactly the ADRs on disk, once each (rule 11) |
| `bash scripts/pre-commit/check-audit-coverage.sh` | exit 0 — no package declaring an `errs.Define`/`errs.Code` sits outside `//:audit_sources` (rule 3) |
| `GOWORK=off GOARCH=386 CGO_ENABLED=0 go test ./...` (per module) | green — the 32-bit RUNTIME bar, run by CI's `test-386` job. `cross-build` proves the SDK compiles on 386; this proves it behaves, which is where a `int(0xffffffff)` read as `-1` shows up |
| `cd pkg && go test ./...` | green; `pkg/v1/codec` completes in seconds — `TestGenerateBenchMD` self-skips unless named via `-run` |
| `make lint` | drift assertion (read-only): mod tidy + gazelle diff + gofumpt -l + ktn-linter + alloc-lane coverage + `make guard` |
| `make guard` | `tools/sdkguard` over the SDK's own tree at invariant level (ADR 0033); no network — `-version-check=off` |

## Reference

- ADR 0001 — multi-module layout — `docs/adr/0001-sdk-go-multimodule-layout.md`
- ADR 0002 — layered `errs` package — `docs/adr/0002-sdk-errors-package.md` (Registry section superseded by ADR 0005)
- ADR 0003 — universal codec package — `docs/adr/0003-sdk-codec-package.md`
- ADR 0004 — Bazel 9 build system (its layer-firewall mechanism amended by ADR 0068) — `docs/adr/0004-sdk-bazel-build-system.md`
- ADR 0005 — dotted-quad error codes + wrap trail — `docs/adr/0005-sdk-error-codes-dotted-quad.md`
- ADR 0006 — error code registry extension (logger v2 + ring) — `docs/adr/0006-sdk-error-code-registry-extension.md`
- ADR 0007 — release workflow + docs versioning — `docs/adr/0007-sdk-release-and-versioning.md`
- ADR 0008 — README generation from Go doc comments — `docs/adr/0008-readme-from-code-generation.md`
- ADR 0009 — public module `go get`-resolvability — `docs/adr/0009-pkg-public-module-resolvability.md`
- ADR 0010 — kernel object-recycling primitive — `docs/adr/0010-kernel-recycler-primitive.md`
- ADR 0011 — kernel copy-on-write snapshot primitive — `docs/adr/0011-kernel-snapshot-primitive.md`
- ADR 0012 — logger writer registry (named, config-driven Sink factories) — `docs/adr/0012-logger-writer-registry.md`
- ADR 0013 — crypto domain (AEAD Seal/Open, hidden nonce; AES-256-GCM default) — `docs/adr/0013-sdk-crypto-domain.md`
- ADR 0014 — the verb wave (core/transform 5th sibling; crypto MAC/Agreement/StreamSealer ports + KeyEnvelope/KeyTree; logger.FromConfig + ConfigDecoder; single-MR sequencing) — `docs/adr/0014-sdk-transform-crypto-ports-config-topology.md`
- ADR 0015 — writer taxonomy + depTier as a first-class property; default writers console+file; rotation off-by-default → compress + 24h archive + 7-day retention; dep-light DB/transport gate (WI-9..WI-11) — `docs/adr/0015-sdk-logger-writer-taxonomy-and-rotation.md`
- ADR 0016 — OS process-supervision domain (`proc`): 6th core sibling; `process`/`signal`/`reaper`/`rlimit`/`cgroup`/`sdnotify` facades; central error block `0.2.6.*`; build-tag platform selection, no registry — `docs/adr/0016-sdk-process-supervision-domain.md`
- ADR 0017 — public module is the bare `…/pkg` (Go forbids the `/v1` module-path suffix); code stays under `pkg/v1/` so imports are unchanged; tag shape `pkg/vX.Y.Z`, first release `v0.1.0` alpha — `docs/adr/0017-pkg-bare-module-path.md`
- ADR 0018 — cross-platform portability strategy: build bar (compiles on all 8 GOOS) + runtime bar (correct on the real kernel); uniform typed `UnsupportedPlatform` where no native mechanic exists; `_linux`/`_unix`/`_bsd`/`_windows`/`_other` split convention; `cross-platform.yml` (build) + `e2e-vm.yml` (real-kernel) gates; OpenBSD `RLIMIT_AS` precedent; native-backend roadmap (FreeBSD `rctl`, Windows Job Objects, BSD `procctl`) — `docs/adr/0018-sdk-cross-platform-portability.md`
- ADR 0019 — public error construction API (`pkg/v1/errs.New`/`Wrap`/`Field` helpers via non-panicking `kernel/errs.NewRuntime`) + third-party Major-byte reservation `0x40–0x7F` (`MinAppMajor`/`MaxMajor`); reverses the `errs` "no constructors" rule so downstreams adopt the error model wholesale — `docs/adr/0019-pkg-errs-public-construction.md`
- ADR 0020 — errs AST audit accepts two Reason derivations (`screamingSnake(varName)` OR `screamingSnake(CodeConst − "Code")`); closes the `//:audit_sources` coverage gap that silently excluded ~10 namespaced emitters (ring + every logger middleware/sink) — `docs/adr/0020-errs-audit-dual-reason-derivation.md`
- ADR 0021 — BSON codec (M5) over `go.mongodb.org/mongo-driver/bson` in `internal/service/codec/bson` (library-backed codec precedent, not third-party quarantine); non-streaming + Appender; error block `0.3.36.*` — `docs/adr/0021-sdk-codec-bson.md`
- ADR 0022 — HCL codec (M5) quarantined in `third-party/codec/hcl` (root module) because `hcl/v2` (via `x/tools`) would **introduce** `x/sys` — banned SDK-wide — into `internal/service`; the ADR's original "downgrades `x/sys`" wording is corrected by ADR 0034; opt-in, not in `pkg/v1/codec`; first `third-party/codec/` subtree; error block `0.3.37.*` — `docs/adr/0022-sdk-codec-hcl.md`
- ADR 0023 — schema codecs (M6): opt-in under `third-party/codec/` (schema-bound, can't honour the universal round-trip contract); Protobuf concrete (`0.3.38.*`), Avro/Cap'n Proto deferred — `docs/adr/0023-sdk-schema-codecs.md`
- ADR 0024 — identifier-generation domain (`id`): 7th core sibling, `Generator`/`Scheme` registry (UUIDv4/v7, ULID, snowflake), canonical-string output, stdlib-only/cross-OS; opens the Phase-B new-domain wave; error block `0.2.7.*`/`0.3.39.*`. Extended in place — NanoID, KSUID and TypeID ship in the same shape and the same `0.3.39.*` block; TypeID is constructor-only (no registered default prefix — ADR 0038, applying ADR 0031) — `docs/adr/0024-sdk-id-domain.md`
- ADR 0025 — generic LRU+TTL `Cache[K,V]` as a **kernel** primitive (`internal/kernel/cache`): domain-neutral, stdlib-only, reuses `clock` for testable TTL; `Fetch` (not `Get`) since a hit mutates LRU; no error codes; `pkg/v1/cache` aliases — `docs/adr/0025-sdk-cache-kernel.md`
- ADR 0026 — reliability domain (`resilience`): 8th core sibling, **no registry** (concrete composable `Runner` policies — retry/circuit-breaker/rate-limit/bulkhead/timeout, later + fallback/hedging); error block `0.2.8.*` — `docs/adr/0026-sdk-resilience-domain.md`
- ADR 0027 — observability domain (`metrics`): 9th core sibling, Counter/Gauge/Histogram + `Meter` + `Exporter` registry (writer-registry model); in-memory meter + the stdlib `text` and `prometheus` exporters; error block `0.2.9.*` (+ `0.3.45.*` for the names the Prometheus wire format refuses); **labels shipped in place** — name + label set = one series, bounded per name with a visible aggregated overflow series (`0.2.9.4`, since renamed INVALID_ATTRIBUTE); **the Prometheus text exposition exporter shipped in place too** (stdlib-only, no client library). **OTLP/JSON shipped in ADR 0048**, in-tree and stdlib-only for the same reason; the Prometheus protobuf format and OTLP/protobuf stay deferred. **Re-shaped by ADR 0044**: the domain now speaks the OpenTelemetry metrics data model, implemented from the specification with zero OTel imports. — `docs/adr/0027-sdk-metrics-domain.md`
- ADR 0028 — configuration domain (`config`): 10th core sibling, `Source`/`Validator`/`Watcher` ports; env+file layering + JSON round-trip decode + cross-OS poll watcher; closes the Phase-B wave; error block `0.2.10.*` — `docs/adr/0028-sdk-config-domain.md`
- ADR 0029 — network domain (`net`): 11th core sibling covering inbound AND outbound over one substrate (TLS/mTLS identity, per-phase deadlines, policy, metrics); goroutine-per-connection on the runtime netpoller (not an event loop); `net/http` adapted, not reimplemented; `recvmmsg` + `SO_REUSEPORT` via raw stdlib `syscall` because `x/net` pulls in the banned `x/sys`; **no registry**; error block `0.2.11.*`. Extended in place by **Server-Sent Events, server side** (`internal/service/net/sse`, `pkg/v1/server/sse`, `0.2.11.23`–`0.2.11.26`): a newline is not escaped but SPLIT across `data:` lines, so a terminator in `id`/`event` is refused rather than truncated; `Last-Event-ID` is handed to the handler and **never replayed** by the SDK. It also amends the drain: `http.Server.Shutdown` waits for in-flight requests and a stream that never ends never becomes idle, so an open stream burned the whole budget and was then killed by socket severance. The engine now publishes a drain SIGNAL on the request context (`server.DrainSignal`) — additive, never a cancellation, because cancelling would tell every handler to abandon the response the drain exists to let it finish — `docs/adr/0029-sdk-net-domain.md`
- ADR 0030 — stdout is a protocol channel: no SDK default writes to `os.Stdout` (the registered `text` metrics exporter targets stderr; `ConsoleStderr` becomes the `ConsoleStream` zero value). A zero value is the choice made by someone who has not yet learned the question exists — so it must not be the dangerous one — `docs/adr/0030-stdout-is-a-protocol-channel.md`
- ADR 0031 — a policy's zero value is a safe default or an explicit refusal, never an inert policy: **clamp** where a working default needs no explanation, **refuse** where any SDK-chosen value would be arbitrary (`PolicyMisconfigured` `0.2.8.6`) — `docs/adr/0031-policy-zero-values-are-never-inert.md`
- ADR 0032 — `log/slog` is an adapter at the public edge, not a domain dependency: permitted in `pkg/v1/logger/slogbridge` ONLY, so a consumer facing a concrete `*slog.Logger` parameter stops building a second pipeline (two thresholds, two formats, half-stamped records); core keeps its "never log/slog" rule; error block `1.1.1.*` — `docs/adr/0032-logger-slog-bridge.md`
- ADR 0033 — SDK rules are enforced on consumers at BUILD time by `tools/sdkguard`, a stdlib-only CLI: runtime detection was measured impossible (`log/slog` is not a module, a local `slog.Logger` never touches `slog.Default`), and a vettool would need `x/tools`, which `tools/*` cannot take without breaking its Bazel build; rules are split invariant/convention so adoption is incremental — `docs/adr/0033-consumer-rule-enforcement.md`
- ADR 0034 — the HCL quarantine is right, its stated mechanism is not: under MVS a dependency requiring a *lower* version cannot downgrade a higher requirement; measured, adding `hcl/v2` to `internal/service` **introduces** `x/sys` (at `v0.20.0`, via `x/tools`) into a module that **bans** it. Decision unchanged, downgrade narrative retired as a placement rule — `docs/adr/0034-hcl-quarantine-rationale-corrected.md`
- ADR 0035 — PP-range ownership is enforced by an audit the audited code cannot edit: value-uniqueness never saw two packages sharing a `PP` with different `SS`; ownership is keyed on Code *declarations* (not `Define` calls, which miss `core/codec`'s `0.2.2.*`) against a hand-written table, never one generated from the constants — `docs/adr/0035-pp-range-ownership-enforcement.md`
- ADR 0036 — form-urlencoded codec: repetition is the only array syntax (`a=1&a=2` → `["1","2"]`); the `a[]=`/`a[0]=`/comma dialects are framework inventions the format does not define, and last-wins discards a value the wire carried while reporting success; non-bijectivity is stated — the **second** round-trip is the byte-level fixpoint — `docs/adr/0036-sdk-codec-form-urlencoded.md`
- ADR 0037 — multipart/form-data codec: the delimiter lives in the `Content-Type` header, which `Marshal(v any) ([]byte, error)` cannot carry. Rather than widen the contract for one format, the codec domain grows **extension interfaces** (`BoundaryCodec`, `BoundaryProvider`) discovered by type assertion, as `Appender` already is — `docs/adr/0037-sdk-codec-multipart.md`
- ADR 0038 — `id` gains NanoID, KSUID and TypeID, and the `Scheme` registry stops being exhaustive: TypeID is **not** registered because a prefix names the caller's entity, so a singleton would either invent that vocabulary or mint a prefixless identifier — `docs/adr/0038-id-schemes-and-the-unregistered-typeid.md`
- ADR 0039 — a published port is extended by a **sibling interface**, never by widening: `pkg/v1/cache.Config` is a type alias that carries `clock.Clock` into the released module, and Go interfaces are structural, so adding a method breaks every downstream two-method double at compile time with no deprecation window. `Clock` stays frozen, `Waiter` is new, `Timed` is the union, and `System` widens as a *value* — which is safe. Applies to every port an alias publishes — `docs/adr/0039-extending-a-published-port-without-breaking-it.md`
- ADR 0040 — the concrete-type half of ADR 0039: a published **shape** has no sibling trick, so `SnapshotValue` changing form reached every consumer through the `pkg/v1/metrics.Snapshot` alias. Permitted **only because the module is v0** — stated out loud in the commit, and gone at v1. One rule for both halves: an interface gets a sibling at any version; a concrete shape may change while v0, loudly, and not after — `docs/adr/0040-changing-a-published-shape-while-v0.md`
- ADR 0041 — time-driven execution domain (`scheduler`): 12th core sibling, **no registry**; `Job`/`Schedule` are FUNC ports so a published port cannot grow a method at all (ADR 0039 applied structurally); five-field POSIX cron with the day-field OR rule, everything outside the subset refused BY NAME (six/seven fields, `@reboot`, `@every`, Quartz `L`/`W`/`#`/`?`, 7-for-Sunday, and an expression matching no date); DST decided — spring-forward skips, fall-back fires once; missed deadlines skipped **and counted**, never replayed; overlap skipped by default with the assertion `AllowOverlap` opting in; the engine waits through `clock.Timed` and a named AST test fails the build on any wall-clock wait, in production code and in the suite; error blocks `0.2.12.*` (port) / `0.3.43.*` (parser) — `docs/adr/0041-sdk-scheduler-domain.md`
- ADR 0042 — security-token domain (`token`): 13th core sibling, `Issuer`/`Verifier` ports, JWT over JWS Compact Serialization + PASETO v4.public; **no registry** because its key would be the attacker-written `alg` header. Algorithm confusion is made *unwritable* — one constructor per algorithm, each taking only the key type it can use, so handing an EC public key to the HMAC path does not compile — and the header is then still compared against the binding before any key reaches a primitive. `alg:none` has no representation in the `Algorithm` enum. Bounds are checked before the work they fund (the CVE-2025-30204 class). `exp` is required by default on both sides. Error blocks `0.2.13.*`/`0.3.44.*`; `v4.local` refused by name because XChaCha20+BLAKE2b live in `third-party/` — `docs/adr/0042-sdk-token-domain.md`
- ADR 0043 — draining is **announced** to the handler, not imposed. Adding SSE exposed a defect that predated it: an open stream held `Shutdown` for the caller's entire budget and returned `DRAIN_TIMEOUT`, because `http.Server.Shutdown` waits for in-flight requests and one that never ends never becomes idle — the normal outcome of every deployment, covered by no test. The adapter now closes a **signal channel** carried on the request context; deliberately **not** a cancellation, which would tell every well-behaved handler to abandon the response the drain exists to let it finish — `docs/adr/0043-drain-is-a-signal-not-a-cancellation.md`
- ADR 0044 — the `metrics` domain adopts the OpenTelemetry metrics DATA MODEL and **imports none of its code**: OTel is a published specification, so it is implemented from the document exactly as the Prometheus exposition format and RFC 7517 are, and the SDK's dependency budget stays zero. Typed attributes replace string labels and the attribute KIND joins the series identity (`String("v","1")` and `Int64("v",1)` are two series); aggregation **temporality** becomes real rather than implicit — a delta `Collect` CONSUMES the window it reports and advances the start, so a delta meter has exactly one reader; `Resource` and `InstrumentationScope` are carried once per payload; `UpDownCounter` and the three observable families land on the sibling ports `UpDownMeter`/`AsyncMeter` (ADR 0039) with monotonicity as a FIELD of the sum rather than a second point type. The published `pkg/v1` shapes change under the ADR 0040 v0 licence, named out loud. The Prometheus exporter is kept and requalified as a deliberately **lossy connector**: a delta snapshot is REFUSED, every attribute flattens to a string, Resource/Scope are dropped, and an OTel-conventional dotted key is refused rather than mangled. Exemplars, exponential histograms and array attributes are deferred by name; error codes `0.2.9.6` / `0.3.45.4` — `docs/adr/0044-metrics-adopts-the-otel-data-model.md`
- ADR 0045 — server-side session domain (`session`): 14th core sibling, **no registry**; the stateful counterpart to `token`, and the difference is revocation — a token ends only when it expires, a session ends when `Destroy` says so. Session fixation is prevented by ABSENCE: `Regenerate` is the only call that binds a subject and it always mints a new identifier, so "keep this identifier and log this user in" is a sentence with no method — guarded by reflection over the port, not by a comment. The identifier is a 256-bit secret whose type redacts itself, compares in constant time, and is never a map key, a filename or at rest (stores index by its SHA-256). Expiry is absolute AND sliding with the **earlier deadline always winning**, clamped twice — in the store and in the value type — and a rotation buys no extra time unless the principal changed. The file store seals every record, binds it to its own filename, narrows **and then stats** its permissions, publishes by `rename(2)`, and serialises under one `flock`; where those mechanics do not exist it returns `UnsupportedPlatform` at construction rather than pretending (ADR 0018). Error blocks `0.2.14.*` (port) / `0.3.46.*` (stores) — `docs/adr/0045-sdk-session-domain.md`
- ADR 0046 — value-validation domain (`validation`): 15th core sibling, **no registry**; `Constraint[T]` is a FUNC port so a published port cannot grow a method at all (ADR 0039 satisfied structurally). A violation says **where** — one path grammar, `user.addresses[2].zip`, with the member named by its `json` tag because `config.Load` decodes every format through a JSON round trip. Collect-all is the default and stopping is a real short-circuit, never a truncated report. A violation is **not** an error and neither is the report — a value type implementing `error` makes `if err != nil` true for a PASSING validation — so `Report.Err()` converts on demand and returns a genuine nil. A message names the rule and the bound and **never the value**, which is a security property with its own test. Both front ends ship: the programmatic one uses no reflection, the struct-tag one caches a compiled plan per type (measured: 74× over recompiling, 31 % over the reflection-free path). `email`, `url`, `uuid` and a tag-borne regexp are refused BY NAME; a validator with no constraint passes, a constraint that cannot be honoured is refused at construction (ADR 0031's two sides). Blocks `0.2.15.*` / `0.3.47.*` — `docs/adr/0046-sdk-validation-domain.md`
- ADR 0047 — WebSocket (RFC 6455), server side, in the stdlib — no `gorilla/websocket`: the format lives in `internal/core/net`, the connection in `internal/service/net/websocket`, the façade in `pkg/v1/server/websocket`. Every MUST-fail is enumerated with its section and its close code and provoked by hand-assembled bytes (unmasked client frame — a security requirement, not ceremony; reserved bits and opcodes; fragmented or oversized control frames; non-minimal lengths; orphan continuations; interrupted fragmentation; unsendable close codes; invalid UTF-8). UTF-8 is judged on the REASSEMBLED message, because a rune may straddle a fragment and a per-frame check would reject valid input. Ceilings are checked against the length the PEER announced, before any allocation, and there is deliberately no unbounded setting. permessage-deflate is refused twice on the wire — no `Sec-WebSocket-Extensions` header, and any RSV bit fails the frame. The origin is checked by default, because the browser's same-origin policy does not apply to WebSocket. It also fixes a defect that predated it: the engine closed every hijacked socket the instant `net/http` reported the hijack, so no upgrade-based protocol could work at all — a hijacked connection is now the handler's, and is not waited for by the drain, exactly as `net/http`'s own `Shutdown` carves out. Error block `0.2.11.27`–`0.2.11.33`; no client, because the outbound contract returns a fully-read body and never a socket — `docs/adr/0047-sdk-net-websocket.md`
- ADR 0048 — **OTLP/JSON**: the wire the OTel data model was adopted for, written from the specification with `encoding/json` and `net/http` and **zero** `go.opentelemetry.io` imports and no protobuf runtime. Two surfaces, and the boundary is the byte slice — `EncodeOTLPJSON` is a pure snapshot→bytes function that touches no socket, `NewOTLPHTTPExporter` adds only transport — so an encoding defect and a network defect are never the same investigation. The four protobuf-JSON rules that are invisible until a receiver silently drops the payload are cited and pinned: lowerCamelCase field names, 64-bit integers as **decimal strings** (a JSON number is a double, so a counter past 2⁵³ would lose its low bits), enums as **integers** — OTLP overriding generic proto3 JSON, which uses names — and unknown fields ignored. Three fields are emitted at their zero because presence IS the meaning: the `asInt`/`asDouble` oneof (omitted, a data point has no value at all), the `optional` histogram `sum`, and `isMonotonic`, which would otherwise disappear precisely when it carries the surprising answer. **Delta temporality travels** — the whole difference from Prometheus — while `UNSPECIFIED` is REFUSED because the schema says it MUST NOT be used, and so is a bucket ladder that is not one count more than strictly-increasing finite bounds. The POST is a connector: bounded response read, no redirects (CWE-918), endpoint refused at construction rather than 404-ing forever, and **no retry** — it classifies (`OTLPRetryable`, exactly `resilience.RetryConfig.Retryable`'s shape) so a caller owns the backoff instead of a hidden one. `otlpjson` registers on stderr; the HTTP emitter is never registered, because arming a network client from an import is worse than the stdout hazard ADR 0030 already refuses. The conformance test compares against bytes written BY HAND from the `.proto`, since a test that decodes its own output preserves exactly the bug it should catch. Blocks `0.3.45.5`–`0.3.45.10` — `docs/adr/0048-sdk-metrics-otlp-json.md`
- ADR 0049 — `cache` becomes a domain, and gains the kernel primitive it needed to: the ADR 0025 LRU+TTL primitive **does not move**; a domain is added above it (`internal/core/cache` `0.2.18.*`, `internal/service/cache` `0.3.48.*`) carrying the three things a primitive deliberately lacks — invalidation by tag, stampede protection, L1/L2 chaining. `Store[V]` is FROZEN at three methods with `EntryFetcher`/`Tagger`/`Loader` as ADR 0039 siblings, guarded by a named three-method-double test; no published shape changed, so the ADR 0040 v0 licence is not used — said out loud rather than left silent. **No registry**, and the mechanics decide it: `Store` is generic in `V`, and a `map[Name]Store[V]` for an open `V` would have to erase the very type the port exists to keep. Tag invalidation is indexed both ways and MEASURED — 100 entries out of 1 000 costs 31.2 µs, out of 100 000 costs 36.2 µs, so a 100× store costs 1.16× and the rest is locality, not work; the reverse index is unlinked on four paths that share no mechanism, because the primitive deliberately does not fire `OnEvict` for an explicit `Delete`. One mutex covers the primitive and the index, `Fetch` takes it exclusively (a hit mutates — the reason ADR 0025 named it `Fetch`), and `onEvict` takes no lock because it runs on the caller's goroutine inside a held one. The chain refuses at construction any tier that cannot return its entry, since promotion without tags is a copy `InvalidateTag` can never reach; its `Delete` goes far→near because near-first would let the next read promote the value back and undo the delete permanently. **Stampede protection stops at the process boundary** and says so four times. Backed by the new kernel primitive `singleflight`: `fn` runs on its own goroutine under `WithCancel(WithoutCancel(leaderCtx))`, so an abandoning caller gets its own `ctx.Err()` while the call survives for the others and is cancelled only when the last leaves; a panic is captured with the ORIGINATING stack and re-raised in every waiter; ≈2 µs per leading call, measured, and turned into a rule about when not to use it. Its claimed second consumer (`config`) was checked and does not exist — recorded with two named candidates deferred — `docs/adr/0049-cache-becomes-a-domain.md`
- ADR 0050 — ordered start/stop domain (`lifecycle`): the SDK had every piece of a service's startup and no assembly of them, and **two lifecycle defects found in this repository are the argument** — ADR 0043 (one endless stream spent the whole drain budget, so `DRAIN_TIMEOUT` was the normal outcome of every deployment) and ADR 0047 §D9 (the engine closed the socket it had just handed to the handler, so no hijack worked at all). Both are cleanup that ran, looked reasonable in isolation, and destroyed something still needed. The Add order IS the dependency order and shutdown is its exact reverse — **no graph**, because a linear sequence already is a topological order and the caller who wrote the constructors is the only party who knows it; **no autowiring**, because a wiring convention is a framework. `Start`/`Stop` are FUNC ports (ADR 0039 satisfied structurally). A partial start is unwound before `Start` returns, through the **same** `stopEach` an ordinary `Stop` uses, over a list appended to before the next `Start` is attempted, with contexts detached by `WithoutCancel` so a start aborted by cancellation still gets a real cleanup — and the component that FAILED is never stopped, because a `Start` that fails owns what it acquired. The shutdown budget is **per component**, not shared: expiry cancels that `Stop`'s context (an announcement), stops waiting, records `StopTimeout`, and continues — it kills no goroutine and closes nothing the component owns, so `Stop` returns in at most n × StopTimeout and nothing that could finish is cut short. A non-positive budget CLAMPS to 30s (ADR 0031). Signals and `sd_notify` are opt-in `RunConfig` fields delegating to `service/proc/{signal,sdnotify}`. Errors aggregate with `errors.Join` so `errs.HasCode` and the caller's `errors.Is` both answer. **No registry**; blocks `0.2.19.*` / `0.3.49.*`. Nothing in the package waits on the wall clock — a named AST test fails the build on any `time` wait, in production code and in the suite — `docs/adr/0050-sdk-lifecycle-domain.md`
- ADR 0051 — distributed-tracing domain (`trace`): 18th core sibling, observability's THIRD pillar, on the OpenTelemetry trace DATA MODEL and W3C Trace Context — both implemented from their documents with zero `go.opentelemetry.io` imports, because interoperability is a property of the WIRE and the official SDK would introduce the banned `x/sys`. The sampling decision is taken ONCE at the root and travels in the `sampled` bit (a per-span decision makes orphans a backend renders as separate roots), so an unsampled span is a no-op that still CARRIES a context — without it one dropped trace becomes N kept ones downstream. `Extract` returns no error: W3C §4.3 prescribes exactly one response to a malformed header a stranger wrote, and an error would invite failing the request; the parent's refusals (all-zero ids, version `ff`, uppercase hex, an undashed tail on a higher version) are each cited by section, and undefined flag bits are masked on OUTPUT but preserved on input, which is the only combination §3.2.2.5.2 and §3.2.4 both permit. `Ratio(0)` is REFUSED because 0 also spells "unconfigured" and the symptom of guessing is the ABSENCE of telemetry (ADR 0031). The attribute, `Resource` and `Scope` model is REUSED from `core/metrics` rather than twinned — they are OTel's shared `common.proto`/`resource.proto`, so `trace.Attr` IS `metrics.Attr` and the exemplars ADR 0044 deferred have somewhere to put a trace id; `DefaultScopeName` is the one thing deliberately not shared. OTLP/JSON on `/v1/traces` follows ADR 0048's template exactly, with a fifth rule of its own: trace and span identifiers are **hex, not base64** — the one place OTLP overrides the protobuf-JSON mapping, and the one a generated encoder gets wrong silently. `flags` is `fixed32`, so it is a JSON NUMBER, and it is always emitted; `status` is OMITTED when unset, which is the opposite call from metrics' three always-emitted fields and for a stated reason. The OTLP/HTTP emitter is never registered, for a reason one step beyond ADR 0030: no endpoint could be a correct default. `ServerMiddleware`/`ClientMiddleware` are the net domain's OWN middleware type instantiated at `http.Handler`/`http.RoundTripper`, and the status wrapper implements `Unwrap` and nothing else so it cannot LIE about Flush/Hijack — the ADR 0047 defect, re-introduced one layer up. Blocks `0.2.20.*` / `0.3.50.*` — `docs/adr/0051-sdk-trace-domain.md`
- ADR 0052 — mutual-exclusion domain (`lock`): 19th core sibling, **no registry**; `Locker` frozen at two methods and `Lease` at three, and **neither takes a TTL** — a lease lifetime belongs to the work being protected, is configured where the locker is wired, and therefore cannot be typed as a zero at a call site. A non-positive TTL is **refused at construction**, because its two natural readings ("already expired", "never expires") are opposites and the first grants every `Acquire` while excluding nobody — indistinguishable, from outside, from a lock nobody contends (ADR 0031's refuse half; `FileConfig.Poll` is the clamp half, in the same package for contrast). The ADR's subject is what expiry forces, decided rather than left emergent: **ownership** is token-checked, so a lapsed holder's `Release` releases NOTHING and reports `LOCK_NOT_HELD` — unlocking by name would end the new holder's section and the new holder would never learn of it; **renewal** is `Extend`, refused on a lapsed lease even when nobody has taken the lock (expiry is the deadline, not "until someone else takes it"), plus a `Keepalive` that CANCELS a derived context rather than returning an error, because the caller who needs the news is already inside the section — the exact inverse of ADR 0043's drain signal; **fencing** ships, a strictly-increasing per-name `uint64`, `fsync`ed into the lock file for the file backend so it survives a reboot, and a corrupt ledger is refused rather than reset since a restarted fence reissues numbers the resource already accepted. **What is NOT guaranteed is stated in four places**: the SDK can only ISSUE a fence, so where the protected resource cannot compare it, mutual exclusion is not guaranteed against a GC pause or a `SIGSTOP`. The two backends expire differently on purpose — a dead process is noticed by the KERNEL, a dead goroutine by nobody — so the memory lease expires and implements `Deadliner` while the file lease never expires and deliberately does not, the absence being how a caller discovers which world it is in (ADR 0039). `flock(2)` was **measured** before use: two open file descriptions in one process do exclude, but the same description re-locked is a lock *conversion* that returns at once, and eight goroutines sharing one descriptor were all inside one counted section, every run — so the file locker takes an in-process gate first, never calls a blocking `flock`, and refuses a world-writable non-sticky directory where unlinking the lock file hands the next process a different inode. Windows refused rather than approximated. Blocks `0.2.21.*` / `0.3.51.*` — `docs/adr/0052-sdk-lock-domain.md`
- ADR 0053 — in-process event bus domain (`events`), and **the line between it and the `queue` domain that comes next**, drawn before either is written because the two are described with the same words and guarantee opposite things: `events` is one process, synchronous, the publisher's goroutine and the publisher's transaction, with no durability, no retry and no dead-letter path — if the process dies mid-dispatch the event never happened; `queue` is the other column. A caller who wants "asynchronous but reliable" is sent to `queue` rather than served the asynchrony without the reliability, so there is deliberately **no async mode, not even as a config field**. The routing key is the event's concrete Go TYPE, never a name — the compiler mints it, two packages cannot both spell `"user.created"`, and an interface type is REFUSED at registration because `reflect.TypeOf` never yields one and such a listener would be listed and never called (ADR 0031). Go methods take no type parameters, so the port is erased and the typing lives in a package-level `On[E]`: **the type assertion is measured at 2.4 ns per listener call against an identical erased control (3.5 % of a dispatch), and the 16 B the caller pays to box the event is published beside it rather than hidden**. Listeners run in ascending priority with ties in registration order, sorted at registration so the read path allocates nothing. Stopping propagation is an AUTHORITY declared at the wiring site (`MayHalt`) and exercised by returning `Halt`; an unauthorised halt does not stop the dispatch and is reported, and a halt is not a failure. Listener errors never short-circuit and aggregate with `errors.Join`, verdict beside cause; a listener PANIC is recovered on the publisher's goroutine with the originating stack, never hijacks the code, is never read as a halt, and does not silence the siblings. `kernel/topic` was read and deliberately **not** used — its per-subscriber buffer makes `Publish` return before the listeners have run and puts `Halt` on the wrong side of a channel — so the domain takes `kernel/snapshot`, the primitive `topic` itself is built on. No registry, no `Config`, no new kernel primitive; blocks `0.2.22.*` / `0.3.52.*` — `docs/adr/0053-sdk-events-domain.md`
- ADR 0054 — asynchronous durable message-queue domain (`queue`): the right-hand column of the frontier table ADR 0053 drew before either domain existed, honoured on every axis by a MECHANISM rather than an intention — many processes, asynchronous, a consumer's goroutine, durable, retried, dead-lettered. Delivery is **at-least-once**, because exactly-once does not exist over a transport, and the consequence lives in the TYPE rather than in a paragraph: `Deliveries` is a field of every delivery counting from 1, and `ConsumerConfig.HandlerIsIdempotent` is REFUSED at its zero value — `resilience.HedgeConfig.Idempotent`'s instrument, for its reason. A message is removed at ACKNOWLEDGEMENT and never at read, so a consumer that is SIGKILLed loses only its lease; a lapsed holder's `Ack` releases NOTHING and says `LEASE_EXPIRED` (ADR 0052's rule one domain over), while `UNKNOWN_RECEIPT` stays separate because "you were too slow" and "this was never yours" are different bugs. **Ordering is not guaranteed and one retry is enough to lose it**, demonstrated by a test rather than left to be discovered. The dead-letter record keeps the failure's reason, dotted-quad code and WIRE-SAFE Public half — never the log-only Private half, because a dead-letter store is read by whoever is investigating and often not the trust domain that failed. The durable broker's entire state is a DIRECTORY and every transition is one `rename(2)`, which is also the whole exclusion: the loser gets `ENOENT`, identically for goroutines and processes — unlike the `flock(2)` ADR 0052 measured giving zero exclusion between goroutines — so there is no lock, no sweeper, and nothing a dead process can hold. It composes `vfs` for the two writes that must be atomic AND durable and stops exactly where `WritableFS` stops, at `Rename`. The consumer-death test **SIGKILLs a real child process** holding a real lease and asserts the message returns with `Deliveries == 2`, which proves every fact recovery needs was on disk before the kill — a `Nack` test proves only that `Nack` works. Measured: durability costs **1 865×** on a round trip and the cost is the two `fsync`s and NOT the payload (5 % across a 1 024× size range); `Receive` scales with the BACKLOG, the domain's stated limit; and two defects were found by the numbers and fixed with a profile first — a double payload clone at 99.78 % of allocated bytes, and an O(in-flight) lease reclaim taken from 105 µs to 1.83 µs with `kernel/heap`. Blocks `0.2.23.*` / `0.3.53.*` — `docs/adr/0054-sdk-queue-domain.md`
- ADR 0055 — relational-database domain (`sql`): a set of PORTS above `database/sql` and deliberately never an ORM — the ports speak the stdlib's own `*sql.Rows`/`*sql.Row`/`sql.Result`, because once `Rows` is ours scanning is ours and mapping is one refactor away. **`database/sql` is permitted and a DRIVER never is** — pgx, go-sql-driver and the sqlite bindings are vendor connectors that live under `third-party/` or nowhere (ADR 0012) — so the suite writes its own `driver.Driver`, which buys a statement log and deterministic failure on a named statement that no live database can give. Transaction ownership is STRUCTURAL: a `TxFunc` receives an `Executor` with no Commit, no Rollback and no Begin, so a callee committing under its caller is a sentence with no spelling. Nesting is a SAVEPOINT with every trap closed — an SDK-generated name, a counter that never resets inside one transaction (a reused name shadows rather than replaces, so `RELEASE` would free the wrong one), non-zero options refused, and a context chain keyed on the manager pointer so two databases never nest into each other. **A cleanup statement never travels on the context whose cancellation caused it** — the nested undo ran on the expired context and a per-scope deadline poisoned a perfectly healthy transaction, a real defect found by writing the test first; `ErrTxDone` is now separated from poison, because "already over" is a KNOWN state and "unknown" is the one that must not commit. The migration lock is the ENGINE's session-scoped advisory lock and deliberately not the `lock` domain, since what decides a migration lock is what happens when its holder DIES: a row in a table survives that death and every later deploy hangs behind it, while the server drops a session lock when the process stops existing. SQLite is refused BY NAME rather than run unlocked. And a partially applied migration is answered PER ENGINE rather than glossed — PostgreSQL and SQLite roll the schema change and its version row back together, MySQL's DDL commits implicitly and cannot, stated in four places instead of once. Measured: ownership 715 ns / 4 allocs over raw `database/sql`, the `Executor` guard 25 ns and ZERO allocations per statement. Blocks `0.2.24.*` / `0.3.54.*` — `docs/adr/0055-sdk-sql-domain.md`
- ADR 0056 — filesystem domain (`vfs`): 23rd core sibling, **no registry**; the read half is `io/fs` **unchanged** — `FS` is a type ALIAS, so `fs.WalkDir`, `fs.Glob` and `fs.ReadFile` apply with no adapter and there is deliberately no `vfs.Walk` — while the write half is a port FROZEN at four verbs and publication is `AtomicWriter`, an ADR 0039 sibling rather than a fifth method. The ADR's subject is **how far "atomic" reaches**, decided rather than assumed. It GUARANTEES that a concurrent reader sees the complete old or the complete new content — measured at **0 torn reads out of 600** against **540–597** for the same harness writing in place — and that on any failure through the rename the previous bytes are byte-for-byte intact with no temporary surviving, proved by injecting a failure at each of five steps over a genuinely half-written handle and comparing SHA-256 before and after. It does NOT reach across filesystems (`EXDEV` is made *unreachable* by keeping the temporary in the target's directory, never handled, and there is no copy-and-delete fallback because that is not atomic and would make the guarantee depend on a mount table), anywhere `rename` is not the kernel's (FUSE, SMB, FAT/exFAT), beyond what `fsync` truly does (a device that lies about write-through loses the file and no code here can detect it), or to mutual exclusion between publishers — the domain takes no locks, the last renamer wins, and no reader ever sees a mixture. A directory flush that fails AFTER the rename is a different verdict (`DirectorySyncFailed`) and is deliberately NOT rolled back, because undoing it means a second non-atomic write to repair a durability problem. The `Lstat`-then-open symlink window is documented as observed-state, not race-free; confinement to the root is the kernel's via `os.Root`. Windows is refused at construction (ADR 0018) rather than approximated. Measured: publication costs **two device round trips and not the bytes** — a 256× larger payload costs 4.7 % more — so batching helps and buffering does not. Blocks `0.2.25.*` / `0.3.55.*` — `docs/adr/0056-sdk-vfs-domain.md`
- ADR 0057 — authorization domain (`authz`): RBAC + ABAC + combinators, and the two things that make it composable. **There is no policy DSL** — a condition is a Go func, a grant table a Go slice, a resource a string compared by byte equality; no parser, no grammar, no wildcard, no relationship model. A third-party policy engine would be a connector, not a mechanism of this SDK. And **abstention is a third verdict**: `Allow` / `Deny` / `Abstain`, with `Abstain` as the ZERO value, because folding "this policy has no opinion" into a grant is a hole and folding it into a refusal is an outage that gets repaired by making the combiner permissive — which puts the hole back one layer up. `DenyOverrides` is the only combining algorithm and the XACML alternatives are refused by name; an `Allow` does not short-circuit, so the fold is commutative and associative. A request no policy is applicable to is **refused** — in `Check`, the one closure, and as a refusal rather than an error. Every refusal carries the same wire sentence and says nothing about which policy or attribute produced it, with the diagnosis moved to Private + fields; RBAC abstains rather than denying so it composes; an absent attribute is never a false comparison and an unevaluable branch is absorbing everywhere, `Not` included. Evaluation allocates zero at ~210 ns. Blocks `0.2.26.*` / `0.3.56.*` — `docs/adr/0057-sdk-authz-domain.md`
- ADR 0058 — server-side rendering domain (`view`): built ON the stdlib `html/template` and deliberately not reimplementing it. Contextual escaping is not an escaping function, it is an HTML **parser** tracking the cursor through `{{if}}`/`{{range}}` branches that must agree on where they leave it — a hand-written one would be the single most likely source of the XSS the domain exists to prevent, and html/template being the stdlib means "zero dependencies, total control" is already satisfied. `text/template` therefore has **no representation at all**: the two packages are API-compatible, so the substitution COMPILES, passes every test that does not assert on escaped output, and ships stored XSS — an AST audit parses every `.go` file in all three packages, production AND test, and fails the build on the import. `Render` returns `[]byte` and never takes an `io.Writer`, because execution fails halfway and a `ResponseWriter` would already carry the status line, the headers and a plausible prefix of the page. Templates are named by their full SLASH path, since html/template's `ParseFS` names by `filepath.Base` and would collapse `admin/page.html` into `user/page.html`. The tree is parsed eagerly AND every escaping plan forced at construction, so the three failures html/template only detects at first `Execute` become a deploy that does not come up. `TrustHTML` is the one bypass the SDK spells and the other six trust types are refused in render data with the path named — while what it does NOT prevent is stated in four places: `TrustedHTML` is a type ALIAS, because html/template recognises trust by an exact type switch and a defined type would be escaped like any other string, so a `Trusted` the caller built from user input is an XSS the SDK cannot see. It also fixes a defect its own tests caught: every construction failure returned a NON-NIL `Renderer` holding a nil pointer, so a broken template tree came up and panicked on the first request instead of stopping the deploy. Measured: reparsing per request costs **34×** on a realistic 42-template tree and only 1.7× on a two-file one — small enough to survive review and look like noise in staging. Blocks `0.2.27.*` / `0.3.57.*` — `docs/adr/0058-sdk-view-domain.md`
- ADR 0059 — **there is no `mapper` domain**, and the ADR is the refusal: a projection is a TYPE, not a tag. The ticket's premise was checked first and is false — `codec.Codec` is `Marshal(v any) ([]byte, error)` / `Unmarshal(data []byte, v any)`, object-oriented on BOTH ends, so "`codec` stops at bytes" describes a codec this SDK does not have; renaming is `json:"name"`, omission is `json:"-"`, and map→struct already ships in twelve lines as `config.decodeInto`. The one surviving candidate is serialisation GROUPS, and the SDK has answered that question three times already — `jwk.MarshalPublic`/`MarshalPrivate`, `health`'s closed `bodyValue`/`checkValue`, and rule 4's Public/Private split — always with a closed wire type plus a named method whose name a reviewer reads at the call site. A group mapper would have replaced NONE of the three, because each needs something a field-subset cannot express: `jwk` REFUSES (an `oct` JWK is its secret, so there is no public projection of one), `health` RESHAPES (`Detail` decides whether `Checks` is computed at all), `token` passes private claims through as raw bytes. Measured against the strongest form of the proposal — ADR 0046's plan compiled once per type and cached, the mechanism the ticket asked to follow — it is 3.2× slower and allocates 5.9× the bytes of the code it would replace (705 ns / 96 B / 3 allocs for a closed wire struct against 2 261 ns / 568 B / 11 allocs at the reflect floor); the cache works, buying 1.58× over recompiling, but the gap is STRUCTURAL, since a mapper boxes every field into `any` and then encodes a `map[string]any` instead of a static struct whose encoder the stdlib compiles once — when the destination is the wire, the map is not a step toward it but a detour away from it. And it is the one that LEAKS, demonstrated rather than asserted: a field added without a `groups` tag reaches the public projection carrying its value under the permissive default, and silently vanishes from a projection believed complete under the restrictive one — neither is ADR 0031's safe default, and a domain whose zero value cannot be made correct should not have one, while a closed wire struct's output is unchanged and CANNOT change. `groups:"public,admin"` is also ADR 0057's refused DSL — string-keyed, untyped, runtime-evaluated — deciding which fields leave the process. What WOULD reopen it is named: a real in-tree consumer of struct→map (where reflection genuinely wins, 16× over the JSON round trip), or a wire-struct GENERATOR, which keeps the compile-time guarantee. Slots `0.2.28` / `0.3.58` are RELEASED — `docs/adr/0059-no-mapper-domain.md`
- ADR 0060 — `health`: an orchestrator asks three questions and the industry answers two of them with one mechanism, which is how a slow dependency becomes a restart storm. So the probes take **different types** — readiness/startup take `Check func(ctx) error`, liveness takes `SelfCheck func() error` with **no context at all**, so a liveness check cannot reach a dependency without a closure that visibly throws the deadline away. Startup gates liveness (a slow boot is not a dead process), `Drain` makes readiness fail permanently without running a check so a replica leaves rotation *before* ADR 0043's drain begins, every check runs under a timeout because an unbounded one turns the probe into a place the process hangs, and the HTTP body carries only the `errs` Public half — `docs/adr/0060-sdk-health-domain.md`
- ADR 0061 — configuration schema: the `config` domain (ADR 0028) gains the declaration of what an application expects — which keys, required or not, and what each is worth when nobody supplied it. No new domain and no new `PP` range: three serials in a block `core/config` already owns. A missing required key fails at **LOAD** rather than at first access, and the pass runs over the **merged map, before the decode**, because after the decode an absent key and a zeroed key are the same bytes; **every** missing key is named at once — ADR 0046's collect-all rule, for the same reason it exists there. `Required` (was the key supplied?) and `validate:"required"` (is the value non-zero?) are different statements and both exist: `port = 0` satisfies the first and fails the second. A key no field addresses is **REFUSED** by default (ADR 0031: the zero value must not be the dangerous one), with `AllowUnknownKeys` as the named escape for a shared file or an `EnvSource("")`; the walk stops at leaves decided by a PROPERTY (`json.Unmarshaler` / `encoding.TextUnmarshaler`) and never by a list of names, and the list is sorted because a message that changes between runs cannot be diffed. A default is a **LAYER** merged under every source — `default < file < env`, placed first STRUCTURALLY, since a default that could win is not a default — and a key is never both required and defaulted, in either direction and at any nesting depth. The schema describes the **SHAPE** and **feeds** `validation` without replacing it: `Rule` IS a `validation.Constraint`, `Check` returns a `validation.Report`, tags compile through `service/validation.Struct`, there is no `min`/`max`/`oneof` here, and `config.Validator` still runs afterwards. Everything decidable at construction is refused there, including ADR 0031's quietest trap — a default outside the bounds the schema itself declares, which produces an invalid configuration only on the deployment where nobody set the key, and blames the operator. No message ever repeats a value. Measured in `internal/service/config/BENCH.md`; blocks `0.2.10.5`–`0.2.10.7` — `docs/adr/0061-sdk-config-schema.md`
- ADR 0062 — the logger names the span a line came from, and the bridge lives at the TOP layer: `Logger.Log` already took a `context.Context`, so nothing was widened and ADR 0039 never came into play — the obstacle was that `Encoder.Append` receives NO context, so the identity has to travel ON the record. `RecordEvent` therefore grows one field rather than two attributes, and that is also the only shape the specification allows: OpenTelemetry prescribes `trace_id` / `span_id` as **top-level keys** of the log object, while an attribute under `WithGroup("http")` renders `http.trace_id`, which no ingestion pipeline recognises. An absent span emits **nothing at all** — not `trace_id=""`, not 32 zeroes, both invalid under W3C Trace Context §3.2.2.3/§3.2.2.4 and both of which would put an unjoinable field on every line a service logs outside a request. The correlation costs **zero additional allocations**: the extraction returns a value type and the ids are `hex.Encode`d into a stack array appended into the buffer the handler already borrowed, so no Go string for an id is ever materialised — measured at 1 alloc/op with a span and without, and profiled (96.9 % of allocated objects remain the pre-existing attrs clone; `hex.Encode` is 4.8 % of an in-span emit's CPU and never appears in the allocation profile). The placement is half the decision: the port (`TraceContextSource`, a FUNC port) is declared in `core/logger`, which stays stdlib-only; `service/logger` takes it at construction and keeps **zero edges** to any sibling service domain; and `pkg/v1/logger` — the layer already allowed to know both domains — is the ONLY file where logging and tracing meet, binding it unconditionally so nobody has to opt in. `core/trace` registering itself into `core/logger` from an `init()` was refused: it inverts the dependency and makes behaviour depend on link-time reachability. No new `PP` range and no error code — nothing on this path can fail — `docs/adr/0062-logger-trace-correlation.md`
- ADR 0063 — message-translation domain (`i18n`): the CLDR subset is NAMED (thirteen hand-transcribed entries — `ar de en es fr it ja nl pl pt pt-PT ru zh` — covering all six categories and every rule shape) and every other language is refused BY NAME at construction with the supported set beside it, because falling back to English's two categories renders a grammatically wrong sentence on every Polish, Russian and Arabic page with no symptom any test or monitor can see. A counted message missing a category ITS OWN language can produce is refused at LOAD, naming the file, the key and the form: the one catalogue defect a translator cannot see by reading their own file. The plural rules follow the MESSAGE and not the request. `Catalog` is frozen at two methods with `KeyLister`/`Fallbacker` as ADR 0039 siblings, and `Lookup` is EXACT so the fallback walk exists in one place that knows which language answered. Negotiation implements RFC 4647 §3.4 Lookup for selection and §3.3.1 basic filtering for a `q=0` refusal, refuses §3.3.2 by name, skips `*`, bounds the header at 32 elements — and has no error return at all, since the header was written by a stranger — so it allocates nothing for any header, after a profile showed `strings.Split` was 31.6 % of the objects on that path. A missing key renders the KEY plus `MESSAGE_NOT_FOUND`, because a blank is a defect nobody reports. Interpolation is named placeholders only — positional, format specifiers, ICU `plural`/`select` and filters each refused by name — substituted once and never rescanned, and no error ever names an argument VALUE. Nothing is escaped here; that is `view`'s, and it is contextual. The catalogue is decoded through `codec` and read through `io/fs` (= `vfs.FS` unchanged), so the domain ships no parser and no filesystem. It also CORRECTS, ADR 0034-style, the reason given for not adopting `golang.org/x/text`: measured, it does NOT introduce the banned `x/sys` — what argues against it is doctrine plus +25.5 % of linked binary and 211 KB of generated tables nobody reads. Blocks `0.2.30.*` / `0.3.60.*` — `docs/adr/0063-sdk-i18n-domain.md`
- ADR 0064 — electronic-mail domain (`mail`), and where the doctrine's line falls: MIME composition is a MECHANISM, written from RFC 2045/2046/2047/5322 in-tree, while SMTP is a third-party PROTOCOL whose client already ships in the standard library — so `net/smtp` costs zero dependencies and what the SDK commits to maintaining is the POLICY above it: a required STARTTLS refused rather than downgraded, credentials never leaving the process unencrypted, the `ServerName` `net/smtp` does not set, the context it does not take. A provider API (SES, SendGrid) would be a `third-party/` connector and is not this domain. **Header injection is REFUSED, never sanitised**: the three stdlib helpers that would repair a CRLF — `mime.QEncoding.Encode`, `mime.FormatMediaType`, `net/mail.Address.String()` on a display name — each deliver a message the caller did not write while reporting success, and `Address.String()` passes a CRLF in the ADDRESS straight through. Thirteen caller-controlled fields are driven with a syntactically perfect injected `Bcc:` and every one must refuse; the refusal names the field and never the value. Bcc reaches `RCPT TO` and no header. The MIME container is a pure function of the populated fields, with plain FIRST inside `multipart/alternative` because RFC 2046 §5.1.4 orders alternatives by increasing preference, and the structure is verified by REPARSING with `net/mail` + `mime/multipart` rather than by comparing strings. `TLSMode`'s zero is refused and there is no opportunistic mode at all. What is NOT guaranteed is stated as loudly: a nil error is ACCEPTANCE by the next hop and not delivery, there is no SPF/DKIM/DMARC because a signing key is infrastructure, and nothing promises how a client will render it. Measured: ≈2.24 ms and 1.370× expansion per MiB of attachment, after a profile put `bytes.growSlice` at 43.7 % of allocated bytes and pre-sizing the buffer took the 1 MiB case down 65 % in memory and 41 % in time. Blocks `0.2.31.*` / `0.3.61.*` — `docs/adr/0064-sdk-mail-domain.md`
- ADR 0065 — command-line domain (`cli`): built on the stdlib `flag` with **zero** third-party dependencies, because an argument parser is a mechanism and not a connector. It adds exactly four things `flag` does not have — sub-commands to arbitrary depth, one help generated from the declarations, a typed exit status, and a seam to `config` — and touches nothing else: a `Binder` receives the stdlib's own `*flag.FlagSet`, unwrapped, exactly as `vfs` takes `io/fs`. `flag.ExitOnError` is refused BY NAME — it calls `os.Exit` from inside a library, which makes the parse untestable, skips every deferred function, and takes a decision that belongs to `main` — and the refusal is enforced by an AST audit over all three packages AND their suites, itself proved against a fixture. `flag` is also kept SILENT: on failure it writes its own message and calls usage BEFORE returning the error, so a default set reports every failure twice, once to a stream the caller cannot intercept. The status is the SDK's existing `errs.WithExitCode`/`errs.ExitCodeOf` and not a second convention; the one addition is `Status`, because `ExitCodeOf(nil)` is 70 and success is the common case at a CLI boundary. `-h` returns nil (0) once its help was delivered — EX_IOERR (74) when the stream refused the page, since an answer nobody received is not a success — and a bad command line writes the SAME help and returns EX_USAGE (64), so the status distinguishes a question from a mistake and the text does not have to. A command is a LEAF or a GROUP, never both — with both, `tool db migrate` changes meaning the day somebody adds a child named `migrate` — and never neither (ADR 0031); the WHOLE tree is validated at construction, which requires CALLING each `Binder` once and is what catches a command binding `-h`, the one flag whose absence is how `flag` signals a help request. No prefix matching, no case folding and **no "did you mean"**: the help just written already lists every name that would have worked. What it composes is stated as loudly as what it refuses — `config` through ONE adapter using `Visit` and never `VisitAll`, `lifecycle` through nothing at all (no `Signals` field: a library that installs a handler because it was constructed fights the caller's `main`), and an Action's error returned VERBATIM so its own exit status is never overwritten. Measured and concluded: the domain adds +3 allocations over `flag` doing the same parse, `New` is 77 % `flag`'s own work, and the whole thing is 2.9 % of the 1.99 ms it takes to start an empty Go binary — so nothing here is worth optimising, and the optimisation the design invites is refused with its price — `docs/adr/0065-sdk-cli-domain.md`
- ADR 0066 — third-party compressors (zstd + s2), and the first placement argued AGAINST ADR 0034's two criteria rather than by reflex: `klauspost/compress` introduces no module, no cgo and no `x/sys`, and cross-builds 10/10 — so criterion 1 does not apply and the quarantine rests entirely on criterion 2, not imposing a graph on consumers who never use it, proved by `go list -deps` returning zero hits in all four inner modules. **brotli is refused**: slower than stdlib gzip in BOTH directions, +2.75 MiB of binary, drags `net/http`, and its only unique value — `Content-Encoding: br` — has no negotiator in this SDK to hook it to. **lz4 is refused**: beaten by `s2` on three axes of four by a scheme costing no extra module, and the C-world wisdom that lz4 out-decompresses zstd is INVERTED in Go, because klauspost's decoder is assembly-optimised and pierrec's is not. A trap is recorded with it: `go list -deps` prints `vendor/golang.org/x/sys/cpu` for anything pulling `net/http`, so a naive grep rejects a library for a reason that is not true. The output ceiling is a **required positional parameter**, never an option — an option can be omitted, and a bound that can be omitted is not mandatory — a non-positive value is refused at construction while `ZstdLevel` clamps, so both halves of ADR 0031 sit in one package. The default is 64 MiB rather than the stdlib schemes' 256 MiB, on arithmetic: the same ceiling costs an attacker 254 KiB through DEFLATE and **28 bytes** through s2. The bomb refusal has its own code, because "unparseable bytes" and "bytes engineered to exhaust this process" have different readers, and a multi-frame bomb whose frames are each individually under the ceiling is refused too — measured, because a per-frame guard would have accepted all sixty-four. Blocks `0.3.63.*` — `docs/adr/0066-third-party-compressors.md`
- ADR 0067 — an instrument gets a description, and it belongs to the NAME: `Meter` is frozen and so is `FullMeter`, so the description arrives on a fourth sibling, `Describer.Describe(name, description string)`, discovered by TYPE ASSERTION and deliberately not folded into the union — the false branch is how a caller learns a foreign `Meter` will drop their documentation, exactly as `cache.Tagger` and `lock.Deadliner` answer by their absence. It is `Describe(name, …)` and not a parameter on `Counter` because the OTel data model puts `description` on the Metric and calls it explicitly NON-IDENTIFYING, and because a docstring on the one variadic call the compiler must prove non-escaping would spend the SDK's tightest budget on prose — measured: the described fetch is 191.4 ns / **0 allocs** against 188.9 ns undescribed, and the map is nil until the first `Describe`. `Description` joins the three metric envelopes, which are PUBLISHED shapes — permitted only because the module is v0, said out loud (ADR 0040), and breaking nothing because every literal in the tree is keyed. Each wire answers the absent case by omission: `# HELP` above `# TYPE` or no line at all, `Metric.description` or no field at all (a plain proto3 string has NO presence, so `""` and absent are one value — the opposite call from ADR 0048's three zero-emitted fields, and the reason is in the schema), `description="…"` last on the text header or no key at all. The Prometheus docstring gets its OWN escape — backslash and line feed, and NOT the quote, because a docstring is not a quoted token and escaping it would corrupt the prose to defend against a delimiter that is not there. An empty description is refused and so is a second differing one, while identical text is idempotent — `0.2.9.7`/`0.2.9.8` — `docs/adr/0067-metrics-instrument-description.md`
- ADR 0068 — the layer firewall is a checked GRAPH, because Gazelle's visibility admits the whole repository: ADR 0004 put kernel → core → service → pkg/v1 on `package_group` + `visibility` and promised that a rogue import fails `bazel build`, but Gazelle mirrors Go's `internal/` rule and gives every package under `//internal` the visibility `//:__subpackages__` — the whole repo — and no directive removes it (105 of 193 BUILD files). Demonstrated, not argued: a throwaway kernel package importing core built. So `scripts/check-layer-deps.sh` asserts four `bazel query` expressions empty — kernel reaches only kernel, core no service/pkg/third-party, service no pkg/third-party, pkg no third-party — in `make lint` and CI, failing closed; `# keep` on 105 visibility attributes was refused as the same failure moved — `docs/adr/0068-layer-firewall-is-a-checked-graph.md`
- ADR 0069 — behind a proxy that announces the scheme, the default WebSocket origin rule refuses instead of guessing: `Forwarded` and `X-Forwarded-Proto` are read by PRESENCE and never by value, so a header a stranger writes can only make the check stricter; where TLS ended in this process the scheme is known first-hand and the announcement is ignored, and the refusal names `AllowOrigins`. Amends ADR 0047 §D7 — `docs/adr/0069-websocket-origin-behind-a-proxy.md`
- ADR 0070 — the two keys the logger writes itself are reserved: an attribute named `trace_id` or `span_id` at the TOP level renders as `attr.trace_id`, because two members of one JSON object with the same name let a decoder keep the caller's value as the line's correlation — renamed and never dropped, grouped keys untouched, one predicate for every writer. Amends ADR 0062 — `docs/adr/0070-logger-reserves-the-correlation-keys.md`
- ADR 0071 — a registry refuses a plug-in it cannot store, at the call that publishes it: `if x == nil` let a TYPED nil through (measured — stored, and handed back by `Lookup`, to fail at the first dispatch) and let a NON-COMPARABLE plug-in reach the duplicate check, where `==` panics with Go's `comparing uncomparable type` instead of the domain's conflict. One kernel primitive, `plugin.Unusable`, answers for the fifteen registrars in eight core packages; it returns a REASON rather than a bool or an error, and each registrar keeps its own dotted-quad code — `docs/adr/0071-a-registry-refuses-what-it-cannot-store.md`
- ADR 0072 — `health` bounds every wait it owns: the sd_notify announcement had no deadline, and a unixgram write blocks once the receiver's queue is full (measured — a brand new sender stops at 514 small datagrams), so under the lock that serialises announcements one deaf supervisor stopped every later probe from answering. `sdnotify.NotifyContext` bounds the write by the PROBE's context, falling back to the budget a check would get, armed on the injected clock. And a check RUN now owns its budget instead of borrowing the waiter's, so a run every caller left is still told its time is up. Amends ADR 0060 — `docs/adr/0072-health-bounds-every-wait-it-owns.md`
- ADR 0073 — the session file store's two waits can be abandoned: `flock(LOCK_EX)` parks a thread inside a syscall no cancellation reaches, so a cancelled request waited for a lock nobody would read the result of, and the in-process half was a `sync.Mutex` whose `Lock` cannot be told its caller left. Both now observe the context — `LOCK_NB` plus a poll on the injected clock (`FileConfig.Poll`, the same 25 ms default as the `lock` domain, whose ADR 0052 had answered the same question the other way) and a one-slot channel gate; `FileConfig.Clock` widens to `clock.Timed` under the ADR 0040 v0 licence. Amends ADR 0045 — `docs/adr/0073-session-waits-are-abandonable.md`
- ADR 0074 — what a public alias may point at: the axis is OWNERSHIP, not value-versus-handle. A type the port speaks lives in core; a type meaningful to exactly one engine — its handle, its `Option` closures, and above all its construction parameters (`sql.Config`, `session.FileConfig`, the five `resilience` configs) — lives with that engine, because hoisting a `*sql.DB` or an SMTP TLS mode into the contract layer makes core describe one backend. Measured: 233 core aliases, 69 service, 3 kernel. Nothing moved; the rule now describes the code — `docs/adr/0074-what-a-public-alias-may-point-at.md`
- ADR 0075 — the SDK reads the cgroup cap that already bounds this process, not only the ones it writes for others: `cgroup` writes a group to bound a child and `rlimit` sets a kernel-enforced ceiling, but nothing read the cap already on the caller — and the Go runtime does not either (`GOMAXPROCS` is cgroup-aware since 1.25, the memory limit never was), so a service in a 512 MiB container is SIGKILLed where a soft limit would have pushed the collector. `memlimit` reads `/proc/self/cgroup` rather than the mount root, minimises across ancestors and across both hierarchies, keeps 10% headroom and declines under 64 MiB. Every outcome is a VALUE — `Apply` cannot fail — and `MemorySource` names which of the three declining outcomes happened, with the enum's zero left unminted per ADR 0031. Versed from `kodflow/ktn-linter` `pkg/memlimit`. Its three §Deferred items are now CLOSED: the mount's root within its filesystem (mountinfo field 3) is read and the membership path expressed relative to it, the octal escapes the kernel writes into mountinfo paths are decoded while `/proc/<pid>/cgroup`'s literal bytes are left alone, every attachment of a hierarchy is kept rather than only the last, and the 90% share is computed over the allowance's hundreds and remainder so it is exact for every `int64` and cannot overflow — `docs/adr/0075-reading-the-cgroup-cap-that-already-bounds-us.md`
- ADR 0076 — what a branch changed is a value that can say it does not know: `Resolve` returns NO error, because a resolver that cannot tell what changed must never answer with an empty set — "nothing changed" and "I could not tell" are opposite instructions, and a scoped review receiving the first passes on a branch it never examined. Degrading produces `FullFallback` + a `Reason`; the zero `ResolutionValue` is neither readable shape, so a caller who skipped `Degraded()` cannot read "nothing changed" out of a value nothing minted (ADR 0031). The set spans the three-dot merge-base delta, the index, the working tree and untracked files. Every git invocation is hardened against a hostile `.git/config`: `core.fsmonitor` set to a script executed 5 times on read-only queries before the guard and 0 after, and `diff.external` needs `--no-ext-diff` because setting it empty makes git execute `""` and abort — with what is NOT hardened listed too. The `.go` suffix + generated-file filter the source hard-coded becomes `Config.Include`, nil admitting everything. Versed from `kodflow/ktn-linter` `pkg/git` — `docs/adr/0076-what-a-branch-changed-is-a-value-that-can-say-it-does-not-know.md`
- ADR 0077 — a self-update is an order of operations, and a product name is not part of it: comparing an archive's SHA-256 against a checksum file fetched from beside it verifies NOTHING, because whoever substitutes the archive substitutes the manifest too. The digest counts only after a detached ed25519 signature over that manifest verifies against a key linked into the build — and reversing the two leaves every log line identical, which is why the suite asserts the order. A build with no vendor key installs nothing: verify-if-present fails OPEN and makes the guarantee a property of a build flag nobody inspects. Eighteen codes in three classes (retryable / fixable by an opt-in / supply-chain, where no retry helps and no manual install is safe), re-exported from the facade as `pkg/v1/authz` does. Host, repositories, asset pattern, binary name and both env vars move into `SourceValue`, the env names DERIVED by uppercase-and-fold so `ktn-linter` still yields `KTN_LINTER_AUTO_UPGRADE`. Adds `golang.org/x/mod/semver`, which `go list -deps` shows is stdlib-pure. Versed from `kodflow/ktn-linter` `pkg/updater` — `docs/adr/0077-a-self-update-is-an-order-of-operations-and-a-product-name-is-not-part-of-it.md`
- ADR 0078 — entitlement ships under `third-party/` because proving key possession brings `x/sys` with it: the mechanism is generic (vendor-signed roster with a bounded window, possession PROVEN rather than asserted, an offline cache re-verifying the signature on every read, an anti-rollback clock ratchet, a CI seat from the runner's OIDC provenance rather than a shared secret) but `x/crypto/ssh` pulls `x/term` and through it introduces `x/sys` — banned SDK-wide — measured with a probe module the way ADR 0034 insists (`x/sys v0.48.0 // indirect`). So it is quarantined in the root module exactly as the HCL codec (ADR 0022/0034) and the AWS writers (ADR 0012) are, and `pkg/v1` consumers inherit nothing. The vendor's origins, cache directory, OIDC audience and enrolment URL become `ProductValue`, whose every method tolerates a nil receiver because the one code path that runs when nothing else is working must not be the one that panics; and `ProductValue.Validate` is the source's own origins-are-distinct TEST turned into an API, since moving the origins to the caller would have moved that property out of reach with them. Fifteen typed sentinels, and "cannot decide" still never reads as "no" — `RosterUnreachable` names the NETWORK, because reporting an outage as a revocation is the one wrong answer. Versed from `kodflow/ktn-linter` `pkg/license` — `docs/adr/0078-entitlement-is-quarantined-because-ssh-brings-x-sys.md`
- ADR 0079 — the entitlement split: `x/sys` was never in the mechanism, only in the identity: ADR 0078 quarantined the WHOLE domain under `third-party/` and recorded the split as the better long-term shape it deliberately was not doing, so a versement stayed diffable against its source — a reason that expired the moment the versement landed. Measured rather than assumed: 4 146 of 4 835 production lines reach no ssh import at all, so `x/sys` is in the IDENTITY and the quarantine was drawn along the package the dependency happened to arrive in. The domain splits along the four-layer contract — a three-method `Identity` port in `internal/core` (`Discover`/`Fingerprint`/`ProvePossession`, none of which says "ssh"), the engine in `internal/service/entitlement`, a facade in `pkg/v1/entitlement`, and ONLY the ssh implementation left under `third-party/`. Measured on the `pkg` module with `GOWORK=off`: 8 modules before, 8 after — the public API gains a complete machine-entitlement mechanism and ZERO dependencies, because the service's one non-stdlib import (`x/mod/semver`) was already in that graph. Code range rebased `0.3.65.*` -> `0.2.35.*`. A consumer with a TPM, a cloud KMS, a hardware token or its own key format implements three methods and inherits none of the root module — `docs/adr/0079-the-entitlement-split-x-sys-was-never-in-the-mechanism.md`
- ADR 0080 — the gate decides, and performs nothing: A product that verifies an entitlement at start-up answers three questions before doing any work — is THIS invocation subject to the check, what does a mandated upgrade do, and what should the process do about a no — and every consumer answered them by hand in a root command. `gate` owns the first two as VALUES and performs none of the three: it does not verify (that is `entitlement`), does not upgrade (that is `selfupdate`), and never ends a process — ADR 0065's rule applied one domain over, on a path that would otherwise touch the network and replace the running executable. Two exemption lists rather than one, because a name-only match cannot say that `skill` runs unchecked while `skill install` does not; and descending is a prefix on a SEPARATOR boundary, never on bytes, or `licensed` inherits `license`'s exemption and runs unchecked. `RecoveryPaths` turns the lockout that LOOKS fine — a policy gating its own repair command — into a construction-time refusal, and `Validate` returns ONE error naming every fault rather than an `errors.Join`, because a joined error has no single Private detail and a structured-logging caller would fix them one start-up at a time. Both enums leave zero unclaimed (ADR 0031): refusing and upgrading are opposite answers, and a decision nobody made must not read as "allow" — `docs/adr/0080-the-gate-decides-and-performs-nothing.md`
- ADR 0081 — the Windows file lock is a different primitive, measured rather than recited: Windows was refused by ADR 0052 §D8 on the argument that `LockFileEx` behaves *almost* like `flock(2)` and "almost" is the whole problem for the one primitive whose failures are invisible when they happen. Every difference it named is real; the conclusion drawn from them was not — "almost" is a problem when the differences are unknown and a specification when each is measured on a real kernel, written down, and pinned by a test that fails when it changes. The cost of the refusal stopped being hypothetical when a consumer moved its daemon's single-instance lock onto `pkg/v1/lock` and the daemon stopped starting on Windows, then reimplemented `LockFileEx` locally with none of the evidence. The `x/sys` ban is NOT a blocker and is not relaxed: the stdlib `syscall` exports neither `LockFileEx` nor `UnlockFileEx`, so both are bound from `kernel32.dll` through `syscall.NewLazyDLL` with the flags and the one error code hand-cited — ADR 0018's own discipline, already the shape of `proc/{exec,cgroup,signal}` — and the SDK gains no module, no `go.sum` entry and no `MODULE.bazel` change, so `third-party/` would quarantine a dependency that does not exist. The range is the WHOLE file because the fencing counter lives at offset 0 of the very file that carries the lock: a high-offset byte excludes just as well and leaves the ledger writable by every non-holder, measured rather than argued. Its locks being MANDATORY cuts both ways and both are kept — the ledger is strictly BETTER protected here than under advisory `flock(2)`, and a foreign read of a HELD lock file fails, which bites least in the incident that wants it since a dead holder's lock is already released. The holder's exemption is per HANDLE, which is what lets `ReadAt`/`Truncate`/`WriteAt`/`Sync` reach the ledger through the descriptor holding the lock; all four are asserted together, because a refusal on any one would fail every acquisition AFTER taking the lock. And the row ADR 0052 §D7 measured comes out INVERTED: re-locking the same handle is refused where `flock` converts, so the in-process `nameGate` is redundant for exclusion on Windows and kept for three other reasons — it queues goroutines instead of polling a range their own process holds, it makes the two kernels behave identically, and it keeps the one-descriptor refactor from turning a silent no-op on Unix into a hard failure here. The second blocker was quieter: `checkDir` carried NO build tag and read POSIX mode bits, and `os.Stat` on Windows synthesises `0777` for every writable directory from one read-only attribute — so the rule would have refused EVERY directory, as a typed `LOCK_DIRECTORY_UNSAFE` blaming the deployment, and only from the second start since `prepareDir` checks only directories it did not create. It splits by build tag: the POSIX rule is unchanged and now pinned by all seven of its rows rather than two, and Windows accepts with the gap named — because what the rule prevents is an inode swap, and `os.OpenFile` reaches `CreateFileW` WITHOUT `FILE_SHARE_DELETE`, so a held lock file can be neither deleted nor renamed whatever the ACL says. A real DACL check is deferred with its cost, not glossed. The lane is named in the same change: `./lock` joins `SERVICE_PKGS` in `e2e-cross.yml`, the only gate either the backend or its suite has — `docs/adr/0081-the-windows-file-lock-is-a-different-primitive.md`
- ADR 0082 — the lock path is a file, never a link to one: the file locker derived its filename as `hex(sha256(name)) + ".lock"`, which keeps every caller string off the filesystem and, in the same stroke, makes the name PREDICTABLE — and `checkDir` never reached it, because its rule governs UNLINKING an entry that exists while this attack CREATES one at a name nobody has taken yet. The sticky bit does not help, since the planter owns the link they made, and `0777|sticky` — exactly what `/tmp` is — is a row ADR 0081 §D5's table explicitly ACCEPTS. Probed against the shipped `pkg/v1/lock` from an external program: `held=true`, `err=<nil>`, and the `flock` plus the fencing ledger on a file outside the checked directory, with no layer reporting anything — two processes inside one section over different inodes, which is the failure the whole domain exists to prevent, reached with no race at all. The fence goes with it: a link to a file holding `48213\n`, which is a pidfile's shape, made `Acquire` return fence 48214 and rewrite the pidfile with it — so the planter chooses the token the victim hands to the protected resource. It is NOT arbitrary truncation and that was measured rather than assumed, because `readFence` refuses a non-decimal target with `LOCK_FENCE_CORRUPT` BEFORE `writeFence` truncates. ADR 0081 §Deferred saw the Windows half and deferred it on "both sides or not at all"; the Unix side is now confirmed live, so the symmetric answer is to close both. One sentinel, `LOCK_PATH_REDIRECTED` (`0.3.51.4`, `EX_CONFIG` like `LOCK_DIRECTORY_UNSAFE` because it is the same remedy), and two opposite mechanisms behind it: `O_NOFOLLOW` makes the kernel FAIL the open, while `FILE_FLAG_OPEN_REPARSE_POINT` makes it SUCCEED on the link itself, so Windows needs the pairing check on `GetFileInformationByHandle` — the flag alone would fix the redirection and leave the lock sitting on a link the planter can retarget. `LOCK_BACKEND_FAILED` was rejected because nothing failed and it invites a retry, the one wrong response to a deliberate substitution; `vfs` already answers this with `PathEscaped` and `queue` with `why=symlink`, so a third domain calling it a medium fault would be the odd one out. The errno is never branched on — measured `ELOOP` on linux/amd64, documented `EMLINK` on FreeBSD/DragonFly, `EFTYPE` on NetBSD, `ELOOP` on OpenBSD/Darwin — and `os.Lstat` after a refusal the kernel already took is diagnosis with nothing to race. The open stays `os.OpenFile` because go1.27 forwards `FILE_FLAG_*` in the high bits of its flag word, which keeps ADR 0081 §D5's "without `FILE_SHARE_DELETE`" justification literally true instead of restated in a second place that can drift. What is NOT closed is named: `O_NOFOLLOW` governs the FINAL component only, so a symlink at a parent of `FileConfig.Dir` is still traversed, and a hard link is still accepted — both weaker, both the caller's own configuration or the planter's prior access. A FIFO was already refused loudly by the ledger, measured, so no general regular-file rule was added — `docs/adr/0082-the-lock-path-is-a-file-never-a-link-to-one.md`
- ADR 0083 — a path is a chain, and a held lock can lose its file: ADR 0082 §Deferred's first two items, closed. A link planted at a PARENT component of `FileConfig.Dir` redirected every lock and every ledger and `checkDir` took its verdict on the TARGET, so an attacker redirecting into a tidy `0700` directory of their own also passed the mode rule — reproduced at `c41c9b3e`. `O_NOFOLLOW` cannot reach it and that is a kernel limit, so the missing capability was built: `internal/kernel/pathchain` walks a path one component at a time over `os.Root` directory handles and returns one `StepValue` per component — what it is, where it points, and the mode of the directory it was found IN. It goes in the kernel on rule 1 alone, stdlib-only and generic, and it REFUSES NOTHING, because the callers in view want opposite verdicts on the same shape. The mechanism was read in the pinned toolchain rather than assumed: `syscall.Openat` is declared for linux (`syscall_linux.go:285`), aix and wasip1 and NOWHERE else — darwin does not even define `SYS_OPENAT` — so a hand-rolled `openat` walk would have served one of the six kernels `e2e-cross` runs and silently skipped five; `os.Root` is the standard library's own `openat` walk, on every GOOS, at the price of RESOLVING a within-root symlink, so `pathchain` never asks it to traverse one and restarts the walk itself after an `Lstat`+`Readlink`. `..` is a stack pop, because `Root.OpenRoot("..")` is `path escapes from parent` by construction. The domain's rule: an indirection at a parent is refused only when the directory HOLDING it is world-writable — anybody could have planted it — and the sticky bit exempts nothing, which is ADR 0082's own creation-versus-unlink argument one level up. A blanket refusal was rejected on measurement, not taste: `/tmp` is a symbolic link on macOS, `/var/run` is one on most Linux distributions, `C:\Users\All Users` is a junction, and refusing them would be ADR 0018 §(a)'s failure mode wearing an error that blames the deployment. On Windows nothing new is refused, because `os.Stat` synthesises `0777` for every writable directory and the rule would refuse every junction under one; the gap is pinned by a test on a real kernel so it is measured rather than assumed, and the DACL check that would close it is priced in §Deferred down to the two advapi32 entry points it needs. The second half is `LOCK_FILE_REPLACED` (`0.3.51.5`, `EX_CONFIG`): in `0777|sticky` — `/tmp`'s mode, a row the table accepts by name — the account that created the lock file owns that entry and may unlink it WHILE the victim holds it, measured as two holders over inodes 69831 and 69832 with the fence RESET to 1 on both and the victim's `Extend` returning `<nil>`. It is DETECTION and not prevention and the ADR refuses to call it anything else: the descriptor outlives the name on every kernel, so the second holder still acquires and `TestTheSplitIsDetectedAndNotPrevented` asserts that it does. `Extend` is the only call a holder makes DURING the section, so it is where the check lives and where a `Keepalive` turns it into a cancelled context whose cause carries both codes. An owner check was re-rejected on ADR 0081 §D5's shared-group arrangement and `O_EXCL` was evaluated and prevents nothing, since the unlink happens later on a descriptor already opened and locked. The comparison is `os.SameFile` against `os.Lstat` with no platform split and it FAILS OPEN — only a positively established mismatch refuses — and the one thing that could not be reasoned about safely from documentation, that Windows lets `loadFileId` open a file this process holds without `FILE_SHARE_DELETE` because it asks for zero access, is measured on the `windows` job of `e2e-cross` — `docs/adr/0083-a-path-is-a-chain-and-a-held-lock-can-lose-its-file.md`
- ADR 0084 — the Windows lock directory has an answer, and it is not a mode: two rules in `lock` rest on one question — is this a directory any account can put an entry into? — and on Windows it had no answer at all, so `checkDir` accepted every directory (ADR 0081 §D5) and `checkChain` refused nothing (ADR 0083 §D4), while the Unix side refused a world-writable-without-sticky one. The remedy was deferred THREE times on a single cost estimate, ~250 lines of ABI, which neither record carrying it forward ever re-checked. Re-read against the pinned toolchain it is TWO `advapi32` exports: go1.27's `syscall` already ships `StringToSid` (`ConvertStringSidToSidW`), `(*SID).String` (`ConvertSidToStringSidW`) and `LocalFree`, so only `GetNamedSecurityInfoW` and `GetAce` are bound — with `syscall.NewLazyDLL`, the discipline ADR 0081 used for `LockFileEx`, adding no module, no `go.sum` entry and no `MODULE.bazel` change. `EqualSid` is not bound at all: rendering each ACE's SID back to its string form makes the comparison byte equality against a two-element table, which is what ADR 0057 does for a resource and where a real part of the old estimate went. The MASK is read and not merely the identifier — a world-READABLE lock directory is not a world-writable one, exactly as `0755` is not `0777`, and that row is in the table; `FILE_DELETE_CHILD` counts because unlinking the lock file and recreating it is the same attack from the other side, and `WRITE_DAC`/`WRITE_OWNER` count because a right to become writable is a right to write. Deny entries subtract in LIST order and nothing is reordered, because a DACL that is not canonical is evaluated as it is; `INHERIT_ONLY_ACE` is skipped since it grants nothing on the object itself; object-type ACEs are not decoded because a GUID sits between their mask and their SID, and an entry whose layout this code does not know is one it does not judge. A NULL DACL is read as the most permissive state Windows has rather than the emptiest — reading it as "no entries, so no grants" is the exact inversion that makes a security check worse than none. "Anybody" is Everyone (`S-1-1-0`) and Authenticated Users (`S-1-5-11`), the pair ADR 0081 named, and `BUILTIN\Users` is EXCLUDED with its reason written down rather than the exclusion left implicit: Windows itself grants it write on directories it ships. The whole check fails OPEN, and the asymmetry decides — a wrong refusal costs a locker that will never build on a perfectly safe directory, on a platform this code cannot be iterated on locally, while a wrong acceptance leaves the platform exactly where it already was; the Win32 status travels in the refusal's field so "the check ran and accepted" is distinguishable from "the check could not run". Five rows drive real ACLs through `icacls` on `windows-latest`, and the ACCEPTING ones come first on purpose: if the runner's temporary directory carried an Everyone-write entry, every lock test would fail and read as a bug in the domain rather than as a wrong assumption about the runner. A Windows deployment that worked now fails, by design, with the same `LOCK_DIRECTORY_UNSAFE` the Unix side has had since ADR 0052 — `docs/adr/0084-the-windows-lock-directory-has-an-answer-and-it-is-not-a-mode.md`
- ADR 0085 — both halves of a release read the same range: `compute-bumps.sh` answers WHETHER to release over a RANGE while `cut-tags.sh` answered HOW BIG from `git log -1 HEAD`, and when the two disagreed the bump fell silently to patch. A release fires only on a SUCCESSFUL CI run, and the concurrency group cancels the in-flight run when the next push lands — so a cancelled run yields no release at all and the commit that carried `Release-bump: minor` is routinely not `HEAD` when a release finally runs. Its `pkg/` change was still inside the range, so a release was cut; its trailer was never sought, so the release was a patch. Fifty new public symbols nearly shipped as `pkg/v0.1.42`. Measured rather than assumed to be rare: of the 44 consecutive release ranges on `main`, 23 span more than one commit and the largest spans 213. `release_base()` moves into the shared `lib/tag-format.sh` so both halves compute the baseline once, and `cut-tags.sh` reads the trailer over `release_base()..HEAD` taking the LARGEST bump found — a trailer states a floor, so a later commit that says nothing cannot shrink one that did. The per-commit path scoping is kept deliberately: a trailer counts only for a commit that itself touched `pkg/` (ADR 0007 §2), because scoping the whole range at once would let a docs merge's trailer ride on an unrelated commit's `pkg/` change. The issue's other direction — refuse to cut when the range holds a commit whose own release never ran — was rejected on that same measurement: it fires on about half of all releases, it can only be answered by the Actions API from a script that reads nothing but git, and it still leaves a maintainer re-running CI by hand; the visible half is kept as a stderr line naming the commit the winning trailer came from, since stdout is the tag list the workflow feeds to `gh release create`. One refusal also comes back to life: `Release-bump: major` from a `v1` base was rounded down to a silent patch whenever it sat behind `HEAD`, and ADR 0009's rejection-by-name now reaches. Reading the history rather than the scripts turned up two more facts that change what "the same range" must mean: 75 of main's last 500 commits are TRUE merges, so a plain walk descends into the commits a merge brought IN — five commits here carry the trailer, touch `pkg/`, and are not on main's first-parent line — which would hand ADR 0007 §2's authorisation to anyone able to open a PR, so the walk is `--first-parent`; and `git log -1 --name-only` reports NO files for a merge, so a maintainer's trailer on one was scoped against an empty list and discarded, which is 14 of the 33 trailer-bearing commits on main, closed by asking for the diff against parent 1. An unwalkable `--range` is refused rather than read as no trailer, because a silent empty answer is the shape of this defect and must not be reintroducible by a flag — `docs/adr/0085-both-halves-of-a-release-read-the-same-range.md`
- ADR 0086 — creating an entry is not replacing one, and Windows says so in two bits: ADR 0084 gave Windows a DACL check with ONE question and one mask for two rules, and left four items deferred; three of them rested on a premise, and the measurement on `windows-latest` refuted all three. `checkDir` and `plantable` never asked the same question — the Unix pair differ over the sticky bit, since sticky governs UNLINKING an entry that exists while planting a component CREATES one at a free name — so the mask splits into `replaceRights` (`FILE_DELETE_CHILD`, `WRITE_DAC`, `WRITE_OWNER`), `createRights` (`FILE_ADD_SUBDIRECTORY`, `WRITE_DAC`, `WRITE_OWNER`), and a third that Unix has no need of at all: what the FILES created in the directory INHERIT, because a lock file there is created `0600` whatever the mode says while here it takes the directory's inheritable entries — so a directory nobody can unlink from can still hand every account the fencing ledger, and `INHERIT_ONLY_ACE`, which ADR 0084 skipped outright, is exactly where such a grant is written. That split is what makes `BUILTIN\Users` (`S-1-5-32-545`) the third "anybody" — ADR 0084 §Deferred item 1, CLOSED: it feared refusing `%ProgramData%`-shaped deployments, which was true only while both rules shared one mask, because `%ProgramData%` grants that group `FILE_ADD_FILE | FILE_ADD_SUBDIRECTORY` and NOT `FILE_DELETE_CHILD`, on itself and on every subdirectory — create-but-not-replace, which is what `0777|sticky` means and which ADR 0084 §Consequences said Windows had no ACL for. So a lock directory under `%ProgramData%` is ACCEPTED and a junction planted beside it is REFUSED, the two rules disagreeing about one identifier on a directory Windows itself configured. `%SystemRoot%\Temp`, which ADR 0084 named as the realistic case of its own breaking change and did not in fact refuse — the entry that makes it dangerous names `BUILTIN\Users` — is now refused, measured at `0x001f01ff`, full control. All EIGHT discretionary ACE shapes are decoded — ADR 0084 §Deferred item 3, CLOSED, because its premise was false: an ordinary NTFS directory DOES store an `ACCESS_ALLOWED_OBJECT_ACE` (`SetNamedSecurityInfoW` refuses it at ACL revision 2 with `ERROR_INVALID_ACL` and accepts it at revision 4), and the walk stepped past a grant of `FILE_DELETE_CHILD` to Everyone spelled that way. The identifier sits at offset 8 for the plain and callback shapes and at `12 + 16 x` the GUIDs the entry's own flags word announces for the object ones, and the size is checked against `AceSize` before the pointer is formed. A callback entry's conditional expression is never evaluated — there is no token to evaluate it against, the question being about an account that does not exist yet — so it is resolved the one way that cannot weaken the verdict: a conditional allow grants, a conditional deny denies nothing. The other two deferrals are re-argued and made PERMANENT rather than carried. The SACL is not read, and the privilege was never the reason: `windows-latest` is the built-in Administrator, holds `SeSecurityPrivilege` disabled, enables it on request and reads the list successfully — but no ACE type a SACL may carry GRANTS anything, since audit and alarm entries describe logging while the mandatory label, the scoped policy identifier and the access filter only restrict, so reading it could move the verdict only towards ACCEPTING, the direction the rule already fails in. And a planted lock file kept held is not a gap in the check but the PRICE of the row the check deliberately accepts: a lock cannot tell a squatter from a peer, because a peer holding it and an attacker holding it are the same bytes and the same kernel state, and it is the exposure `/tmp` has carried on Unix since ADR 0052. Two breaking changes in opposite directions, both named: a directory granting an unlink right or an inherited file write is refused where it was accepted, and a directory whose only broad grant is the right to create an entry is accepted where it was refused. No new symbol, no new error code, no `go.sum` entry — the same two lazy-bound `advapi32` exports, plus a third bound in a TEST because the check reads a list and never writes one — `docs/adr/0086-creating-an-entry-is-not-replacing-one-and-windows-says-so-in-two-bits.md`
- ADR 0087 — the root a caller named is a spelling it did not choose, and a probe that did not answer is not an answer: six of ADR 0076's seven deferred entries, closed. Four of them are ONE failure arriving four ways — the resolver answers, `Degraded()` is false, the set is not empty, and the query a caller actually makes returns false for a file that did change. `git rev-parse --show-toplevel` canonicalises, so a repository reached through a symbolic link was keyed under a root the caller never spells; ADR 0076's own answer (*query with paths resolved the same way `Resolve` resolved the root*) missed which spelling belongs to whom, because a caller chooses the paths it builds and does NOT choose the root — it hands one in and gets a different one back. One `EvalSymlinks` on the hint, taken only when the hint is not already inside the canonical root, buys an O(1) prefix rewrite per query and makes BOTH roots answer with neither privileged; an indirection anywhere else stays lexical and the port comment now says which is which. `--is-shallow-repository` landed in git 2.15 and a failed probe read as "not shallow", so `shallowState` reports whether it KNOWS and an unknown degrades — reproduced with a `git` shim on PATH, because the fact under test is what this package does with a probe that will not answer. `symbolic-ref` reports a pruned symref exactly as happily as a live one, which ADR 0076 called rare and which is the state of every clone taken before its upstream renamed master to main: `rev-parse --verify --quiet --abbrev-ref` resolves AND verifies in ONE invocation, so the fix costs no extra subprocess, and the full ref path is passed so rev-parse cannot be steered onto a `refs/origin/HEAD` a repository planted higher in its search order. The fourth was never on the list: ADR 0076 derived its hostile-`.git/config` guard from what a repository could make git EXECUTE, and `diff.srcPrefix`/`diff.dstPrefix`/`diff.mnemonicPrefix`/`diff.noprefix` execute nothing — they rename the `a//b/` prefixes, so `TrimPrefix(raw, "b/")` leaves them in place and the line ranges are filed under `<root>/DST/x.go`; measured as `ContainsFile` true and `ContainsLine` FALSE, with membership surviving only because the NUL-separated name-status pass carries no prefixes at all. `diff.noprefix` is pinned even though it survives by coincidence, because it OVERRIDES the two explicit prefixes and neutralising them without it changes nothing. `ShowFile` finally returns the `PathAbsent` ADR 0076 declared and never used: the probes were costed in the wrong place and belong on the FAILURE path, which is already exceptional, and it is TWO of them because `cat-file -e` answers "no such object" identically for an unresolvable commit and for an absent path. The `GitDir` memo's stated mitigation was false where it mattered — a root inside a PARENT repository resolves successfully, so `git init` there leaves the parent's git directory memoized — and two `os.Lstat` calls, ~2.9 µs against the ~2.7 ms subprocess they stand in for, re-validate it against `os.SameFile`. The copy entry closes with NO code change, and that is the finding: without `--find-copies-harder` git only offers a copy whose SOURCE was modified in the same changeset, so the source always carries its own `M`, `D` or rename-old record and removing the marking would remove nothing — while a wrong measurement would under-report, the one direction this domain must never move in. `Only git` stays open, because a second implementation is a feature and not a debt, with its question answered by inspection instead — `docs/adr/0087-the-root-a-caller-named-is-a-spelling-it-did-not-choose.md`
- ADR 0088 — a suite nothing runs is not a test suite, and a gate is a name CI says out loud: A suite nothing runs is not a test suite. ADR 0085 shipped 32 BATS tests for the two scripts that decide which tag every merge gets and recorded, under Deferred, that no CI lane and no `make` target executed them; re-verified before this change, `grep -rn bats Makefile .github/` returned nothing across all ten workflows. That is worse than having no tests, because none of the three defects the suite covers fails loudly — each produces a PLAUSIBLE version number. `separator=` is not an output separator: git parses the `%(trailers:…)` option list on commas, so `separator=,` leaves the separator EMPTY and concatenates repeated trailers — `Release-bump: mi` + `Release-bump: nor` reads back as the single value `minor` and cuts a minor release, measured on git 2.47.3 and invisible on review; the same construct also turns a doubled `Release-bump: minor` into `minorminor`, which ranks as unrecognised and silently degrades to a patch. A walk without `--first-parent` descends into the commits a merge brought IN, handing ADR 0007 §2's authorisation to anyone able to open a PR — five commits here carry the trailer, touch `pkg/`, and are not on main's first-parent line. A true merge reports NO files under a plain `--name-only`, so a maintainer's trailer on one was scoped against an empty list and counted for nothing — 14 of 33 trailer-bearing commits. Each was confirmed to make its own test go RED when reintroduced and GREEN when reverted: the suite had never been red, because it was written alongside the fix. A fourth defect was found in the harness itself and not predicted by it — the disposable fixtures inherit the host's `core.hooksPath`, so a developer's own refusing `pre-commit` failed the suite in `setup` on correct code, and neither a repo-local `core.hooksPath` nor `GIT_CONFIG_GLOBAL=/dev/null` outranks a hooks path injected at `-c` precedence; only `--no-verify` does. `release-scripts-test.sh` runs the suites and bats-core is a prerequisite, not a vendored artefact: the runner resolves it from PATH and exits 127 naming three install routes rather than cloning a third-party repository on every run, because `make lint` already refuses network egress. `ci-gates-check.sh` holds the manifest — a listed gate must have a target, be `.PHONY`, and be invoked by `bazel-ci.yml` — and lists its own target, so the commit that drops it from CI fails it. Confirmed red in all three failure modes before being wired green. The lane is a sibling job, `shell-gates`: neither gate needs Bazel or Go, and these are the first `make` invocations in `bazel-ci.yml`'s history, which is exactly why the Makefile↔CI link needed asserting — it did not exist
- ADR 0089 — a file that cannot cut a release cannot size one: A file that cannot cut a release cannot size one. ADR 0007 §2 required a behavioural change in `internal/**` observable through `pkg` to be a MINOR, while its very next row gated the only way to ask for one on the commit touching `pkg/` — so the table demanded something it gave no way to express. Measured against the unmodified script: a commit touching only `internal/service/svc.go` with a well-formed `Release-bump: minor` cut `pkg/v0.1.1`, while `compute-bumps.sh` cut the release for it anyway through rdeps and tagged the whole graph. The scope it did enforce protected nobody: the repository merges with `squash_merge_commit_message: COMMIT_MESSAGES`, so GitHub composes the squash message from the contributor's own commit bodies rather than the maintainer's, and a commit touching only `pkg/v1/foo/CLAUDE.md` with `Release-bump: minor` cut `v0.2.0` — a documentation file sizing a release that the same file cannot cut, because `compute-bumps.sh:70` excludes `*/CLAUDE.md` and `*/BUILD.bazel` and `cut-tags.sh` never learned that exclusion. Both halves now share ONE notion of a file that counts: under `pkg/` or `internal/`, minus those two names. The net is a hardening, not a relaxation — widening makes the required minor expressible, excluding closes the cheapest bypass, and after both, sizing a release requires touching code that actually cuts one. The two are correct only together, and the history proves it: `3f681412` touched `pkg/v1/resilience/CLAUDE.md` AND three `.go` files under `internal/service/resilience/`, so the exclusion alone would have dropped its only `pkg/` path and SILENCED the misplaced-trailer refusal that the widening keeps — measured: that commit's `Release-bump: minor` sits at line 99 of a 126-line message, git parses NOTHING from it, so what the pair rescues is the alarm and not the value, and the exclusion alone would have re-created issue #217 inside the fix for #218. Per-commit `bazel query rdeps` is deliberately not repeated in `cut-tags.sh`: it runs only once `compute-bumps.sh` has already emitted `pkg`, so the release is happening and only its size is open. Four BATS tests, red against the pre-fix script and green after, with a `pkg/` code control that passes both ways so a rule answering no to everything cannot pass. Deliberately NOT a fix for issue #220: the shared notion still counts `_test.go`, and measured against `8addc948` (the commit that cut `pkg/v0.4.1`, two files, both tests, both internal) the old `^pkg/` rule says no while `counts_for_release` says YES — this widens what may SIZE that release rather than stopping it, and `compute-bumps.sh`, which alone decides whether to cut, is byte-identical to main here
- ADR 0090 — a port named in public must be implementable in public: `internal/kernel/clock` was already NAMED in the public API and still could not be used. Every SDK configuration taking a time source aliases an internal struct whose `Clock` field is typed `clock.Clock` or `clock.Timed`, so the compiler printed `want NewTicker(time.Duration) clock.Ticker` at a consumer's OWN call site — while `Waiter.NewTimer`/`NewTicker` return types declared behind the `internal/` firewall, and Go compares a method signature by TYPE IDENTITY rather than by method set. Both escapes were compiled rather than argued: a locally declared interface with the identical method set yields `have … Ticker / want … clock.Ticker`, and importing the kernel package is refused outright. The inventory is measured by resolving every `pkg/v1` alias to its internal type — 25 exported clock-typed fields, 23 published, **9** typed `clock.Timed` and therefore unimplementable downstream, 14 typed `clock.Clock` and merely unnameable with `System` out of reach; a hand-written list of the nine had EIGHT and missed `lock.MemoryConfig`, which is why the ADR ships the script rather than the number. So nine domains — scheduler, lock (×3), session, sql, health, lifecycle, queue — could be USED downstream but never tested deterministically, and `ManualClock`, 13 KB behind 20 KB of tests, was unreachable. `pkg/v1/clock` publishes the kernel package IN FULL as aliases and nothing else — an alias gives type identity where a redeclaration would ship while fixing nothing, pinned by a negative control that still fails to compile. The ADR 0039 freeze is taken with its halves SEPARATED rather than inherited quietly: `Clock` was already bindingly frozen through `pkg/v1/cache.Config`, while `Waiter`, `Timed`, `Timer` and `Ticker` become so NOW, because until this change no downstream double could exist to break. `ManualClock` is concrete but carries ZERO exported fields, so it can only be built through `NewManualClock` and ADR 0040's v0 licence is not spent. `System` stays a `var` mirroring the kernel, and the doc states it is a value and not an override hook, rather than the facade quietly choosing a different shape from the thing it re-exports. No dependency, no error code, no behaviour change, and nothing else promoted — `worker`, `batcher`, `singleflight` and the rest stay internal, each its own decision — `docs/adr/0090-a-port-named-in-public-must-be-implementable-in-public.md`
- ADR 0091 — a single trust anchor is a key with no way out: `internal/service/entitlement` held `vendor []byte`, ONE key that every path read, so an installation whose anchor had to change was blocked from BOTH sides — a roster signed by a new key B came back `ErrRosterUnsigned`, the SPOOFING sentinel, from a document the vendor genuinely signed, and it came back in `rosterFrom` which `Verify` reaches BEFORE `RequiresUpdate`, so the mandatory-update floor that exists to say "upgrade this binary" was never read; while a release signed by B was refused by the anchor A the running installation still held. The only route left was out of band, on every machine, in a domain whose whole point is that revocation is real for cooperative installs. The anchor becomes an **ORDERED LIST** and a roster is authentic when it verifies against ANY entry — the whole of what rotation needs and nothing more: publish under B, installations carrying {A,B} accept it, installations carrying only {A} keep reading A until they are updated, and NEITHER SIDE HAS TO BE UPDATED FIRST, which is the property one anchor cannot have at any ordering. Several accepted keys are still ONE SIGNER: authentication establishes that the vendor issued these bytes and nothing else, so no quorum is taken and none is offered. The tension is written down rather than mitigated — an anchor on the list is a key whose compromise is ACCEPTED while it is listed, including the key whose compromise prompted the rotation, and no mechanism here fixes that because a client cannot be told "stop trusting A" by a document signed with A. What bounds it is that the list is ORDERED, BOUNDED at `maxAnchors = 4` and a BUILD decision: no roster field, no environment variable and no cache file can extend it, and a key leaves the list the way it entered. Past the bound the TAIL is dropped and logged — ADR 0031's clamp half, because a build mistake answered by refusing every verification is an outage over a misconfiguration one log line names precisely. An EMPTY list refuses EVERY document under `ErrRosterUnsigned` with a new `condition` and **no sixteenth code**, for the reason recorded when a sixteenth was last refused: the caller that maps sentinels to advice is downstream and an unknown code falls through to its default. The anchor loop obeys `currentRoster`'s own rule — move on from a document an anchor cannot AUTHENTICATE, never from one it authenticated and the rules then refused — because an expired roster tried against the next anchor comes back as a forgery claim, and an operator whose publisher had merely fallen behind would be told somebody is impersonating the vendor. The ratchet reads its mark through the SAME list: a bundle cached under a still-accepted anchor is still the vendor's statement about when it signed, and reading it against one key would drop the whole anti-rollback distance at exactly the moment an installation is least able to re-establish it. The single-key form IS the one-element list rather than a second representation, and a nil vendor stays a one-element list holding a MALFORMED key so it keeps naming `key_bytes` instead of an absent anchor. `pkg/v1/entitlement.New` keeps its signature byte-for-byte; `NewWithAnchors` and `(*Service).WithAnchors` are added beside it. A data shape and a function set, never an interface, so ADR 0039/0040's sibling rule is not engaged. No new error code, no `codeRangeOwners` entry, no dependency — `docs/adr/0091-a-single-trust-anchor-is-a-key-with-no-way-out.md`
- ADR 0092 — a possession proof that cannot name the key it is about: `matchSubject` called `Fingerprint(subject)`, compared the answer ITSELF against `roster.SubjectFor(subject).Fingerprint`, and then called `ProvePossession(subject)` — so the AUTHORISED fingerprint never traversed the port, and no implementation of `Identity` could bind its proof to it. Everything that can write the key material gets to act in the window between the two calls — a rotation landing mid-verification, a mounted volume remounted, a `<uuid>.pub` replaced on purpose — and the implementation signs with whatever the second call finds, having never been told which key the decision was about. The ceiling was MEASURED by a consumer rather than assumed: `ktn-linter` pushed it as far as three methods allow, memoising the key object across the two calls and refusing a proof when nothing is memoised, and recorded the residual in its own `pkg/license/CLAUDE.md`. That is change DETECTION, not BINDING — it can tell that something moved, never what it was supposed to be. The consumer proposed `ProvePossession(subject, authorised string) error`: one parameter, no new method. **That shape is refused and the capability is not.** `Identity` is reachable through `pkg/v1/entitlement.Identity`, a type ALIAS, so ADR 0039's rule binds — any port reachable through a `pkg/v1` alias is frozen the same way — and ADR 0040 §4 removes the escape in as many words: "is it an interface? Extend with a sibling. **The version does not matter**; structural satisfaction breaks callers at any version." The argument that settles it goes past the ADRs, though: **widening would not buy what it appears to buy**, because an implementation can satisfy the two-parameter signature and ignore the second argument — it compiles, it returns nil, nothing notices — so binding is the implementation's choice under either shape. What widening DOES guarantee is that every existing implementation stops compiling until somebody edits it, and it invalidates a method the implementer believed was correct rather than merely adding one; in this repository alone that is `SSHIdentity` plus four test doubles across three packages, and outside it the blast radius is unbounded and invisible. So the capability ships as `coreent.BoundProver` — one method, `ProvePossessionFor(subject, authorised string) error` — preferred by `(*Service).prove` through a type assertion, with the three-method call as the fallback: ADR 0052's `Deadliner` mechanism, where the ABSENCE of the sibling is how a caller discovers which world it is in. The refusal taxonomy does not grow by one entry: a bound proof over material that is not the authorised material refuses with `ErrKeyMismatch`, which is exactly what "a local key that does not match the published fingerprint" means. The ssh implementation re-reads the key directory ONCE and compares `Fingerprint(pub)` against the authorised value by byte equality in the roster's own spelling, signing over THAT read — the comparison and the signature against one read is the property two separate calls cannot have — and its refusal names the subject in its FIELDS and neither the subject nor the authorised value in the wire-safe sentence, because this refusal is reachable by anyone who can write the key directory. The freeze is guarded EXECUTABLY as ADR 0039 §2 requires rather than by comment: a bare three-method double assigned to `Identity`, plus a second assertion that the same double is NOT a `BoundProver`, so folding the method into the port fails the first to COMPILE. What it does NOT close is stated in three places rather than guaranteed in one and caveated in another: an identity that does not implement the sibling keeps the window and is fully supported; binding is not freshness, since a proof is a statement about a moment by construction; and the engine's own comparison STAYS, because it is the cheap gate before the signing round trip and it is what produces `ErrKeyMismatch` for every three-method identity. Nothing breaks — `Identity` is byte-identical — and a five-step migration note ships with the ADR for an implementation that wants the binding. No new error code, no `codeRangeOwners` entry, no dependency — `docs/adr/0092-a-possession-proof-that-cannot-name-the-key-it-is-about.md`
- ADR 0093 — a sweep takes the zombie, never the status: the reaper answers every SIGCHLD with `wait4(-1)`, which collects ANY child — including one a `Process` handle spawned and is waiting for — and the kernel gives a zombie's status to exactly one wait, so when the sweep won, the handle's own wait failed with `ECHILD` and a child that exited 0 came back as `WAIT_FAILED`. Measured downstream at 5 of 120 clean exits under load, each read as a failure and restarted by an `on-failure` policy; reproduced here at 43/1000 with eight concurrent spawners and 1000/1000 when `Wait` runs after the sweep, and at 175–231/1000 under CPU load. A new service package, `internal/service/proc/childwait`, owns the question: `Spawn` forks and CLAIMS the child under a gate a sweep must pass before calling any pid an orphan, `ReapAny` is the SDK's only `wait4(-1)` and stores a claimed child's status on its claim under a lock held from before the collection until after the hand-off, and the owner still waits for its own child — reading the claim first, never waiting by pid for a child already collected, and reclaiming after an `ECHILD` once the hand-off has landed. So a process with no reaper is exactly as before, and no `Wait` depends on the reaper loop being alive. `waitid(WNOWAIT)` peeking was refused twice over — unreachable (no `Waitid` in go1.27.1's `syscall`, `x/sys` banned, raw syscalls `ENOSYS` on OpenBSD) and wrong (`P_ALL` reports the same first zombie forever, hiding every orphan behind a claimed one). Children spawned outside the SDK stay exposed, stated in three places. 0/1000 in every cell after, with and without `-race`; no new symbol, no error code, no dependency — `docs/adr/0093-a-sweep-takes-the-zombie-never-the-status.md`
- ADR 0094 — a test compiles where its package does, and macOS runs every one: `cross-build` only ran `go build`, which never compiles a `_test.go`, so `net/server`'s tests called helpers only Linux defined and failed to compile on darwin and windows, and a benchmark named `_linux_bench_test.go` compiled everywhere (Go reads a GOOS only right before `_test.go`) and broke windows. `cross-build` and `scripts/cross-platform-audit.sh` now `go vet` too (10 targets × 6 modules green); helpers sit where their callers compile (`testsockets_{,unix_,other_}internal_test.go`); Unix sockets in tests bind under `socketDir(t)` (macOS's 104-byte `sun_path`), a fixture standing for a canonical path resolves its temp dir (`/var` → `/private/var`); the macOS lane runs every package and gates, the Windows lane runs them as an inventory until clean — `docs/adr/0094-a-test-compiles-where-its-package-does.md`
- ADR 0095 — Windows runs every test and gates, and each of its nineteen failures was answered on its own terms: ADR 0094's inventory becomes a gate (`continue-on-error` removed). Production bugs fixed: `codec/multipart` decoded a filename through the host's `filepath.Base` (one body, two answers) and now keeps the last element after `/` or `\` everywhere; `net/server` counted no oversized datagram on Windows (`WSAEMSGSIZE`), reported a shard reason with `degraded=false`, and failed `unixgram`/`unixpacket` as `LISTEN_FAILED` (now `UNSUPPORTED_PLATFORM`); `vcs/git` keyed git's `/`-spelled root against `\`-spelled entries; `queue` judged directories by mode bits Windows synthesises (now the DACL, through `lock.GrantsAnyone`, refusing a list it cannot read where the lock accepts one); `selfupdate` downloaded for a replacement Windows cannot do (now refused first); `journald` dialled a socket family Windows lacks; a `NewMulti`/`FromConfig` Logger could never close its files (now an `io.Closer`). The rest were Unix premises — two clock readings assumed to differ, `0600` read as a mode, open files deleted, stale facade build tags — replaced by what Windows promises, with skips only where the fixture cannot exist — `docs/adr/0095-windows-runs-every-test-and-gates.md`
- ADR 0096 — a secret is a value no rendering writes down, a name with versions, and a key that rotates without breaking what it sealed: `secret.Value` redacts in every rendering and decodes from a string only; a frozen five-method `Store` over never-reused version numbers; memory, environment (`NAME_FILE`) and sealed file stores; a `Keyring` whose boxes carry their version; a `Rotator` that keeps at least two — `docs/adr/0096-a-secret-is-a-value-no-rendering-writes-down.md`
- ADR 0097 — a load says where each value came from, and a secret field is loaded as written: `LoadWithOrigins` / `LoadSchemaWithOrigins` return one `Origin{Key, Layer, Detail, Secret}` per leaf key (never the value), sources describe themselves through the `Describer` sibling, and a `secret.Value` field reads the environment's raw text instead of its JSON coercion; closes ADR 0028 / 0061 §Deferred secret items — `docs/adr/0097-config-provenance-and-secret-fields.md`
- ADR 0100 — a program reads what it was built from, and what it is doing, and asks git only about a working tree: `git.Head` answers a working tree's commit, its committer date and whether a TRACKED file differs, through three hardened invocations — the time read from the raw commit object because `git log` honours `log.showSignature`, the status with `--no-optional-locks`, a failed status an error and never "clean" — and the value lives with the engine, so the vcs port still models no commit (amends ADR 0076). `process.Build`/`ParseBuild` read `debug.BuildInfo` into modules that keep a RELEASE, a COMMIT and a local DIRECTORY apart (pseudo-versions split by `x/mod/module`, `+dirty` as `Modified`, a workspace module local with no directory, as measured); `process.Self` snapshots goroutines, heap, collections, the GC-pause and scheduling-latency distributions and CPU time (`getrusage`, or the runtime's estimate, flagged). Deferred: clean filters, measured running once during the hardened status and seven times during `Resolve`'s diff — `docs/adr/0100-a-program-reads-what-it-was-built-from-and-asks-git-only-about-a-working-tree.md`
- ADR 0101 — a secret shown is a secret replaced, and the bound is exact: `redact` (service `0.3.73.*`, facade `pkg/v1/redact`) renders a value, a JSON document, a text or log attributes for display with a NAME containing a configured word, a field DECLARED secret by a configurable tag (`redact:"secret"`, or a framework's own) or a field rule, and a URL's CREDENTIALS replaced — never mutating the input. The writer is the package's own over a `jsontext` reader, so every byte is counted before it is written: a result is never longer than its bound and always one well-formed value, a cut is a prefix, and credentials are scrubbed BEFORE a text is cut. Fixes three defects of the copy it replaces: cut-before-scrub showed a clipped password, the bound overshot, and `json:"-,"` dropped a declared secret. No core package: no port, the values are the engine's — `docs/adr/0101-a-secret-shown-is-a-secret-replaced-and-the-bound-is-exact.md`
- ADR 0102 — a document somebody else wrote is decoded one way, or not at all: `encoding/json`'s defaults accept a duplicate name (last wins), a case-only match, an unknown member, invalid UTF-8, trailing data through the streaming decoder, any size — and quote the input in their errors. `codec/strictjson` (service `0.3.72.*`, facade `pkg/v1/codec/strictjson`) decodes on `encoding/json/v2` with `RejectUnknownMembers`, reads at most the bound plus one byte, refuses a zero bound, keeps no decoder error in the chain and locates a failure through `PointerOf` only. `DecodeRequest` adds `http.MaxBytesReader` (413 and a closed connection), an empty body as its own code before the media type, and 415 for a body that does not declare JSON. Not a Format, and its own facade so a server does not link sixteen codecs — `docs/adr/0102-a-document-somebody-else-wrote-is-decoded-one-way-or-not-at-all.md`
- ADR 0103 — a bucket per caller, one backoff curve, and a retry that waits on the clock it is given: `NewKeyedRateLimiter` keeps a token bucket per `Key(ctx)`, at most `MaxKeys` (least recently used forgotten) and each forgotten after `IdleTimeout` on its own next call, with `Rate`/`Key`/`MaxKeys`/`IdleTimeout` refused at zero; `Backoff.Delay(n)` publishes the curve `NewRetry` waits and the retry builds from it; `RetryConfig.Clock` lets a `ManualClock` drive every backoff. Publishing the curve fixed a defect: the growth was converted to a `time.Duration` unchecked, and past 2^63 ns amd64 yields `math.MinInt64` — measured, a one-second retry with a one-hour ceiling waited `-2562047h47m16.85s` from attempt 35, a timer firing at once (amends ADR 0026) — `docs/adr/0103-a-bucket-per-caller-one-backoff-curve-and-a-retry-on-the-clock-it-is-given.md`
- ADR 0104 — an idle consumer sleeps until there may be work: both brokers implement a `Waker` sibling (ADR 0039) whose `Wake()` hands out a signal a `Publish` or a `Nack` in this process closes, and how long — a duration on the broker's clock, never an instant — until a retry falls due or a lease lapses; `Consume` takes the signal before its `Receive` so no wake is lost, and waits `min(PollInterval, In)`. `PollInterval` becomes a bound, left with another process's publications: twelve idle durable consumers measured 5.19 % of a core at 50 ms and 0.06 % at 5 s, now with no latency lost in-process. Two durable brokers over one directory share a weakly-held signal (amends ADR 0054) — `docs/adr/0104-an-idle-consumer-sleeps-until-there-may-be-work.md`
- Layer placement audit — `.claude/contexts/sdk-layer-placement-audit.md`
- Bazel adoption context — `.claude/contexts/bazel-9-go-sdk.md`
